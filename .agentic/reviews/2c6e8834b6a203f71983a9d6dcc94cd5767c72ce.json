{
  "reviewedSha": "2c6e8834b6a203f71983a9d6dcc94cd5767c72ce",
  "reviewer": "codex",
  "verdict": "approve",
  "blockingFindings": 0,
  "reviewedAt": "2026-08-05T03:59:01.629Z",
  "evidence": "Reading additional input from stdin...\nOpenAI Codex v0.145.0\n--------\nworkdir: /Users/bradleybond/Developer/crystalball/.worktrees/acc401\nmodel: gpt-5.6-sol\nprovider: openai\napproval: never\nsandbox: read-only\nreasoning effort: xhigh\nreasoning summaries: none\nsession id: 019fd00f-1e32-7c90-95bf-81a90d31956e\n--------\nuser\nYou are reviewing round 20 of PR #1625 (ACC-501 frozen correlation benchmark migration gate hardening), in src/services/correlation/bench-correlation-baseline.ts and docs/validation/ACC-501-MUTATION-PROOFS.md. Do NOT delegate to sub-agents — do the review yourself directly. This round responds to your own round 19 REQUEST_CHANGES, which had two findings, both accepted as correct: (1) oneHopSchemaVersionCheckReason's docstring still claimed the check was unreachable through the public validator via any caller-supplied manifest, which was true before round 18b's guard restructuring but is now false since toSchemaVersion is deliberately excluded from the blanket identity comparison specifically so this check is reachable — the docstring was rewritten to describe current, correct reachability; (2) the round 18b mutation-proof result in the doc was transcribed as '223 pass / 1 fail', which is arithmetically impossible for a 223-test suite with one failure — corrected to '223 tests / 222 pass / 1 fail' matching the actual recorded npx tsx --test output. Read the diff (piped via stdin), confirm both fixes are accurate and complete, and check there are no other stale claims or arithmetic errors remaining anywhere in ACC-501-MUTATION-PROOFS.md or in bench-correlation-baseline.ts's comments/docstrings relating to this migration-guard work across all rounds. Run npx tsx --test src/services/correlation/__tests__/bench-correlation.test.mts and npm run typecheck:all as QA/QC. End your response with a single line starting with 'Verdict: ' followed by APPROVE or REQUEST_CHANGES.\n\n<stdin>\ndiff --git a/docs/validation/ACC-501-MUTATION-PROOFS.md b/docs/validation/ACC-501-MUTATION-PROOFS.md\nindex 6beca619..772af184 100644\n--- a/docs/validation/ACC-501-MUTATION-PROOFS.md\n+++ b/docs/validation/ACC-501-MUTATION-PROOFS.md\n@@ -2,12 +2,18 @@\n \n Date: 2026-08-04\n \n-Reviewed implementation commit: `c5e63061`\n+Reviewed implementation commit: `870b08f6` (round 14) plus the round-15 working\n+tree on `claude/acc-501-round-14` — round 15 addressed a Codex REQUEST_CHANGES\n+review of PR #1625.\n \n-The focused suite was green before mutation at `182 pass / 0 fail`.\n-All mutations below began from an empty `git status --short`. Each edit was\n-confirmed with `git diff` before the targeted test ran, then reverted with\n-`apply_patch`.\n+The focused suite was green before mutation at `182 pass / 0 fail` for the\n+round-14 mutations below, and grew to `216 pass / 0 fail` (full\n+`bench-correlation.test.mts`) plus `409 pass / 0 fail` (`npm run\n+test:correlation`) by round 15. All mutations began from a `git status\n+--short` containing only this session's legitimate round-15 diff (no\n+unrelated changes); each file's SHA-256 was recorded before and after every\n+mutation to confirm exact restoration, and each edit was confirmed with `git\n+diff`/`git status --short` before its targeted command ran.\n \n ## Previous-baseline quality pin\n \n@@ -105,7 +111,576 @@ confirmed with `git diff` before the targeted test ran, then reverted with\n   checking for an initial baseline”.\n - Restored checksum: `852aa154f9cd1eb8027abdc2d70f0ce4b9af0cef95bbf21fa209145e138353c5`.\n \n+## Round 15 — learned-rule resync self-attestation\n+\n+Codex's review of PR #1625 found `checkLearnedRuleResync` validated only the\n+report's own internal consistency, never re-deriving `afterRetirement`\n+against a live engine — so a producer that started computing it\n+arithmetically (`installed.filter(id => id !== retiredId)`) instead of\n+calling `engine.getRules()` would satisfy every check while the underlying\n+removal path went unmeasured. The fix added `verifyResyncProbe()`\n+(`__bench__/rule-probe-verify.ts`), which constructs its own fresh\n+`CorrelateEngine`, runs the same install→retire→resync sequence independently\n+of the producer, and the gate now demands the two agree.\n+\n+- Files: `src/services/correlation/bench-correlation.ts` (before\n+  `62b008e868e5e7e0e73ee59b645670828149181727f510a1881e217c8a03020f`),\n+  `src/services/correlation/learned-rules.ts` (before\n+  `5f96ebbd460397ef75aef59ec50023d4b4e5d6a3420aed5975b6943a2bb38546`).\n+- Mutation 1 alone: changed `probeLearnedRuleResync`'s `afterRetirement` from\n+  `learnedIdsOf()` (a live `engine.getRules()` query) to\n+  `installed.filter((id) => id !== retiredId)` (self-attested arithmetic).\n+  Result: `npm run bench:correlation` still **PASSED** — the arithmetic\n+  answer happens to equal the true engine state when the underlying removal\n+  actually works, so this alone doesn't prove the removal path is exercised.\n+  This matches the framing of the defect precisely: self-attestation isn't\n+  wrong on the happy path, only silent on the path where the system it claims\n+  to have queried actually breaks.\n+- Mutation 2 (combined with mutation 1, recreating the historical bug the\n+  file's own doc comment describes — \"Deleting `engine.unregisterRule` from\n+  `syncLearnedRules()` left a retired coupling permanently installed... while\n+  the function REPORTED it removed\"): removed the\n+  `engine.unregisterRule(existing.id)` call from `syncLearnedRules` in\n+  `learned-rules.ts`, so retirement stops actually removing the rule from the\n+  engine while `removed += 1` still increments.\n+- Targeted result: `npm run bench:correlation` → **FAIL**.\n+- Failing assertion: `the learned-rule resync probe does not reproduce: the\n+  report says {...\"afterRetirement\":[\"learned:cyber->finance\", ...4 ids]...}\n+  and re-running the retirement against a live engine gives\n+  {...\"afterRetirement\":[...5 ids, including the one reported retired]...}` —\n+  `verifyResyncProbe()`'s independently-constructed engine still has the rule\n+  installed; the producer's self-attested field claims it is gone.\n+- Restored checksums: both files matched their pre-mutation SHA-256 exactly;\n+  `git status --short` returned only the pre-existing round-15 diff.\n+\n+## Round 15 — v12→v13 migration self-consistent forgery\n+\n+Codex also found `validateCorrelationBenchV12ToV13Migration` reconstructed\n+its own \"previous v12 baseline\" comparison object from data available at\n+validation time, with nothing pinning that reconstruction to the actual\n+reviewed v12 payload — a forged previous baseline that moved a value in\n+lockstep on both sides of the comparison would pass. The fix added\n+`previousPayloadDigest`, computed once from the real `origin/main` v12\n+baseline and pinned in `CORRELATION_BENCH_V12_TO_V13_MIGRATION`, checked via\n+`benchBaselinePayloadDigest()` against whatever baseline is actually being\n+migrated from.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Before checksum:\n+  `dd078f9e8d6044b3eb1d7e454f8867a37032d6f27397d52b026d96a1675b7d6c`.\n+- Mutation: short-circuited the payload-digest check with `if (false && ...)`,\n+  disabling it while leaving `previousPayloadDigest` itself and every other\n+  check in place.\n+- Confirmed diff: the digest comparison branch became dead code.\n+- Targeted result: `node --import tsx --test\n+  src/services/correlation/__tests__/bench-correlation.test.mts` →\n+  `214 pass / 1 fail`.\n+- Failing assertion: exactly `refuses a previous baseline whose payload is\n+  not the reviewed one` failed; no other test in the 215-test file was\n+  affected, confirming the disabled check — not some other path — was what\n+  that test exercises.\n+- Restored checksum:\n+  `dd078f9e8d6044b3eb1d7e454f8867a37032d6f27397d52b026d96a1675b7d6c`.\n+\n+## Round 15 — the \"broken tolerance\" re-seed test was not constructed\n+\n+Codex's third finding was a P2 on\n+`refuses to re-seed over a broken tolerance without --force`: it ran\n+`--seed` against the committed baseline (which passes today) and accepted\n+either `Seeded from the current run` or `REFUSED` as a pass — a CLI that\n+always printed one of those two strings regardless of what the comparator\n+did would satisfy the assertion. It never constructed a baseline the live\n+run actually violates, so it proved the CLI can print those words, not that\n+`--seed` compares anything.\n+\n+The fix replaces it with two tests built on the CLI's own\n+`--previous-baseline <path>` override:\n+\n+- **Refusal case**: writes a temp copy of the committed baseline with\n+  `couplingPrecision: 1.5` (unreachable — precision is a fraction) and\n+  `tolerances.couplingPrecisionDrop: 0`, runs `--seed --previous-baseline\n+  <temp>`, and asserts exit code `1`, `REFUSED` plus the specific `miner\n+  coupling precision` reason on stderr, and that stdout carries no seeded\n+  JSON (`\"schemaVersion\"` absent).\n+- **Positive control**: writes an unmodified, internally consistent copy of\n+  the committed baseline as `--previous-baseline` and asserts exit code `0`,\n+  `Seeded from the current run` on stderr, no `REFUSED`, and a parseable\n+  seeded object on stdout — proving the comparator ran and passed on its own\n+  merits rather than the refusal path being unreachable.\n+\n+Two earlier attempts at the positive control failed for reasons worth\n+recording: raising `couplingPrecision` alone past its drop tolerance without\n+loosening the tolerance tripped the refusal (expected); loosening the\n+tolerance to `1` tripped a *different* guard — `bench-correlation-baseline.ts`\n+refuses any tolerance width above a `0.1` ceiling (\"a tolerance wide enough\n+to absorb the whole measurement disarms its gate while still looking\n+armed\"); and setting `couplingPrecision: 0` with a `0.05` tolerance tripped a\n+third guard against non-positive baseline metrics (\"a non-positive baseline\n+permanently disarms the gate it feeds\"). All three are the gate's own\n+defenses working correctly, not obstacles to route around — the final\n+version validates against an object with no manufactured internal\n+inconsistency at all.\n+\n+- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n+- No source mutation was needed here — the previous test was a coverage gap\n+  in the *test*, not a defect in `bench-correlation.ts` or\n+  `bench-correlation-baseline.ts`, so this section documents construction and\n+  verification, not a mutation/restore round-trip. Verified by running both\n+  new tests: refusal case exits `1` with the expected reason text; positive\n+  control exits `0` with a parseable candidate. Full suite: `216 pass / 0\n+  fail`.\n+\n+## Round 16 — Codex REQUEST_CHANGES on round 15 (four findings)\n+\n+Round 15 was sent for cross-agent review and came back `Verdict:\n+REQUEST_CHANGES` with one P1 and three P2 findings. All four are fixed here.\n+\n+### P1 — migration source remained forgeable via unpinned tolerances\n+\n+`V13_MAY_MOVE` excludes `tolerances` from `benchBaselinePayloadDigest` so an\n+ordinary (non-migration) reseed can edit tolerances freely — but\n+`validateCorrelationBenchV12ToV13Migration` carried the caller-supplied\n+`previousV12.tolerances` straight into the seeded candidate and, via\n+`checkMigrationSharedGates`, into grading, with nothing authenticating it.\n+Codex's repro: widening `couplingPrecisionDrop` from `0.02` to `0.1` in the\n+supplied previous baseline still returned `{ok:true,reasons:[]}`.\n+\n+Fix: added `previousTolerancesDigest` to `CorrelationBenchMigrationV12ToV13`,\n+pinned once via a new `benchTolerancesDigest()` helper computed over the real\n+reviewed v12 `tolerances` block, and validated inside\n+`validateCorrelationBenchV12ToV13Migration` independently of the\n+`V13_MAY_MOVE` exemption — so tolerances remain editable at ordinary reseed\n+time but a migration can't be graded against unreviewed tolerances.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Before checksum: `dd078f9e8d6044b3eb1d7e454f8867a37032d6f27397d52b026d96a1675b7d6c`\n+  (same file identity as the round-15 entry above, prior to this round's edits).\n+- Verified via `refuses a previous baseline whose tolerances are not the\n+  reviewed ones — the shared-gate forgery Codex found` in\n+  `bench-correlation.test.mts`: reproduces Codex's exact repro\n+  (`couplingPrecisionDrop: 0.02 → 0.1`) against the fixed code and confirms\n+  `ok:false` with the new `does not carry the reviewed v12 tolerances`\n+  reason. No source mutation/restore round-trip needed for this one — it's a\n+  new check with no prior code path to mutate; the negative test above\n+  demonstrates it fires, and `accepts the real additive transition with its\n+  real, unforged tolerances` demonstrates the pinned digest matches the real\n+  reviewed value so genuine migrations still pass.\n+\n+### P2 — refusal test never reached `checkDrop`\n+\n+The round-15 refusal test used `couplingPrecision: 1.5`, which trips an\n+earlier baseline range-validity guard (`[0,1]`) before ever reaching the\n+tolerance-comparison logic (`checkDrop`) the test claimed to exercise — so it\n+proved the CLI refuses invalid input, not that the tolerance gate works.\n+\n+Fix: rewrote the test to use `meanTruePairConfidence: 1` (valid — sits at the\n+metric's own ceiling) with `meanTruePairConfidenceDrop: 0`, forcing\n+`checkDrop`'s own `baseline - live > tolerance` branch to fire for real\n+(`report.meanTruePairConfidence` is provably below `1`). Asserts the actual\n+`checkDrop` diagnostic format (`/exceeds .* tolerance/`), not just `REFUSED`.\n+\n+- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n+- No source mutation needed — this was a test-construction defect, the same\n+  category as the original round-15 finding it's fixing. Verified by running\n+  the corrected test: exits `1`, stderr matches `mean true-pair confidence`\n+  and `exceeds .* tolerance`, stdout carries no seeded JSON.\n+\n+### P2 — positive control too permissive\n+\n+The round-15 positive-control test only checked `typeof seeded.schemaVersion\n+=== 'number'`, which would pass even if the CLI dropped or corrupted every\n+other field.\n+\n+Fix: the positive control now compares the seeded output against the\n+committed baseline field-by-field (union of both objects' keys, so a dropped\n+field is caught too), excluding only the bookkeeping fields an ordinary\n+reseed is allowed to touch (`schemaVersion`, `seededAt`, `note`,\n+`reportDigest`, `witnessed`) — reachable because the corpus is frozen and the\n+benchmark is deterministic, so a fresh seed against an unmodified previous\n+baseline reproduces every graded field exactly.\n+\n+- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n+- Verified: the strengthened assertion passes against the real CLI output\n+  (`refuses to re-seed against a --previous-baseline whose tolerance the live\n+  run actually violates` / `seeds cleanly against a --previous-baseline the\n+  live run genuinely satisfies` both still pass; the latter now exercises the\n+  field-by-field comparison, not just a `typeof` check).\n+\n+### P2 — one-hop migration not enforced at runtime\n+\n+`manifest.toSchemaVersion === CORRELATION_BENCH_SCHEMA_VERSION` was only\n+asserted by a unit test (`pins the migration to exactly one hop`), not by the\n+validator function itself — a future schema bump that forgot to retire or rev\n+this migration would pass every check in\n+`validateCorrelationBenchV12ToV13Migration` and silently become a two-hop\n+migration wearing a one-hop label.\n+\n+Fix: added a runtime check inside `validateCorrelationBenchV12ToV13Migration`\n+comparing `manifest.toSchemaVersion` against the live\n+`CORRELATION_BENCH_SCHEMA_VERSION` constant, pushing a refusal reason if they\n+diverge.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Note on testability: the function's manifest-identity guard\n+  (`JSON.stringify(manifest) !== JSON.stringify(CORRELATION_BENCH_V12_TO_V13_MIGRATION)`)\n+  refuses any manifest that isn't byte-identical to the pinned module\n+  constant, which means this new check's only reachable failure mode in\n+  production is the pinned constant itself drifting from\n+  `CORRELATION_BENCH_SCHEMA_VERSION` — not independently mutable via a test\n+  argument without also tripping the identity guard first (both reasons\n+  would fire together; the identity guard firing is not a bypass, it's proof\n+  the two constants can't drift independently). Verified via `the runtime\n+  one-hop check is real code, not just a test-level assertion` — confirms\n+  the real manifest and the real schema-version constant agree, and that\n+  `validateCorrelationBenchV12ToV13Migration` returns `ok:true` under that\n+  agreement, so the added check does not falsely refuse the real migration.\n+\n+### P2 — union-of-keys drop detection was untested (mutation proof)\n+\n+The migration validator iterates the union of `candidate` and `before` keys\n+specifically so a field present in the reviewed baseline but no longer\n+produced by the live seed function is visible (`before`-only keys) — not just\n+fields the candidate happens to still carry. No existing test forced that\n+distinction: the existing \"sees a forbidden field that was DROPPED\" test\n+deletes a field from `before`, but `candidate` still carries that same key\n+(sourced live from `report`), so candidate-only iteration would already catch\n+it — that test doesn't actually prove the union is load-bearing.\n+\n+Added a complementary test — `sees a field the reviewed baseline HAD that the\n+live candidate no longer produces at all` — using a `before` with an extra\n+field (`legacyRetiredField: 42`) that the candidate-producing path never\n+emits, so only union iteration visits it.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Before checksum: `04696770b63a5ba784692bb0ddbac66743f26b862d9ac9926177191974b3dbe6`.\n+- Mutation: `const keys = [...new Set([...Object.keys(candidate),\n+  ...Object.keys(before)])]` → `const keys = [...new\n+  Set([...Object.keys(candidate)])]` (dropped the `before` half of the union).\n+- Confirmed diff: `git diff` showed exactly the one-line key-union narrowing.\n+- Targeted result: `node --import tsx --test\n+  src/services/correlation/__tests__/bench-correlation.test.mts` →\n+  `219 pass / 1 fail`.\n+- Failing assertion: exactly `sees a field the reviewed baseline HAD that the\n+  live candidate no longer produces at all` failed — actual reason was\n+  `does not carry the reviewed v12 payload` (from the earlier payload-digest\n+  guard, which also fires for this fixture) instead of the expected\n+  `legacyRetiredField moved from 42 to undefined` (which only the\n+  union-iterating field loop produces) — confirming the union half of the key\n+  set is what makes that specific reason reachable. No other test in the\n+  220-test file was affected.\n+- Restored checksum: `04696770b63a5ba784692bb0ddbac66743f26b862d9ac9926177191974b3dbe6`\n+  (matches before checksum exactly).\n+\n+Full suite after all four round-16 fixes: `220 pass / 0 fail`\n+(`bench-correlation.test.mts`), `413 pass / 0 fail` (`npm run\n+test:correlation`), `npm run bench:correlation` → PASS, `npx tsc --noEmit`\n+(both tsconfig.json and tsconfig.api.json) → 0 errors.\n+\n+## Round 17 — Codex REQUEST_CHANGES on round 16 (three findings)\n+\n+Round 16 was sent for a second cross-agent review and came back a second\n+genuine `Verdict: REQUEST_CHANGES` with three P2 findings, all fixed here.\n+\n+### P2 — positive control still permitted corrupted seed output\n+\n+Codex re-ran the positive-control test (`seeds cleanly against a\n+--previous-baseline the live run genuinely satisfies`) and found the round-16\n+`mayDiffer` exclusion set (`schemaVersion`, `seededAt`, `note`,\n+`reportDigest`, `witnessed`) was wider than necessary — a fresh, unmodified\n+reseed only ever legitimately changes `seededAt` (a wall-clock timestamp);\n+`schemaVersion`, `note`, `reportDigest`, and `witnessed` are all reproduced\n+exactly. With the wider set, the test would still pass against a CLI that\n+emitted a forged `schemaVersion`, a stale `reportDigest`, or a missing\n+`witnessed` block.\n+\n+Fix: narrowed `mayDiffer` to `{'seededAt'}` only.\n+\n+- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n+- No source mutation needed — this was a test-construction defect (the\n+  exclusion set was over-broad), the same category as the round-15/16\n+  findings it's fixing. Verified by re-running the narrowed test against the\n+  real CLI: still passes, confirming `schemaVersion`, `note`, `reportDigest`,\n+  and `witnessed` are in fact reproduced exactly on a genuine reseed, and\n+  that the test would now fail if any of them weren't.\n+\n+### P2 — runtime one-hop guard had no load-bearing mutation proof\n+\n+The round-16 test for the `toSchemaVersion` runtime check asserted only that\n+the module constant and the live schema version agree — its comment claimed\n+\"if the runtime check in the source were deleted, this still passes,\"\n+treating the check as untestable given the earlier manifest-identity guard.\n+Codex asked for an actual mutation, not that assumption.\n+\n+Performing the mutation disproved the comment's own claim: mutating the\n+*value* of `CORRELATION_BENCH_V12_TO_V13_MIGRATION.toSchemaVersion` (not\n+deleting the runtime check) leaves the manifest self-identical (it's compared\n+against itself via the same imported constant), so the identity guard still\n+passes — and the runtime check then correctly refuses, proving it IS\n+reachable through the real validator, just not via a caller-supplied\n+mismatched manifest object.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`.\n+- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\n+  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (bypasses the TS literal type\n+  since `tsx` transpiles without type-checking; the mismatch is real at\n+  runtime).\n+- Confirmed diff: `git diff` showed exactly the one-line value change.\n+- Targeted result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n+- Failing tests: `accepts the real additive transition` — failed with reason\n+  `v12→v13 migration manifest targets schemaVersion 99, but the compiled gate\n+  is schemaVersion 13 — this migration is no longer the one-hop path to the\n+  live schema` (the real validator's refusal, proving the check fires\n+  through the public function); `pins the migration to exactly one hop` and\n+  `the runtime one-hop check is real code, not just a test-level assertion`\n+  — both failed on `99 !== 13`, as expected for assertions against the\n+  mutated constant. No other test in the 220-test file was affected.\n+- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n+  (matches before checksum exactly).\n+- The test's comment was corrected to remove the disproven \"if deleted, this\n+  still passes\" claim and point to this proof instead of asserting an\n+  untested assumption.\n+\n+### P2 — tolerances-authentication check (P1 fix) lacked mutation proof\n+\n+Round 16's write-up said the new `previousTolerancesDigest` check had \"no\n+prior code path to mutate\" since it was newly added. Codex correctly pointed\n+out this reasoning doesn't hold — a newly added check still has code that can\n+be short-circuited to prove the corresponding test depends on it, same as\n+any other check in this function.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n+  (same file identity as the toSchemaVersion proof above, run as a separate\n+  mutation after restoring that one).\n+- Mutation: `if (benchTolerancesDigest(previousV12.tolerances) !==\n+  manifest.previousTolerancesDigest)` → `if (false &&\n+  benchTolerancesDigest(...) !== manifest.previousTolerancesDigest)`\n+  (short-circuits the check to never fire).\n+- Confirmed diff: `git diff` showed exactly the one-line `false &&` insertion.\n+- Targeted result: `bench-correlation.test.mts` → `219 pass / 1 fail`.\n+- Failing test: exactly `refuses a previous baseline whose tolerances are not\n+  the reviewed ones — the shared-gate forgery Codex found` failed on\n+  `true !== false` (the test's `assert.equal(ok, false)` — with the check\n+  short-circuited, the forged-tolerance fixture is accepted as `ok:true`). No\n+  other test in the 220-test file was affected, confirming this is the sole\n+  test depending on the check.\n+- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n+  (matches before checksum exactly).\n+\n+Full suite after all three round-17 fixes: `220 pass / 0 fail`\n+(`bench-correlation.test.mts`), `npx tsc --noEmit` (both tsconfig.json and\n+tsconfig.api.json) → 0 errors.\n+\n+## Round 18 (Codex round-3 REQUEST_CHANGES)\n+\n+Codex's third review of PR #1625 found a real flaw in the round-17 proof\n+above:\n+\n+> \"[P2] The one-hop guard still lacks the required revert-the-fix mutation\n+> proof. Round 17 mutated the manifest value, not the guard at\n+> bench-correlation-baseline.ts:383. I independently short-circuited that\n+> guard; all three relevant tests remained green (3 pass / 0 fail).\n+> Therefore the test comment and the evidence write-up incorrectly say guard\n+> deletion was performed and disproved. The toSchemaVersion: 99 mutation\n+> does prove the negative path is reachable, but not that removing the fix\n+> turns the suite red. Add a test that asserts the one-hop refusal reason\n+> through the validator and fails when this guard is disabled.\"\n+\n+This is correct. Round 17 mutated only the pinned constant's *value*\n+(`toSchemaVersion: 99`) with the guard's *code* intact, and confirmed 3\n+tests failed. But with the real, unmutated inputs the constant and the\n+compiled schema version are always in sync (13 === 13), so the guard's\n+branch condition (`manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION`)\n+is never true whether or not the guard code exists — disabling the guard\n+alone, with in-sync inputs, is a no-op test that proves nothing about the\n+guard's necessity.\n+\n+### Combined mutation — the genuine revert-the-fix proof\n+\n+To prove the guard's *code* (not just the drifted value) is load-bearing,\n+both mutations were applied simultaneously: drift the constant AND disable\n+the guard that would have caught the drift.\n+\n+- File: `src/services/correlation/bench-correlation-baseline.ts`.\n+- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`.\n+\n+**Step 1 — guard restored, constant drifted only** (reproduces round 17's\n+proof, captured with exact identities this time):\n+\n+- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\n+  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (line 288). Guard code at line\n+  383 untouched.\n+- Result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n+- Failing tests, with exact assertion text:\n+  1. `round 15 — the v12→v13 schema bump is a pinned migration, not a\n+     skipped check` — `false !== true` against the expected refusal reason\n+     `\"v12→v13 migration manifest targets schemaVersion 99, but the\n+     compiled gate is schemaVersion 13 — this migration is no longer the\n+     one-hop path to the live schema\"`.\n+  2. `pins the migration to exactly one hop` — `99 !== 13`.\n+  3. `the runtime one-hop check is real code, not just a test-level\n+     assertion` — `99 !== 13`.\n+\n+**Step 2 — guard ALSO disabled, constant still drifted** (the genuine\n+revert-the-fix proof):\n+\n+- Additional mutation: `if (manifest.toSchemaVersion !==\n+  CORRELATION_BENCH_SCHEMA_VERSION) {` → `if (false &&\n+  manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION) {` (line\n+  383), applied on top of the still-active `toSchemaVersion: 99` mutation.\n+- Result: `bench-correlation.test.mts` → `218 pass / 2 fail`.\n+- The two remaining failures are exactly `pins the migration to exactly one\n+  hop` and `the runtime one-hop check is real code, not just a test-level\n+  assertion` — both fail on the raw constant-value assertion (`99 !== 13`),\n+  which is unrelated to the guard's presence and would fail regardless.\n+- Critically, `round 15 — the v12→v13 schema bump is a pinned migration, not\n+  a skipped check` (test 1 above) — which had failed on `false !== true` in\n+  Step 1 — now **passes** in Step 2. With the guard's code disabled, the\n+  validator's `ok` result flips back to `true` even though the manifest\n+  still targets a stale schema (99 vs the compiled 13). This is the\n+  false-positive accept: disabling the guard's *code*, not just having a\n+  drifted value, is what causes the migration to be silently accepted. This\n+  is the genuine revert-the-fix proof Codex asked for.\n+- Restored checksum after reverting both mutations:\n+  `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n+  (matches before checksum exactly). `git diff --stat` on the file showed no\n+  diff.\n+- Full suite after restoration: `bench-correlation.test.mts` →\n+  `220 pass / 0 fail`.\n+\n+### Permanent test (not just a doc-recorded mutation experiment)\n+\n+Codex explicitly asked for \"a test that asserts the one-hop refusal reason\n+through the validator and fails when this guard is disabled.\" Because the\n+manifest-identity guard at line 355 makes the `toSchemaVersion` branch\n+unreachable via any caller-supplied manifest — the only way to exercise it\n+through `validateCorrelationBenchV12ToV13Migration` is to mutate the pinned\n+module constant itself, which cannot be part of a permanent, always-runnable\n+unit test — the comparison was extracted into a new exported pure function,\n+`oneHopSchemaVersionCheckReason(manifestToSchemaVersion, compiledSchemaVersion)`,\n+and the validator now calls it instead of inlining the check. Two new\n+permanent tests in `bench-correlation.test.mts` call this real function\n+directly (not a re-implementation):\n+\n+- `oneHopSchemaVersionCheckReason refuses a manifest that no longer targets\n+  the compiled schema` — asserts the exact refusal reason string for\n+  `(99, CORRELATION_BENCH_SCHEMA_VERSION)`.\n+- `oneHopSchemaVersionCheckReason accepts a manifest that targets the\n+  compiled schema` — asserts `null` for matching versions.\n+\n+These tests call the exact function the validator invokes, so deleting or\n+weakening `oneHopSchemaVersionCheckReason`'s own logic fails them directly.\n+Full suite after this change: `222 pass / 0 fail`. `npm run typecheck:all` →\n+0 errors.\n+\n+**This claim turned out to be incomplete** — see round 18b below.\n+\n+## Round 18b (Codex round-4 REQUEST_CHANGES)\n+\n+> [P2] The permanent regression test still does not exercise validator\n+> wiring. Deleting the call at bench-correlation-baseline.ts:405 leaves both\n+> helper tests and the matching-input validator test green. Thus the claim\n+> at ACC-501-MUTATION-PROOFS.md:572 is false, and round 17's requested\n+> validator-level regression test remains missing. The combined manual\n+> mutation is valid evidence, but it is not permanent coverage.\n+\n+Correct. Testing `oneHopSchemaVersionCheckReason()` directly proves the\n+check's own logic is right, but proves nothing about whether\n+`validateCorrelationBenchV12ToV13Migration` still *calls* it — removing a\n+refusal check only produces false negatives (missed refusals), never false\n+positives on already-correct input, so deleting the call site left every\n+existing test green (the helper's own tests, and the validator's\n+matching-input `ok === true` test).\n+\n+### Root cause: the manifest-identity guard, again\n+\n+The reason this was untestable through the public validator is the same\n+guard flagged in round 15/17: `validateCorrelationBenchV12ToV13Migration`\n+did a blanket `JSON.stringify(manifest) !== JSON.stringify(pinned)`\n+comparison that early-returned before ANY caller-supplied manifest\n+differing in even one field — including `toSchemaVersion` — could reach a\n+field-specific check. A manifest built to differ from the pinned constant\n+only in `toSchemaVersion` was swallowed by the generic \"manifest does not\n+match\" refusal before ever reaching the one-hop check.\n+\n+### Fix: exclude `toSchemaVersion` from the blanket identity comparison\n+\n+`validateCorrelationBenchV12ToV13Migration` now destructures `toSchemaVersion`\n+out of the caller manifest, and strips it from a shallow copy of the pinned\n+constant, before the identity comparison — then checks it separately via\n+`oneHopSchemaVersionCheckReason` immediately after. (An initial destructuring\n+form of the pinned-side omission, `const { toSchemaVersion: _pinned, ...pinnedRest }`,\n+tripped `@typescript-eslint/no-unused-vars` / `sonarjs/no-unused-vars`; the\n+`delete`-based form below is what actually shipped.)\n+\n+```ts\n+const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n+const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n+delete pinnedRest.toSchemaVersion;\n+if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n+  reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n+  return { ok: false, reasons };\n+}\n+const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n+if (oneHopReason !== null) {\n+  reasons.push(oneHopReason);\n+}\n+```\n+\n+The comparison is against the independently-compiled `CORRELATION_BENCH_SCHEMA_VERSION`\n+constant, not the pinned constant's own `toSchemaVersion` field — comparing\n+against the latter would silently defeat the check's purpose, since it would\n+just compare the pinned constant against a caller manifest copying it,\n+never catching the pinned constant itself drifting from the compiled schema.\n+\n+### New permanent test — through the real validator\n+\n+`refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n+the compiled schema — validator wiring, not just the helper` builds\n+`{ ...manifest, toSchemaVersion: 99 }` (identical to the pinned migration in\n+every other field) and calls `validateCorrelationBenchV12ToV13Migration`\n+directly, asserting `ok === false` and the exact refusal text. This is now\n+reachable through the public validator for the first time, closing the gap\n+round 18b identified.\n+\n+### Mutation proof — deleting the call site fails the new test\n+\n+Checksum before mutation: `4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc`.\n+\n+**Mutation** — removed the validator's call to `oneHopSchemaVersionCheckReason`\n+(the four lines pushing `oneHopReason` into `reasons`), leaving everything\n+else, including `manifestToSchemaVersion`'s extraction, intact.\n+\n+**Result**: `223 tests / 222 pass / 1 fail`. The new validator-level test\n+failed exactly as expected:\n+\n+```\n+✖ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n+  the compiled schema — validator wiring, not just the helper\n+  AssertionError: Expected values to be strictly equal: true !== false\n+```\n+\n+`round 15 — the v12→v13 schema bump is a pinned migration, not a skipped\n+check` also failed, matching round 18's earlier combined-mutation proof —\n+consistent evidence that this is the same load-bearing check, now reachable\n+without mutating a module constant.\n+\n+**Restoration**: re-added the four-line call site. Checksum after restore:\n+`4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc` — exact\n+match. `git diff --stat` showed only the legitimate (committed) guard\n+restructuring, no residual mutation.\n+\n+**Full verification chain after this fix**:\n+\n+- `bench-correlation.test.mts` — `223 pass / 0 fail`\n+- `npm run typecheck:all` — 0 errors\n+- `npm run bench:correlation` — PASS, within tolerance of the committed baseline\n+- `npm run test:correlation` — `416 pass / 0 fail`\n+\n ## Restoration\n \n-Final checksums matched all pre-mutation values and final\n-`git status --short` was empty before this evidence file was added.\n+Final checksums matched all pre-mutation values for every mutated file across\n+round 14, round 15, round 16, round 17, round 18, and round 18b, and\n+`git status --short` after each restoration showed only the legitimate\n+source diff for that round — no residual mutation.\ndiff --git a/src/services/correlation/__tests__/bench-correlation.test.mts b/src/services/correlation/__tests__/bench-correlation.test.mts\nindex a60572ae..a3660fc3 100644\n--- a/src/services/correlation/__tests__/bench-correlation.test.mts\n+++ b/src/services/correlation/__tests__/bench-correlation.test.mts\n@@ -53,6 +53,8 @@ import {\n   nearMissEvents,\n   positiveEvents,\n } from '../__bench__/rule-probes.ts';\n+import { verifyRuleProbes } from '../__bench__/rule-probe-verify.ts';\n+import { pairToEdge } from '../../intelligence/situation-store-v2.ts';\n \n const here = path.dirname(fileURLToPath(import.meta.url));\n const ROOT = path.join(here, '..', '..', '..', '..');\n@@ -549,6 +551,8 @@ describe('what the corpus is built to stress', () => {\n       eventA: { id: seed.eventIdA },\n       eventB: { id: seed.eventIdB },\n       confidence: 0.5,\n+      edgeType: 'co-located',\n+      detectedAt: new Date(Date.UTC(2026, 5, 1, 12, 0, 0)),\n     })) as unknown as Parameters<typeof gradeEnginePairs>[0];\n \n     const graded = gradeEnginePairs(twice, plantedTruePairKeys(), decoyEventIds());\n@@ -725,6 +729,11 @@ describe('the gate', () => {\n           fromId: decoy,\n           toId: partner,\n           confidence: 0.5,\n+          detectedAtMs: Date.UTC(2026, 5, 1, 12, 0, 0),\n+          confidenceDetailDigest: '0'.repeat(32),\n+          evidenceEdgeType: 'co-located',\n+          evidenceFromId: decoy,\n+          evidenceToId: partner,\n         }],\n         isTruePair: false,\n         decoyEmissions: 1,\n@@ -1843,7 +1852,13 @@ describe('the gate re-derives the numbers it used to take on the report\\'s word'\n       { ...report, ruleProbes: rewritten }, baseline,\n     );\n     assert.equal(ok, false);\n-    assert.ok(reasons.some((r) => r.includes('ruleProbes[0].nearMisses')));\n+    // Since round 14 the gate re-executes the fixtures itself, so a re-aimed\n+    // clause is caught by the disagreement rather than by the ledger digest.\n+    assert.ok(\n+      reasons.some((r) => r.includes('ruleProbes[0].nearMisses')\n+        || r.includes('does not reproduce')),\n+      reasons.join(' | '),\n+    );\n   });\n \n   it('rejects a report that drops an advertised measurement outright', () => {\n@@ -2277,9 +2292,9 @@ describe('the re-seed emitter produces a baseline that passes', () => {\n     assert.equal(ok, true, `re-seeded baseline should pass: ${JSON.stringify(reasons)}`);\n   });\n \n-  it('pins every v12 replay, family, candidate, inhibition, and stream identity field', () => {\n+  it('pins every v13 replay, family, candidate, inhibition, and stream identity field', () => {\n     const seeded = seedCorrelationBenchBaseline(report, carriedOver, '2026-08-03');\n-    assert.equal(seeded.schemaVersion, 12);\n+    assert.equal(seeded.schemaVersion, 13);\n     assert.equal(seeded.replayMode, report.replayMode);\n     assert.deepEqual(seeded.streamDigests, report.streamDigests);\n     assert.deepEqual(seeded.multipleTestingFamily, report.multipleTestingFamily);\n@@ -2805,7 +2820,15 @@ describe('rule probes pin the fixture, not only the verdict', () => {\n     };\n     const { ok, reasons } = compareCorrelationBenchToBaseline(forged, baseline);\n     assert.equal(ok, false);\n-    assert.ok(reasons.some((r) => r.startsWith('probes ledger digest moved')));\n+    // Round 14 added an independent re-execution that runs BEFORE the ledger\n+    // digests and short-circuits, so a moved fixture is now named by whichever\n+    // of the two speaks first. Both name the probe ledger rather than the\n+    // whole report, which is what this test is about.\n+    assert.ok(\n+      reasons.some((r) => r.startsWith('probes ledger digest moved')\n+        || /reports fixtureDigest/.test(r)),\n+      reasons.join(' | '),\n+    );\n   });\n });\n \n@@ -3077,3 +3100,539 @@ describe('round 13 — the halves round 12 missed', () => {\n     );\n   });\n });\n+\n+describe('round 14 — the gate stops taking the producer at its word', () => {\n+  const report = runCorrelationBenchmark();\n+  const baseline = loadBaseline();\n+\n+  it('re-derives every probe verdict independently of the producer', () => {\n+    // The finding: replacing all 73 near-miss executions with the constant\n+    // `rejected: true` reproduced the digest exactly, because the honest output\n+    // and the forged output are the same bytes. No output pin can separate\n+    // them — only a second executor can.\n+    const mine = verifyRuleProbes();\n+    assert.equal(mine.length, report.ruleProbes.length);\n+    assert.deepEqual(\n+      mine.map((p) => p.ruleId),\n+      report.ruleProbes.map((p) => p.ruleId),\n+    );\n+    for (const [i, p] of mine.entries()) {\n+      assert.deepEqual(p, report.ruleProbes[i], `probe ${p.ruleId} disagrees with the report`);\n+    }\n+  });\n+\n+  it('fails when the report claims a near-miss the engine actually matched', () => {\n+    const forged = {\n+      ...report,\n+      ruleProbes: report.ruleProbes.map((p, i) => (\n+        i === 0\n+          ? { ...p, nearMisses: p.nearMisses.map((n) => ({ ...n, rejected: !n.rejected })) }\n+          : p\n+      )),\n+    };\n+    const { ok, reasons } = compareCorrelationBenchToBaseline(forged, baseline);\n+    assert.equal(ok, false);\n+    assert.ok(\n+      reasons.some((r) => r.includes('does not reproduce')),\n+      reasons.join(' | '),\n+    );\n+  });\n+\n+  it('the verdicts do not depend on the instant they are graded at', () => {\n+    // `now` reaches only `detectedAt`, so a different instant must not move a\n+    // single match. If it ever does, the fixtures have drifted into the engine's\n+    // recency behaviour and the probe stopped being a pure matcher test.\n+    const later = verifyRuleProbes(new Date(Date.UTC(2027, 0, 9, 4, 30, 0)));\n+    assert.deepEqual(later, verifyRuleProbes());\n+  });\n+\n+  it('every rule matches its positive fed BACK TO FRONT', () => {\n+    // `correlate-engine.ts:177` tries (b, a) when (a, b) misses. Every corpus\n+    // and fixture was antecedent-first, so deleting that branch changed nothing.\n+    for (const p of report.ruleProbes) {\n+      assert.equal(p.reversedMatched, true, `${p.ruleId} does not survive reversed input`);\n+    }\n+  });\n+\n+  it('fails when a reversed positive stops matching', () => {\n+    const forged = {\n+      ...report,\n+      ruleProbes: report.ruleProbes.map((p, i) => (\n+        i === 0 ? { ...p, reversedMatched: false, reversedDirection: null } : p\n+      )),\n+    };\n+    const { ok } = compareCorrelationBenchToBaseline(forged, baseline);\n+    assert.equal(ok, false);\n+  });\n+\n+  it('exercises every accepted branch of every disjunctive matcher', () => {\n+    // A positive that satisfies BOTH branches of an OR and a near-miss that\n+    // defeats both cannot see one branch being lost. Each branch gets its own\n+    // isolated positive.\n+    // Pinned per rule, OUTSIDE the fixture roster both the producer and the\n+    // independent executor read. `>= 4` let three rules lose every disjunct\n+    // they have and still pass, because both witnesses would simply stop\n+    // asking about them together.\n+    const EXPECTED_BRANCH_COUNTS: Readonly<Record<string, number>> = {\n+      'airquality-wildfire': 2,\n+      'conflict-displacement': 4,\n+      'earthquake-infrastructure': 2,\n+      'earthquake-tsunami': 1,\n+      'space-weather-infrastructure': 4,\n+      'weather-aviation': 1,\n+      'weather-wildfire': 3,\n+    };\n+    const withBranches = report.ruleProbes.filter((p) => p.disjuncts.length > 0);\n+    assert.deepEqual(\n+      Object.fromEntries(withBranches.map((p) => [p.ruleId, p.disjuncts.length])),\n+      EXPECTED_BRANCH_COUNTS,\n+      'the disjunct roster moved — add or remove the pin in a reviewed diff',\n+    );\n+    for (const p of withBranches) {\n+      for (const d of p.disjuncts) {\n+        assert.equal(d.matched, true, `${p.ruleId} lost branch ${d.branch}`);\n+      }\n+    }\n+  });\n+\n+  it('fails when one accepted branch of a disjunction stops matching', () => {\n+    const idx = report.ruleProbes.findIndex((p) => p.disjuncts.length > 0);\n+    const forged = {\n+      ...report,\n+      ruleProbes: report.ruleProbes.map((p, i) => (\n+        i === idx\n+          ? { ...p, disjuncts: p.disjuncts.map((d, j) => (j === 0 ? { ...d, matched: false } : d)) }\n+          : p\n+      )),\n+    };\n+    const { ok, reasons } = compareCorrelationBenchToBaseline(forged, baseline);\n+    assert.equal(ok, false);\n+    assert.ok(reasons.some((r) => r.includes('branch')), reasons.join(' | '));\n+  });\n+\n+  it('moves the digest when a pair is emitted at the epoch', () => {\n+    const stale = {\n+      ...report,\n+      pairs: report.pairs.map((r) => ({\n+        ...r,\n+        emissions: r.emissions.map((e) => ({ ...e, detectedAtMs: 0 })),\n+      })),\n+    };\n+    assert.notEqual(benchReportDigest(stale), baseline.reportDigest);\n+    const { ok, reasons } = compareCorrelationBenchToBaseline(stale, baseline);\n+    assert.equal(ok, false);\n+    assert.ok(reasons.some((r) => r.includes('detectedAt')), reasons.join(' | '));\n+  });\n+\n+  it('fails when the confidence BREAKDOWN disappears behind the scalar', () => {\n+    const flat = {\n+      ...report,\n+      pairs: report.pairs.map((r) => ({\n+        ...r,\n+        emissions: r.emissions.map((e) => ({ ...e, confidenceDetailDigest: null })),\n+      })),\n+    };\n+    const { ok, reasons } = compareCorrelationBenchToBaseline(flat, baseline);\n+    assert.equal(ok, false);\n+    assert.ok(reasons.some((r) => r.includes('breakdown')), reasons.join(' | '));\n+  });\n+\n+  it('projects every pair into the evidence graph the store really builds', () => {\n+    for (const row of report.pairs) {\n+      for (const e of row.emissions) {\n+        const edge = pairToEdge({\n+          ruleId: e.ruleId,\n+          edgeType: e.edgeType,\n+          eventA: { id: e.fromId } as never,\n+          eventB: { id: e.toId } as never,\n+          confidence: e.confidence,\n+          detectedAt: new Date(e.detectedAtMs),\n+        } as never);\n+        assert.equal(e.evidenceEdgeType, edge.type);\n+        assert.equal(e.evidenceFromId, edge.sourceEventId);\n+        assert.equal(e.evidenceToId, edge.targetEventId);\n+      }\n+    }\n+  });\n+\n+  it('fails when the projection contradicts the engine it came from', () => {\n+    // EDGE_TYPE_MAP inverted: real situation edges asserting the opposite\n+    // relationship, with every benchmark number unchanged.\n+    const inverted = {\n+      ...report,\n+      pairs: report.pairs.map((r) => ({\n+        ...r,\n+        emissions: r.emissions.map((e) => ({ ...e, evidenceEdgeType: 'contradicts' })),\n+      })),\n+    };\n+    const { ok, reasons } = compareCorrelationBenchToBaseline(inverted, baseline);\n+    assert.equal(ok, false);\n+    assert.ok(reasons.some((r) => r.includes('evidence graph')), reasons.join(' | '));\n+  });\n+\n+  it('proves a retired learned rule actually leaves the engine', () => {\n+    const p = report.learnedRuleResync;\n+    assert.ok(p.installed.length >= 2);\n+    assert.ok(p.installed.includes(p.retiredId));\n+    assert.ok(!p.afterRetirement.includes(p.retiredId));\n+    assert.equal(p.afterRetirement.length, p.installed.length - 1);\n+    assert.equal(p.reportedRemoved, 1);\n+    assert.equal(p.reportedAdded, 0);\n+    assert.equal(p.builtInsIntact, true);\n+  });\n+\n+  it('fails when a retired rule stays registered but is reported removed', () => {\n+    const stuck = {\n+      ...report,\n+      learnedRuleResync: {\n+        ...report.learnedRuleResync,\n+        afterRetirement: report.learnedRuleResync.installed,\n+      },\n+    };\n+    const { ok, reasons } = compareCorrelationBenchToBaseline(stuck, baseline);\n+    assert.equal(ok, false);\n+    assert.ok(reasons.some((r) => r.includes('still registered')), reasons.join(' | '));\n+  });\n+\n+  const scriptPath = path.join(here, '..', '..', '..', '..', 'scripts', 'correlation-benchmark.mts');\n+  const repoRoot = path.join(here, '..', '..', '..', '..');\n+\n+  it('refuses to re-seed against a --previous-baseline whose tolerance the live run actually violates', () => {\n+    // Round 15: the prior version of this test only ran --seed against the\n+    // committed baseline, which passes today, so it could not distinguish a\n+    // working refusal path from one that always prints \"Seeded\" — a CLI that\n+    // never compared anything would satisfy the old assertion.\n+    //\n+    // The first attempt at fixing this used `couplingPrecision: 1.5` with a\n+    // zero drop tolerance, meant to force `checkDrop` to fire — but 1.5 is\n+    // outside the baseline's own [0,1] range-validity guard, so the CLI\n+    // refused for an EARLIER reason (\"baseline value 1.5 is outside [0,1]\")\n+    // and never reached the tolerance-comparison logic this test claims to\n+    // exercise. `meanTruePairConfidence` is used the same way `checkDrop`\n+    // uses every other metric, so pinning it to its valid ceiling (1) with a\n+    // zero drop tolerance forces the real comparator: the live run's actual\n+    // mean true-pair confidence is provably below 1, so `previous(1) -\n+    // live(<1) > tolerance(0)` fires inside `checkDrop` itself.\n+    const committed = loadBaseline();\n+    const dir = mkdtempSync(path.join(tmpdir(), 'acc501-broken-tolerance-'));\n+    const brokenPath = path.join(dir, 'broken-previous-baseline.json');\n+    try {\n+      const broken: CorrelationBenchBaseline = {\n+        ...committed,\n+        meanTruePairConfidence: 1,\n+        tolerances: { ...committed.tolerances, meanTruePairConfidenceDrop: 0 },\n+      };\n+      writeFileSync(brokenPath, JSON.stringify(broken, null, 2));\n+\n+      const seed = spawnSync(\n+        'npx',\n+        ['tsx', scriptPath, '--seed', '--previous-baseline', brokenPath],\n+        { encoding: 'utf8', cwd: repoRoot },\n+      );\n+\n+      assert.equal(seed.status, 1, `${seed.stdout}${seed.stderr}`.slice(-600));\n+      assert.match(seed.stderr, /REFUSED/);\n+      assert.match(seed.stderr, /mean true-pair confidence/);\n+      assert.match(seed.stderr, /exceeds .* tolerance/);\n+      // A refusal must not also hand back a usable candidate on stdout.\n+      assert.doesNotMatch(seed.stdout, /\"schemaVersion\"/);\n+    } finally {\n+      rmSync(dir, { recursive: true, force: true });\n+    }\n+  });\n+\n+  it('seeds cleanly against a --previous-baseline the live run genuinely satisfies', () => {\n+    // The counterpart to the refusal case above: the same\n+    // --previous-baseline mechanism, but pointed at an unmodified, internally\n+    // consistent copy of the reviewed baseline (which today's live run\n+    // legitimately meets), so the positive control proves the comparator ran\n+    // and passed on its own merits rather than the CLI having skipped it.\n+    const committed = loadBaseline();\n+    const dir = mkdtempSync(path.join(tmpdir(), 'acc501-satisfied-tolerance-'));\n+    const copyPath = path.join(dir, 'unmodified-previous-baseline.json');\n+    try {\n+      writeFileSync(copyPath, JSON.stringify(committed, null, 2));\n+\n+      const seed = spawnSync(\n+        'npx',\n+        ['tsx', scriptPath, '--seed', '--previous-baseline', copyPath],\n+        { encoding: 'utf8', cwd: repoRoot },\n+      );\n+\n+      assert.equal(seed.status, 0, `${seed.stdout}${seed.stderr}`.slice(-600));\n+      assert.match(seed.stderr, /Seeded from the current run/);\n+      assert.doesNotMatch(seed.stderr, /REFUSED/);\n+      const seeded = JSON.parse(seed.stdout) as CorrelationBenchBaseline;\n+      assert.equal(typeof seeded.schemaVersion, 'number');\n+      // The corpus is frozen and the benchmark is deterministic, so a fresh\n+      // seed against an unmodified previous baseline should reproduce every\n+      // graded field exactly — including schemaVersion, note, reportDigest,\n+      // and witnessed, none of which an ordinary reseed is entitled to\n+      // change. Only seededAt (a wall-clock timestamp) genuinely differs\n+      // between two runs. A wider exclusion set here would pass even if the\n+      // CLI emitted a forged schemaVersion, a stale reportDigest, or a\n+      // missing witness block — Codex caught this in round 16.\n+      const mayDiffer = new Set(['seededAt']);\n+      const seededRecord = seeded as unknown as Record<string, unknown>;\n+      const committedRecord = committed as unknown as Record<string, unknown>;\n+      const keys = [...new Set([...Object.keys(seededRecord), ...Object.keys(committedRecord)])];\n+      for (const key of keys) {\n+        if (mayDiffer.has(key)) continue;\n+        assert.deepEqual(\n+          seededRecord[key], committedRecord[key],\n+          `field \"${key}\" differs: committed=${JSON.stringify(committedRecord[key])} ` +\n+          `seeded=${JSON.stringify(seededRecord[key])}`,\n+        );\n+      }\n+    } finally {\n+      rmSync(dir, { recursive: true, force: true });\n+    }\n+  });\n+});\n+\n+describe('round 15 — the v12→v13 schema bump is a pinned migration, not a skipped check', () => {\n+  const report = runCorrelationBenchmark();\n+  const committed = loadBaseline();\n+  const carriedOver = { note: committed.note, tolerances: committed.tolerances };\n+  const manifest = correlationBaseline.CORRELATION_BENCH_V12_TO_V13_MIGRATION;\n+\n+  // The reviewed v12 baseline differed from today's only in schemaVersion,\n+  // seededAt, and reportDigest — that IS the claim the migration makes. The\n+  // reconstruction is only admissible because `previousPayloadDigest` pins it:\n+  // if any forbidden field differed from the real reviewed v12 baseline, this\n+  // object would digest to something else and the validator would refuse it.\n+  // The test below asserts exactly that before using it.\n+  const previousV12 = (): correlationBaseline.CorrelationBenchBaseline => ({\n+    ...seedCorrelationBenchBaseline(report, carriedOver, committed.seededAt),\n+    schemaVersion: 12,\n+    reportDigest: manifest.previousReportDigest,\n+  } as unknown as correlationBaseline.CorrelationBenchBaseline);\n+\n+  it('reconstructs the reviewed v12 payload exactly — the pin says so', () => {\n+    assert.equal(\n+      correlationBaseline.benchBaselinePayloadDigest(previousV12()),\n+      manifest.previousPayloadDigest,\n+    );\n+  });\n+\n+  it('refuses a previous baseline whose payload is not the reviewed one', () => {\n+    // The reviewer moved minedEdgeCount in BOTH the report and the supplied\n+    // previous baseline and the field-by-field loop compared the moved value to\n+    // itself. The payload pin is what makes that self-consistent forgery fail.\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      { ...report, minedEdgeCount: report.minedEdgeCount + 1 },\n+      { ...previousV12(), minedEdgeCount: report.minedEdgeCount + 1 },\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /does not carry the reviewed v12 payload/);\n+  });\n+\n+  it('sees a forbidden field that was DROPPED, not just moved', () => {\n+    const prev = previousV12() as unknown as Record<string, unknown>;\n+    delete prev['minedEdgeCount'];\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      prev as unknown as correlationBaseline.CorrelationBenchBaseline,\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /minedEdgeCount moved|reviewed v12 payload/);\n+  });\n+\n+  it('sees a field the reviewed baseline HAD that the live candidate no longer produces at all', () => {\n+    // The test above deletes a field from `before` — but candidate still has\n+    // that key (it comes straight from the live report), so it's caught even\n+    // by iterating candidate's own keys alone; it doesn't actually prove the\n+    // UNION is load-bearing. This is the complementary case: a field present\n+    // in the reviewed `before` that the candidate-producing side never emits\n+    // at all. Iterating only `Object.keys(candidate)` would never visit this\n+    // key, so the drop would be invisible; iterating the union catches it.\n+    const prev = { ...previousV12(), legacyRetiredField: 42 } as unknown as\n+      correlationBaseline.CorrelationBenchBaseline;\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      prev,\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /legacyRetiredField moved from 42 to undefined/);\n+  });\n+\n+  it('accepts the real additive transition', () => {\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      previousV12(),\n+      manifest,\n+    );\n+    assert.equal(ok, true, JSON.stringify(reasons));\n+  });\n+\n+  it('refuses a manifest that is not the pinned one', () => {\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      previousV12(),\n+      { ...manifest, reason: 'trust me' },\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /manifest does not match/);\n+  });\n+\n+  it('refuses a source that is not the reviewed v12 baseline', () => {\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      { ...previousV12(), reportDigest: 'f'.repeat(32) },\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /not the reviewed v12 report/);\n+  });\n+\n+  it('refuses a source whose corpus digest is not the reviewed corpus', () => {\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      { ...previousV12(), corpusDigest: 'e'.repeat(32) },\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /not the reviewed v12 corpus/);\n+  });\n+\n+  it('refuses a graded metric that moved under cover of the schema bump', () => {\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      { ...previousV12(), pairPrecision: 0.123456 },\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /additive-only, but pairPrecision moved/);\n+  });\n+\n+  it('refuses a report digest that did not move — nothing was added', () => {\n+    const seeded = seedCorrelationBenchBaseline(report, carriedOver, committed.seededAt);\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      { ...previousV12(), reportDigest: seeded.reportDigest },\n+      { ...manifest, previousReportDigest: seeded.reportDigest },\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /manifest does not match|digest did not move/);\n+  });\n+\n+  it('pins the migration to exactly one hop', () => {\n+    assert.equal(manifest.fromSchemaVersion, 12);\n+    assert.equal(manifest.toSchemaVersion, 13);\n+    assert.equal(manifest.toSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n+  });\n+\n+  it('the runtime one-hop check is real code, not just a test-level assertion', () => {\n+    // \"pins the migration to exactly one hop\" above only asserts a property\n+    // of the module constants; it says nothing about whether the VALIDATOR\n+    // FUNCTION would ever act on a mismatch. Because the manifest-identity\n+    // guard refuses any manifest that isn't byte-identical to the pinned\n+    // constant, a caller-supplied stale manifest is already caught by that\n+    // guard before reaching the toSchemaVersion check — so exercising the\n+    // toSchemaVersion check THROUGH validateCorrelationBenchV12ToV13Migration\n+    // is only reachable by mutating the pinned constant itself (recorded as\n+    // a genuine mutation proof, including the combined \"guard code also\n+    // disabled\" proof Codex asked for in round 18, in\n+    // docs/validation/ACC-501-MUTATION-PROOFS.md). To make the check's own\n+    // logic — not just the module constant — permanently, directly testable\n+    // without any source mutation, the comparison was extracted into\n+    // oneHopSchemaVersionCheckReason(). The tests immediately below call\n+    // that function directly, proving the check's own logic is correct.\n+    // Round 18b (Codex): that alone doesn't prove the VALIDATOR still calls\n+    // it — deleting the call site left those helper tests, and this test,\n+    // both green (removing a refusal only produces false negatives). The\n+    // manifest-identity guard was restructured to exclude toSchemaVersion\n+    // from its blanket comparison so a manifest differing ONLY in that\n+    // field reaches the check through the real validator; see the\n+    // \"validator wiring, not just the helper\" test below for the\n+    // permanent, mutation-free proof that the call site itself is intact.\n+    assert.equal(\n+      correlationBaseline.CORRELATION_BENCH_V12_TO_V13_MIGRATION.toSchemaVersion,\n+      CORRELATION_BENCH_SCHEMA_VERSION,\n+    );\n+    const { ok } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      previousV12(),\n+      manifest,\n+    );\n+    assert.equal(ok, true);\n+  });\n+\n+  it('oneHopSchemaVersionCheckReason refuses a manifest that no longer targets the compiled schema', () => {\n+    // This is the real function the validator calls (not a copy) — a future\n+    // edit that weakens or deletes this check fails this test directly,\n+    // without requiring a source-mutation experiment.\n+    const reason = correlationBaseline.oneHopSchemaVersionCheckReason(99, CORRELATION_BENCH_SCHEMA_VERSION);\n+    assert.equal(\n+      reason,\n+      'v12→v13 migration manifest targets schemaVersion 99, but the compiled gate is schemaVersion ' +\n+      `${String(CORRELATION_BENCH_SCHEMA_VERSION)} — this migration is no longer the one-hop path to the live schema`,\n+    );\n+  });\n+\n+  it('oneHopSchemaVersionCheckReason accepts a manifest that targets the compiled schema', () => {\n+    assert.equal(\n+      correlationBaseline.oneHopSchemaVersionCheckReason(CORRELATION_BENCH_SCHEMA_VERSION, CORRELATION_BENCH_SCHEMA_VERSION),\n+      null,\n+    );\n+  });\n+\n+  it('refuses a caller-supplied manifest whose toSchemaVersion alone drifts from the compiled schema — validator wiring, not just the helper', () => {\n+    // Round 18b (Codex): testing oneHopSchemaVersionCheckReason() directly\n+    // proves the CHECK is correct, but not that the VALIDATOR still CALLS\n+    // it — deleting that call site left both the helper tests and the\n+    // \"matching-input\" validator test green. The old manifest-identity\n+    // guard made this untestable through the public validator (any\n+    // toSchemaVersion mismatch was swallowed by the blanket identity\n+    // refusal before reaching the specific check), so the guard was\n+    // restructured to exclude toSchemaVersion from that blanket comparison\n+    // and check it separately. This test constructs a manifest identical to\n+    // the pinned migration in every OTHER field and calls the real\n+    // validator — no source mutation required.\n+    const staleManifest = { ...manifest, toSchemaVersion: 99 };\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      previousV12(),\n+      staleManifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(\n+      reasons.join(' '),\n+      /targets schemaVersion 99, but the compiled gate is schemaVersion/,\n+    );\n+  });\n+\n+  it('refuses a previous baseline whose tolerances are not the reviewed ones — the shared-gate forgery Codex found', () => {\n+    // P1: `tolerances` sits in V13_MAY_MOVE so `benchBaselinePayloadDigest`\n+    // skips it, letting an ordinary reseed edit tolerances freely. But\n+    // `validateCorrelationBenchV12ToV13Migration` was carrying\n+    // `previousV12.tolerances` straight into the seeded candidate with\n+    // nothing pinning it to the reviewed value — a caller could widen\n+    // `couplingPrecisionDrop` from 0.02 to 0.1 and the migration would still\n+    // report ok:true, because nothing here grades tolerances against\n+    // anything. `previousTolerancesDigest` closes that: a previousV12 whose\n+    // tolerances digest to something other than the reviewed block is\n+    // refused before any grading happens.\n+    const forged = {\n+      ...previousV12(),\n+      tolerances: { ...previousV12().tolerances, couplingPrecisionDrop: 0.1 },\n+    };\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      forged,\n+      manifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(reasons.join(' '), /does not carry the reviewed v12 tolerances/);\n+  });\n+\n+  it('accepts the real additive transition with its real, unforged tolerances', () => {\n+    assert.equal(\n+      correlationBaseline.benchTolerancesDigest(previousV12().tolerances),\n+      manifest.previousTolerancesDigest,\n+    );\n+  });\n+});\ndiff --git a/src/services/correlation/bench-correlation-baseline.ts b/src/services/correlation/bench-correlation-baseline.ts\nindex 4647b7bd..01ce4ac1 100644\n--- a/src/services/correlation/bench-correlation-baseline.ts\n+++ b/src/services/correlation/bench-correlation-baseline.ts\n@@ -43,8 +43,8 @@\n  */\n \n import type {\n-  BenchEdgeRow, BenchInhibitoryEdgeRow, BenchLearnedPairRow, BenchPairEmission, BenchPairRow, BenchRuleProbe,\n-  CorrelationBenchReport, EdgeVerdict,\n+  BenchEdgeRow, BenchInhibitoryEdgeRow, BenchLearnedPairRow, BenchPairEmission, BenchPairRow, BenchResyncProbe,\n+  BenchRuleProbe, CorrelationBenchReport, EdgeVerdict,\n } from './bench-correlation';\n // The gate reads planted truth DIRECTLY. A ledger row that carries its own\n // verdict and its own `isTruePair` flag is the report grading itself: rewrite\n@@ -55,6 +55,7 @@ import {\n   corpusDomains, decoyEventIds, digestRecords, pairKeyFor, plantedCouplingIndex,\n   plantedTruePairKeys,\n } from './__bench__/golden-streams';\n+import { verifyResyncProbe, verifyRuleProbes } from './__bench__/rule-probe-verify';\n import { DEFAULT_WINDOWS_MS, type MultipleTestingFamily } from './lead-lag';\n import { LEARNED_RULE_PREFIX, learnedRuleId } from './learned-rules';\n \n@@ -147,7 +148,7 @@ const TOLERANCE_CEILINGS: CorrelationBenchTolerances = {\n  * confusing reasons, only for the fields that happen to be gated. Pin the\n  * version and say so once, plainly.\n  */\n-export const CORRELATION_BENCH_SCHEMA_VERSION = 12;\n+export const CORRELATION_BENCH_SCHEMA_VERSION = 13;\n \n /**\n  * `goldenCorpusDigest()` emits exactly 32 lowercase hex characters. Comparing\n@@ -235,6 +236,221 @@ export interface CorrelationBenchBaseline {\n   tolerances: CorrelationBenchTolerances;\n }\n \n+/**\n+ * The v12 → v13 transition: ADDITIVE ONLY.\n+ *\n+ * Round 15 adds observations to the report (independent probe re-execution,\n+ * reversed positives, per-branch disjunct positives, the real `pairToEdge()`\n+ * projection, a learned-rule retirement probe, `detectedAt` and the confidence\n+ * breakdown). It changes no corpus and no engine behaviour, so the honest\n+ * statement of this migration is that EVERY graded number and every corpus\n+ * digest is byte-identical and only `reportDigest` moves — because the report\n+ * now carries fields it did not carry before.\n+ *\n+ * That is a stronger claim than the ordinary one-sided tolerances make, and it\n+ * is the only claim that fits: a schema bump short-circuits the normal\n+ * comparator before its tolerance loop runs, so without this the re-seed would\n+ * be waved through by a check that never executed.\n+ */\n+export interface CorrelationBenchMigrationV12ToV13 {\n+  fromSchemaVersion: 12;\n+  toSchemaVersion: 13;\n+  previousCorpusDigest: string;\n+  previousReportDigest: string;\n+  /**\n+   * Digest over the previous baseline's own carried-over payload.\n+   *\n+   * `previousReportDigest` names the reviewed v12 report but does not BIND the\n+   * baseline handed in to it: the reviewer moved `minedEdgeCount` in both the\n+   * report and the supplied previous baseline, left this manifest's report\n+   * digest alone, and the field-by-field loop compared the moved value to the\n+   * moved value and returned ok. The pin has to cover the payload the loop\n+   * actually reads, not a sibling string.\n+   */\n+  previousPayloadDigest: string;\n+  /**\n+   * Digest over the previous baseline's `tolerances` block specifically.\n+   *\n+   * `tolerances` sits in `V13_MAY_MOVE` because an ordinary (non-migration)\n+   * reseed legitimately edits it — but that same exemption let a caller-\n+   * supplied `previousV12.tolerances` flow into `checkMigrationSharedGates`\n+   * ungated: widening `couplingPrecisionDrop` from 0.02 to 0.1 still returned\n+   * `{ok:true}` because nothing pinned tolerances to the reviewed values.\n+   * This digest authenticates the tolerance block used for grading\n+   * independently of whether tolerances are allowed to move at reseed time.\n+   */\n+  previousTolerancesDigest: string;\n+  reason: string;\n+}\n+\n+export const CORRELATION_BENCH_V12_TO_V13_MIGRATION: CorrelationBenchMigrationV12ToV13 = {\n+  fromSchemaVersion: 12,\n+  toSchemaVersion: 13,\n+  previousCorpusDigest: '1a0377333099795fe0ccff8dc5a7a2cf',\n+  previousReportDigest: '354a7790613214a893698b1882eda0ae',\n+  previousPayloadDigest: '129ab5924c521a1897a907059de7ffac',\n+  previousTolerancesDigest: benchTolerancesDigest({\n+    couplingPrecisionDrop: 0.02,\n+    couplingRecallDrop: 0,\n+    pairPrecisionDrop: 0,\n+    pairRecallDrop: 0,\n+    edgeEvidenceSeparationDrop: 1,\n+    meanTruePairConfidenceDrop: 0.05,\n+    falseEdgeGrowth: 0,\n+    causalLearnedRuleShrink: 0,\n+    learnedRuleFalsePositiveGrowth: 0,\n+    learnedRulePairGrowth: 5,\n+    causalLearnedRulePairShrinkRatio: 0.5,\n+    enginePairShrink: 0,\n+    minedEdgeCountShrink: 10,\n+  }),\n+  reason:\n+    'ACC-501 round 15 adds probe/emission observations to the report. No corpus edit, '\n+    + 'no engine change, no graded metric movement.',\n+};\n+\n+/**\n+ * Fields the migration allows to differ. Everything else in the baseline has to\n+ * match the previous one exactly — an allowlist, so a field added later is\n+ * refused by default rather than silently exempted.\n+ */\n+const V13_MAY_MOVE: ReadonlySet<string> = new Set([\n+  'schemaVersion', 'seededAt', 'note', 'reportDigest', 'witnessed', 'tolerances',\n+]);\n+\n+/**\n+ * Digest over exactly the fields the migration forbids to move.\n+ *\n+ * Pinned in the manifest, so the previous baseline handed to the validator has\n+ * to BE the reviewed one rather than merely claim its report digest.\n+ */\n+export function benchBaselinePayloadDigest(baseline: CorrelationBenchBaseline): string {\n+  const src = baseline as unknown as Record<string, unknown>;\n+  const payload: Record<string, unknown> = {};\n+  for (const key of Object.keys(src).sort(compareStrings)) {\n+    if (V13_MAY_MOVE.has(key)) continue;\n+    payload[key] = src[key];\n+  }\n+  const records: string[] = [];\n+  canonicalRecords(payload, '', records);\n+  return digestRecords(records);\n+}\n+\n+/**\n+ * Digest over a `tolerances` block alone, so it can be authenticated\n+ * independently of `V13_MAY_MOVE` exempting it from `benchBaselinePayloadDigest`.\n+ */\n+export function benchTolerancesDigest(tolerances: CorrelationBenchBaseline['tolerances']): string {\n+  const records: string[] = [];\n+  canonicalRecords(tolerances, '', records);\n+  return digestRecords(records);\n+}\n+\n+/**\n+ * Pure one-hop schema check, extracted so it is directly testable in\n+ * isolation from the rest of validateCorrelationBenchV12ToV13Migration.\n+ * The validator's manifest-identity guard excludes toSchemaVersion from its\n+ * blanket comparison specifically so a caller-supplied manifest differing\n+ * only in this field reaches this check instead of being swallowed there.\n+ * Returns the refusal reason string, or null when the manifest still\n+ * targets the compiled schema.\n+ */\n+export function oneHopSchemaVersionCheckReason(\n+  manifestToSchemaVersion: number,\n+  compiledSchemaVersion: number,\n+): string | null {\n+  if (manifestToSchemaVersion !== compiledSchemaVersion) {\n+    return (\n+      `v12→v13 migration manifest targets schemaVersion ${String(manifestToSchemaVersion)}, but the ` +\n+      `compiled gate is schemaVersion ${String(compiledSchemaVersion)} — this migration is ` +\n+      'no longer the one-hop path to the live schema'\n+    );\n+  }\n+  return null;\n+}\n+\n+export function validateCorrelationBenchV12ToV13Migration(\n+  report: CorrelationBenchReport,\n+  previousV12: CorrelationBenchBaseline,\n+  manifest: CorrelationBenchMigrationV12ToV13,\n+): CorrelationBenchComparison {\n+  const reasons: string[] = [];\n+  // toSchemaVersion is deliberately excluded from this identity comparison —\n+  // it gets its OWN check below via oneHopSchemaVersionCheckReason(), so a\n+  // caller-supplied manifest that matches the pinned migration in every\n+  // OTHER field still reaches that check instead of being swallowed here.\n+  // Round 18: Codex found that folding toSchemaVersion into this blanket\n+  // identity guard made the schema check unreachable through the public\n+  // validator — the only way to exercise it was to mutate the pinned module\n+  // constant, which can't be a permanent regression test.\n+  const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n+  const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n+  delete pinnedRest.toSchemaVersion;\n+  if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n+    reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n+    return { ok: false, reasons };\n+  }\n+  const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n+  if (oneHopReason !== null) {\n+    reasons.push(oneHopReason);\n+  }\n+  if (previousV12.schemaVersion !== 12) {\n+    reasons.push(`v12→v13 migration source is schemaVersion ${previousV12.schemaVersion}, not 12`);\n+  }\n+  if (previousV12.corpusDigest !== manifest.previousCorpusDigest) {\n+    reasons.push('v12→v13 migration source is not the reviewed v12 corpus');\n+  }\n+  if (previousV12.reportDigest !== manifest.previousReportDigest) {\n+    reasons.push('v12→v13 migration source is not the reviewed v12 report');\n+  }\n+  if (benchBaselinePayloadDigest(previousV12) !== manifest.previousPayloadDigest) {\n+    reasons.push(\n+      'v12→v13 migration source does not carry the reviewed v12 payload — the baseline being ' +\n+      'migrated FROM is not the one that was reviewed, so every field-by-field comparison below ' +\n+      'is against an unvouched-for number',\n+    );\n+  }\n+  if (benchTolerancesDigest(previousV12.tolerances) !== manifest.previousTolerancesDigest) {\n+    reasons.push(\n+      'v12→v13 migration source does not carry the reviewed v12 tolerances — tolerances are ' +\n+      'exempt from the payload digest so ordinary reseeds can edit them, but the shared gates ' +\n+      'grade the live report against whatever tolerances the caller supplies, so an unpinned ' +\n+      'tolerance here would grade leniently against a value nobody reviewed',\n+    );\n+  }\n+  if (report.corpusDigest !== manifest.previousCorpusDigest) {\n+    reasons.push(\n+      'v13 corpus digest moved — this migration is additive-only and permits no corpus edit',\n+    );\n+  }\n+  // Seed the candidate from the live report and demand exact equality field by\n+  // field. Anything that moved is a behaviour change wearing a schema bump.\n+  const candidate = seedCorrelationBenchBaseline(\n+    report,\n+    { note: previousV12.note, tolerances: previousV12.tolerances },\n+    previousV12.seededAt,\n+  ) as unknown as Record<string, unknown>;\n+  const before = previousV12 as unknown as Record<string, unknown>;\n+  // Union, not the candidate's keys: iterating one side makes a field DROPPED\n+  // by the bump invisible, and a dropped measurement is the same defect as a\n+  // moved one.\n+  const keys = [...new Set([...Object.keys(candidate), ...Object.keys(before)])].sort(compareStrings);\n+  for (const key of keys) {\n+    if (V13_MAY_MOVE.has(key)) continue;\n+    if (JSON.stringify(candidate[key]) !== JSON.stringify(before[key])) {\n+      reasons.push(\n+        `v12→v13 is additive-only, but ${key} moved from `\n+        + `${JSON.stringify(before[key])} to ${JSON.stringify(candidate[key])}`,\n+      );\n+    }\n+  }\n+  if (candidate.reportDigest === before.reportDigest) {\n+    reasons.push('v12→v13 report digest did not move — nothing was actually added');\n+  }\n+  checkReportConsistency(reasons, report);\n+  return { ok: reasons.length === 0, reasons };\n+}\n+\n export interface CorrelationBenchMigrationV11ToV12 {\n   fromSchemaVersion: 11;\n   toSchemaVersion: 12;\n@@ -1031,12 +1247,96 @@ function checkIdSet(\n  * — and the gate's verdict — unchanged. The probes are the only evidence the\n  * gate has about those five, so a missing probe fails as loudly as a failing one.\n  */\n+/**\n+ * A retired learned rule has to actually leave the engine.\n+ *\n+ * `runEngine()` builds a fresh engine per pass, so every learned-rule number in\n+ * this report describes an INSTALL. Removing `engine.unregisterRule()` from\n+ * `syncLearnedRules()` therefore changed nothing here, while a coupling the\n+ * miner had retired stayed registered and went on matching live events — and\n+ * the function still reported it removed. This checks the engine's own\n+ * inventory afterwards, not that report.\n+ */\n+function checkLearnedRuleResync(reasons: string[], report: CorrelationBenchReport): void {\n+  const p = report.learnedRuleResync;\n+  if (!p || typeof p !== 'object') {\n+    reasons.push('the report carries no learned-rule resync probe — the removal path is unmeasured');\n+    return;\n+  }\n+  if (!Array.isArray(p.installed) || !Array.isArray(p.afterRetirement)\n+    || typeof p.retiredId !== 'string') {\n+    reasons.push('the learned-rule resync probe is malformed');\n+    return;\n+  }\n+  // An empty install makes every assertion below vacuously true, which is the\n+  // shape a probe that silently stopped syncing anything would take.\n+  if (p.installed.length < 2) {\n+    reasons.push(\n+      `the learned-rule resync probe installed ${p.installed.length} rule(s) — with fewer than ` +\n+      `two there is no retirement to observe`,\n+    );\n+    return;\n+  }\n+  if (!p.installed.includes(p.retiredId)) {\n+    reasons.push(`the resync probe retired ${p.retiredId}, which it never installed`);\n+  }\n+  if (p.afterRetirement.includes(p.retiredId)) {\n+    reasons.push(\n+      `${p.retiredId} is still registered after being retired — syncLearnedRules reported it ` +\n+      `removed, and a coupling the miner no longer believes in keeps matching live events`,\n+    );\n+  }\n+  if (p.afterRetirement.length !== p.installed.length - 1) {\n+    reasons.push(\n+      `retiring one learned rule left ${p.afterRetirement.length} installed, not ` +\n+      `${p.installed.length - 1} — the sync removed the wrong number of rules`,\n+    );\n+  }\n+  if (p.reportedRemoved !== 1 || p.reportedAdded !== 0) {\n+    reasons.push(\n+      `the resync reported {added ${p.reportedAdded}, removed ${p.reportedRemoved}} for a ` +\n+      `single retirement — callers act on those counters`,\n+    );\n+  }\n+  if (p.builtInsIntact !== true) {\n+    reasons.push(\n+      'syncing learned rules changed the BUILT-IN inventory — the learned:* prefix is the only ' +\n+      'thing keeping a mined coupling from evicting a shipped rule',\n+    );\n+  }\n+  // Everything above reads the report. This runs the retirement again against a\n+  // live engine and demands the two agree: a producer that derives\n+  // `afterRetirement` arithmetically instead of calling `getRules()` moves no\n+  // digest and satisfies every check above it.\n+  const live = verifyResyncProbe();\n+  const reportedSide: BenchResyncProbe = {\n+    installed: p.installed,\n+    retiredId: p.retiredId,\n+    afterRetirement: p.afterRetirement,\n+    reportedAdded: p.reportedAdded,\n+    reportedRemoved: p.reportedRemoved,\n+    builtInsIntact: p.builtInsIntact,\n+  };\n+  if (JSON.stringify(live) !== JSON.stringify(reportedSide)) {\n+    reasons.push(\n+      `the learned-rule resync probe does not reproduce: the report says ` +\n+      `${JSON.stringify(p)} and re-running the retirement against a live engine ` +\n+      `gives ${JSON.stringify(live)}`,\n+    );\n+  }\n+}\n+\n function checkRuleProbes(reasons: string[], report: CorrelationBenchReport): void {\n   const probes = report.ruleProbes;\n   if (!Array.isArray(probes) || !Array.isArray(report.builtInRuleIds)) {\n     reasons.push('live report carries no per-rule coverage probes');\n     return;\n   }\n+  // The gate's own execution of the same fixtures. Everything below reads the\n+  // report; this reads the engine. Replacing the producer's near-miss\n+  // executions with constants left every digest identical — the report can\n+  // only be evidence about a matcher if something independent still runs it.\n+  const live = new Map(verifyRuleProbes().map((p) => [p.ruleId, p]));\n   const probed = new Set<string>();\n   for (const p of probes) {\n     if (!p || typeof p !== 'object' || typeof p.ruleId !== 'string' || p.ruleId === '') {\n@@ -1066,6 +1366,8 @@ function checkRuleProbes(reasons: string[], report: CorrelationBenchReport): voi\n     }\n     checkProbeSemantics(reasons, p);\n     checkProbeNearMisses(reasons, p);\n+    checkProbeDisjuncts(reasons, p);\n+    checkProbeAgainstLiveExecution(reasons, p, live.get(p.ruleId));\n     // The verdicts above are the ANSWER; this is the question. Without it\n     // the report holds still while a fixture is quietly re-aimed at a clause\n     // the rule still has, and both booleans stay true about nothing.\n@@ -1094,6 +1396,94 @@ function checkRuleProbes(reasons: string[], report: CorrelationBenchReport): voi\n  * reduced to a boolean, rewriting `airquality-wildfire` from `causal-candidate`\n  * to `contradicts` — the opposite assertion — moved nothing.\n  */\n+/**\n+ * The report's probe verdicts, re-derived by the gate from the same fixtures.\n+ *\n+ * This is the only check here that does not take the report's word for\n+ * anything. It exists because the reviewer deleted all 73 near-miss engine\n+ * executions — `rejected: true`, hardcoded — and every digest in the report\n+ * stayed byte-identical, because the honest answer to those questions was also\n+ * `true`. Truthful output is not evidence that a question was asked.\n+ */\n+function checkProbeAgainstLiveExecution(\n+  reasons: string[],\n+  p: BenchRuleProbe,\n+  live: BenchRuleProbe | undefined,\n+): void {\n+  if (live === undefined) {\n+    reasons.push(\n+      `rule coverage probe for '${p.ruleId}' has no fixture in the gate's own fixture set — ` +\n+      `the report is reporting on a rule this gate cannot independently exercise`,\n+    );\n+    return;\n+  }\n+  const scalar: [string, unknown, unknown][] = [\n+    ['positiveMatched', p.positiveMatched, live.positiveMatched],\n+    ['positiveEdgeType', p.positiveEdgeType, live.positiveEdgeType],\n+    ['positiveDirection', p.positiveDirection, live.positiveDirection],\n+    ['reversedMatched', p.reversedMatched, live.reversedMatched],\n+    ['reversedDirection', p.reversedDirection, live.reversedDirection],\n+    ['fixtureDigest', p.fixtureDigest, live.fixtureDigest],\n+  ];\n+  for (const [field, got, want] of scalar) {\n+    if (got !== want) {\n+      reasons.push(\n+        `rule '${p.ruleId}' reports ${field}=${JSON.stringify(got)}, but running its fixtures ` +\n+        `here produced ${JSON.stringify(want)} — the benchmark's probe verdicts are not what ` +\n+        `the shipped rule set actually does`,\n+      );\n+    }\n+  }\n+  const asked = (probe: BenchRuleProbe): string => JSON.stringify({\n+    misses: probe.nearMisses.map((m) => [m.clause, m.rejected]),\n+    branches: probe.disjuncts.map((d) => [d.branch, d.matched]),\n+  });\n+  if (asked(p) !== asked(live)) {\n+    reasons.push(\n+      `rule '${p.ruleId}' reports clause verdicts the gate's own run does not reproduce — ` +\n+      `reported ${asked(p)}, executed ${asked(live)}`,\n+    );\n+  }\n+}\n+\n+/**\n+ * Every branch of every disjunctive clause still gets accepted on its own.\n+ *\n+ * A near-miss must defeat BOTH sides of a disjunction to reject at all, so the\n+ * near-misses say nothing about the branches individually. Narrowing\n+ * `weather-wildfire` to accept only a red-flag warning — dropping\n+ * fire-weather-watch — kept the positive matching and every near-miss\n+ * rejecting.\n+ */\n+function checkProbeDisjuncts(reasons: string[], p: BenchRuleProbe): void {\n+  const branches = p.disjuncts;\n+  if (!Array.isArray(branches)) {\n+    reasons.push(\n+      `rule '${p.ruleId}' carries no disjunct roster — rules with an OR in the matcher can ` +\n+      `then lose a branch in silence`,\n+    );\n+    return;\n+  }\n+  const names = branches.map((d) => (d as { branch?: unknown }).branch);\n+  if (names.some((b) => typeof b !== 'string' || b === '')) {\n+    reasons.push(`rule '${p.ruleId}' has a disjunct fixture that names no branch`);\n+  }\n+  if (new Set(names).size !== names.length) {\n+    reasons.push(\n+      `rule '${p.ruleId}' repeats a disjunct branch — duplicates inflate branch coverage ` +\n+      `without covering anything`,\n+    );\n+  }\n+  const lost = branches.filter((d) => d.matched !== true);\n+  if (lost.length > 0) {\n+    reasons.push(\n+      `rule '${p.ruleId}' stopped accepting ${lost.length} advertised branch(es) ` +\n+      `(${lost.map((d) => String(d.branch)).join('; ')}) — the rule got narrower, and a rule ` +\n+      `that quietly covers half of what it documents is a silent miss`,\n+    );\n+  }\n+}\n+\n function checkProbeSemantics(reasons: string[], p: BenchRuleProbe): void {\n   if (!PAIR_EDGE_TYPES.has(p.positiveEdgeType as string)) {\n     reasons.push(\n@@ -1108,6 +1498,21 @@ function checkProbeSemantics(reasons: string[], p: BenchRuleProbe): void {\n       `positive fixture — cause and effect are not interchangeable`,\n     );\n   }\n+  // The engine tries `matchFn(a, b)` and then `matchFn(b, a)`. Every fixture\n+  // and every corpus stream is antecedent-first, so deleting the second attempt\n+  // moved nothing at all — half the matching contract was unobserved.\n+  if (p.reversedMatched !== true) {\n+    reasons.push(\n+      `rule '${p.ruleId}' did not match its positive fixture fed back-to-front — the engine ` +\n+      `promises rule authors both orientations, and this rule now depends on input order`,\n+    );\n+  }\n+  if (typeof p.reversedDirection !== 'string' || !p.reversedDirection.includes('→')) {\n+    reasons.push(\n+      `rule '${p.ruleId}' reports reversed direction ${JSON.stringify(p.reversedDirection)} — ` +\n+      `a reversed feed that emits nothing to describe is the defect above, unrecorded`,\n+    );\n+  }\n }\n \n /**\n@@ -1228,6 +1633,7 @@ function checkPreflight(\n   checkRuleInventory(reasons, report, baseline);\n   checkRuleCoverage(reasons, report, baseline);\n   checkRuleProbes(reasons, report);\n+  checkLearnedRuleResync(reasons, report);\n   // Before the operand loop in the caller: a null separation is either the\n   // perfect run's legitimate answer or a missing number, and `falseEdgeCount`\n   // is what decides which. Saying that plainly beats \"not a finite number\".\n@@ -2794,9 +3200,76 @@ function checkPairEmissions(\n     reasons.push(`${bad} has a confidence outside [0,1] (${confidences.join(', ')})`);\n     return false;\n   }\n+  // `detectedAt` is what downstream recency filters read, and it left no trace\n+  // in the report at all: `new Date(0)` for every pair moved no count, no rate\n+  // and no digest, while live pairs would have been discarded as ancient on\n+  // emission. The benchmark injects one fixed `now`, so a real emission is a\n+  // finite positive instant — zero and NaN are the shapes the mutation makes.\n+  if (emissions.some((e) => typeof e.detectedAtMs !== 'number'\n+    || !Number.isFinite(e.detectedAtMs) || e.detectedAtMs <= 0)) {\n+    reasons.push(\n+      `${bad} carries a detectedAt that is not a real instant — a pair emitted at the epoch is ` +\n+      `already too old to survive recency filtering`,\n+    );\n+    return false;\n+  }\n+  // Six factors multiply into one scalar, so DROPPING the breakdown preserves\n+  // `confidence` exactly and the analyst-facing explanation silently becomes\n+  // undefined. `null` is that regression, and it has to be a FAIL rather than a\n+  // tolerated shape: the engine populates `confidenceDetail` on every pair.\n+  const detailless = emissions.filter((e) => e.confidenceDetailDigest === null).length;\n+  if (detailless > 0) {\n+    reasons.push(\n+      `${bad} has ${detailless} emission(s) with no confidence breakdown — the scalar survives ` +\n+      `but the explanation and factor chips do not`,\n+    );\n+    return false;\n+  }\n+  if (emissions.some((e) => typeof e.confidenceDetailDigest !== 'string'\n+    || !/^[0-9a-f]{32}$/.test(e.confidenceDetailDigest))) {\n+    reasons.push(`${bad} has a malformed confidence-breakdown digest`);\n+    return false;\n+  }\n+  // The engine's `edgeType` is one translation short of what the analyst reads:\n+  // SituationStoreV2 maps it through EDGE_TYPE_MAP on the way into the evidence\n+  // graph. Inverting a row there — 'causal-candidate' to `contradicts` — flipped\n+  // the meaning of real situation edges without moving a number here, so the\n+  // ledger now carries the PROJECTION and the gate checks it against the raw\n+  // claim it came from. Direction has to survive the translation too: a\n+  // projection that silently swapped endpoints would reverse causality.\n+  const projected = emissions.filter((e) => {\n+    const want = PROJECTED_EDGE_TYPE[e.edgeType as string];\n+    return want === undefined || e.evidenceEdgeType !== want\n+      || e.evidenceFromId !== e.fromId || e.evidenceToId !== e.toId;\n+  });\n+  if (projected.length > 0) {\n+    const sample = projected[0]!;\n+    reasons.push(\n+      `${bad} projects ${sample.edgeType} (${sample.fromId}→${sample.toId}) into the evidence ` +\n+      `graph as ${sample.evidenceEdgeType} (${sample.evidenceFromId}→${sample.evidenceToId}) — ` +\n+      `the analyst is told something the engine did not conclude`,\n+    );\n+    // Deliberately not an early return: a stray endpoint shows up here AND in\n+    // the row-shape check below, and the endpoint reason is the more precise\n+    // one. Short-circuiting would hide it.\n+  }\n   return true;\n }\n \n+/**\n+ * The one legitimate reading of each engine edge type, restated here on purpose.\n+ *\n+ * Importing SituationStoreV2's map would make this check tautological: the map\n+ * would be compared against itself and any inversion would agree with itself.\n+ * This table is the SECOND opinion, and it is a table a reviewer can read.\n+ */\n+const PROJECTED_EDGE_TYPE: Record<string, string> = {\n+  'causal-candidate': 'caused_by',\n+  'co-located': 'co-located',\n+  'temporally-adjacent': 'temporally-adjacent',\n+  contradicts: 'contradicts',\n+};\n+\n /**\n  * The row's three CONCLUSIONS — its key, its planted-truth flag, and its decoy\n  * contact — re-derived from the two event ids behind it.\n</stdin>\ncodex\nI’m using the repository’s mandatory `maximum-qa-qc` workflow for this review. I’ll inspect the current files and supplied diff directly, audit all migration-guard prose and test arithmetic, then run the focused suite and full typecheck. I will not delegate.\nexec\n/bin/zsh -lc \"sed -n '1,240p' /Users/bradleybond/.codex/skills/maximum-qa-qc/SKILL.md\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n---\nname: maximum-qa-qc\ndescription: Default for app-building, software changes, releases, and implementation sign-off unless the user asks for a lighter mode. Also use when the user asks for max QA/QC, hardening mode, release gating, or the strongest safety, security, reliability, and efficiency checks before accepting or shipping software changes.\n---\n\n# Maximum QA/QC\n\nUse this skill by default for software delivery work unless the user explicitly wants to trade rigor for speed. It is mandatory whenever the user asks for the strictest practical delivery posture for a change.\n\nThis is not a single test command. It is a gated workflow that raises the bar across design, implementation, security, correctness, reliability, performance, and release readiness.\n\nRead [references/qaqc-checklist.md](references/qaqc-checklist.md) at the start, and use [references/report-template.md](references/report-template.md) when reporting the result.\n\n## Goals\n\nDrive the change toward software that is:\n\n- safe\n- secure\n- reliable\n- efficient\n- verifiably ready\n\n## Risk Tiers\n\nClassify the work before coding or sign-off:\n\n- `Low`: isolated UI copy, low-risk refactor, non-critical tooling\n- `Medium`: normal feature work with bounded impact\n- `High`: auth, payments, file handling, external input, migrations, background jobs, infra changes, high-traffic paths\n- `Critical`: internet-exposed security boundaries, secrets, production data integrity, destructive workflows, multi-tenant isolation, compliance-sensitive or safety-sensitive paths\n\nIf the work is `High` or `Critical`, do not treat any check as optional unless the user explicitly accepts the risk.\n\n## Workflow\n\n### 1. Scope and invariants\n\nDefine:\n\n- what is changing\n- what must not break\n- who or what could be harmed by failure\n- the main correctness and safety invariants\n\nIf scope is unclear, stop and clarify.\n\n### 2. Security gate\n\nIf the change touches exposed inputs, auth, secrets, permissions, persistence, uploads, external APIs, or infrastructure:\n\n- use `security-best-practices`\n- use `security-threat-model` when the attack surface or trust boundaries materially change\n\nCheck at minimum:\n\n- authn/authz\n- input validation and output encoding\n- secrets handling\n- dependency and config safety\n- least privilege\n- unsafe defaults\n\n### 3. Correctness gate\n\nUse `test-driven-development` for behavior changes whenever feasible.\n\nRequire:\n\n- regression coverage for the changed behavior\n- happy-path tests\n- negative-path tests\n- edge-case tests\n- type checks and lint where applicable\n\nIf the change fixes a bug, prove the bug with a failing test or a clear reproduction before claiming it is fixed.\n\n### 4. Reliability gate\n\nCheck failure behavior, not just success behavior.\n\nRequire review of:\n\n- timeouts\n- retries and backoff\n- idempotency where relevant\n- partial-failure handling\n- logging and observability\n- migration safety\n- startup and shutdown behavior for long-running processes\n\nUse `systematic-debugging` if any verification is flaky or unclear.\n\n### 5. Efficiency gate\n\nFor performance-sensitive or user-facing code, verify that the change is efficient enough for the target path.\n\nCheck as relevant:\n\n- build and bundle impact\n- query count or query shape\n- hot loops and allocations\n- network round trips\n- cache behavior\n- render cost and Core Web Vitals for frontend flows\n\nUse `debug-optimize-lcp` or `chrome-devtools` for frontend performance investigation when relevant.\n\n### 6. Review gate\n\nPerform an explicit review pass before sign-off.\n\nLook for:\n\n- behavioral regressions\n- unsafe assumptions\n- test gaps\n- dead code or accidental complexity\n- missing docs or runbook notes for operationally sensitive changes\n\nIf the user wants external review handling, combine with `gh-address-comments` or repo-specific review workflows.\n\n### 7. Release gate\n\nBefore calling the work done, require:\n\n- targeted tests pass\n- full required test suite passes\n- type checks pass\n- build passes if applicable\n- security findings are resolved or explicitly accepted\n- critical open risks are surfaced to the user\n\nFor `High` and `Critical` risk work, also verify rollback or safe recovery thinking exists.\n\n### 8. Final evidence gate\n\nUse `verification-before-completion`.\n\nDo not claim the work is safe, secure, reliable, or efficient without fresh evidence.\n\n## Stop Conditions\n\nDo not sign off if any of these are true:\n\n- unresolved `Critical` or `High` severity security issue\n- failing tests, type checks, or build\n- unverified migration or destructive path\n- unknown behavior under failure conditions\n- no evidence for a major claim\n\nIf the user still wants to proceed, state the exact residual risk clearly.\n\n## Output Contract\n\nWhen using this skill, end with a QA/QC report using [references/report-template.md](references/report-template.md).\n\nThe report must distinguish:\n\n- checks passed\n- checks not run\n- accepted risks\n- blockers\n\n## Default Stance\n\nBias toward slowing down slightly to avoid shipping avoidable defects, security holes, reliability regressions, or wasteful performance regressions.\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' '/Users/bradleybond/.codex/skills/maximum-qa-qc/references/qaqc-checklist.md'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n# Maximum QA/QC Checklist\n\nUse this checklist while executing the `maximum-qa-qc` skill.\n\n## 1. Scope\n\n- Define the exact change surface\n- Define success criteria\n- Define failure impact\n- Define invariants that must hold\n\n## 2. Security\n\n- Inputs validated\n- Auth and authorization boundaries checked\n- Secrets not introduced or leaked\n- Dangerous permissions avoided\n- Secure defaults preserved\n- New dependencies scrutinized\n\n## 3. Correctness\n\n- Regression reproduced or clearly described\n- Tests cover happy path\n- Tests cover negative path\n- Tests cover edge cases\n- Type checks run\n- Lint or static analysis run where relevant\n\n## 4. Reliability\n\n- Failure modes reviewed\n- Timeouts or cancellation reviewed\n- Retry or backoff logic reviewed\n- Idempotency considered where relevant\n- Logging and observability adequate\n- Migration or state transition safety reviewed\n\n## 5. Efficiency\n\n- Build or runtime cost reviewed\n- Hot path efficiency reviewed\n- Query/network overhead reviewed\n- Frontend rendering or loading impact reviewed\n- Benchmark or profiling evidence captured when needed\n\n## 6. Release Readiness\n\n- Full required checks passed\n- Manual verification done where automation is insufficient\n- Rollback or recovery path considered\n- Residual risks listed explicitly\n\n## 7. Sign-off\n\n- No unsupported success claims\n- Evidence is fresh\n- User sees blockers and accepted risks separately\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' '/Users/bradleybond/.codex/skills/maximum-qa-qc/references/report-template.md'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n# QA/QC Report Template\n\nUse this structure at the end of a `maximum-qa-qc` run.\n\n```markdown\n## QA/QC Report\n\nRisk tier: <Low|Medium|High|Critical>\n\nScope:\n- <what changed>\n\nChecks passed:\n- <test / typecheck / build / security review / performance check>\n\nChecks not run:\n- <check> — <why not run>\n\nFindings:\n- <severity> — <issue or \"none\">\n\nResidual risks:\n- <risk or \"none\">\n\nRelease stance:\n- <ready / ready with stated risk / not ready>\n```\n\nRules:\n\n- Keep passed vs not-run separate.\n- If something important was not run, say it plainly.\n- If risk was accepted by the user, name that explicitly.\n- Do not label the work \"safe\" or \"secure\" without evidence that matches the claim.\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' '/Users/bradleybond/.codex/skills/verification-before-completion/SKILL.md'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n---\nname: verification-before-completion\ndescription: Use when about to claim work is complete, fixed, or passing, before committing or creating PRs - requires running verification commands and confirming output before making any success claims; evidence before assertions always\n---\n\n# Verification Before Completion\n\n## Overview\n\nClaiming work is complete without verification is dishonesty, not efficiency.\n\n**Core principle:** Evidence before claims, always.\n\n**Violating the letter of this rule is violating the spirit of this rule.**\n\n## The Iron Law\n\n```\nNO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE\n```\n\nIf you haven't run the verification command in this message, you cannot claim it passes.\n\n## The Gate Function\n\n```\nBEFORE claiming any status or expressing satisfaction:\n\n1. IDENTIFY: What command proves this claim?\n2. RUN: Execute the FULL command (fresh, complete)\n3. READ: Full output, check exit code, count failures\n4. VERIFY: Does output confirm the claim?\n   - If NO: State actual status with evidence\n   - If YES: State claim WITH evidence\n5. ONLY THEN: Make the claim\n\nSkip any step = lying, not verifying\n```\n\n## Common Failures\n\n| Claim | Requires | Not Sufficient |\n|-------|----------|----------------|\n| Tests pass | Test command output: 0 failures | Previous run, \"should pass\" |\n| Linter clean | Linter output: 0 errors | Partial check, extrapolation |\n| Build succeeds | Build command: exit 0 | Linter passing, logs look good |\n| Bug fixed | Test original symptom: passes | Code changed, assumed fixed |\n| Regression test works | Red-green cycle verified | Test passes once |\n| Agent completed | VCS diff shows changes | Agent reports \"success\" |\n| Requirements met | Line-by-line checklist | Tests passing |\n\n## Red Flags - STOP\n\n- Using \"should\", \"probably\", \"seems to\"\n- Expressing satisfaction before verification (\"Great!\", \"Perfect!\", \"Done!\", etc.)\n- About to commit/push/PR without verification\n- Trusting agent success reports\n- Relying on partial verification\n- Thinking \"just this once\"\n- Tired and wanting work over\n- **ANY wording implying success without having run verification**\n\n## Rationalization Prevention\n\n| Excuse | Reality |\n|--------|---------|\n| \"Should work now\" | RUN the verification |\n| \"I'm confident\" | Confidence ≠ evidence |\n| \"Just this once\" | No exceptions |\n| \"Linter passed\" | Linter ≠ compiler |\n| \"Agent said success\" | Verify independently |\n| \"I'm tired\" | Exhaustion ≠ excuse |\n| \"Partial check is enough\" | Partial proves nothing |\n| \"Different words so rule doesn't apply\" | Spirit over letter |\n\n## Key Patterns\n\n**Tests:**\n```\n✅ [Run test command] [See: 34/34 pass] \"All tests pass\"\n❌ \"Should pass now\" / \"Looks correct\"\n```\n\n**Regression tests (TDD Red-Green):**\n```\n✅ Write → Run (pass) → Revert fix → Run (MUST FAIL) → Restore → Run (pass)\n❌ \"I've written a regression test\" (without red-green verification)\n```\n\n**Build:**\n```\n✅ [Run build] [See: exit 0] \"Build passes\"\n❌ \"Linter passed\" (linter doesn't check compilation)\n```\n\n**Requirements:**\n```\n✅ Re-read plan → Create checklist → Verify each → Report gaps or completion\n❌ \"Tests pass, phase complete\"\n```\n\n**Agent delegation:**\n```\n✅ Agent reports success → Check VCS diff → Verify changes → Report actual state\n❌ Trust agent report\n```\n\n## Why This Matters\n\nFrom repeated failure patterns:\n- the user stops trusting status claims that are not backed by evidence\n- Undefined functions shipped - would crash\n- Missing requirements shipped - incomplete features\n- Time wasted on false completion → redirect → rework\n- Violates: \"Honesty is a core value. If you lie, you'll be replaced.\"\n\n## When To Apply\n\n**ALWAYS before:**\n- ANY variation of success/completion claims\n- ANY expression of satisfaction\n- ANY positive statement about work state\n- Committing, PR creation, task completion\n- Moving to next task\n- Delegating to agents\n\n**Rule applies to:**\n- Exact phrases\n- Paraphrases and synonyms\n- Implications of success\n- ANY communication suggesting completion/correctness\n\n## The Bottom Line\n\n**No shortcuts for verification.**\n\nRun the command. Read the output. THEN claim the result.\n\nThis is non-negotiable.\n\ncodex\nScope is high-risk migration-gate sign-off. The key invariants are: the one-hop validator remains publicly reachable for a `toSchemaVersion`-only mismatch, the prose accurately describes that reachability, and every documented test total is arithmetically consistent with its pass/fail counts. The verification skill requires fresh command output before any approval claim, so I’ll preserve the test and typecheck outputs exactly.\nexec\n/bin/zsh -lc 'git status --short --branch' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 114ms:\n## claude/acc-501-round-14...origin/claude/acc-501-round-14\n\nexec\n/bin/zsh -lc 'git log -8 --oneline --decorate' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 354ms:\n2c6e8834 (HEAD -> claude/acc-501-round-14, origin/claude/acc-501-round-14) ACC-501: fix round-19 findings — stale docstring + doc arithmetic error\n235331b2 ACC-501: close round 18b gap — validator-level test for one-hop schema check\n6e0b5448 ACC-501: extract oneHopSchemaVersionCheckReason for a real mutation-proof test\n062b7c8a ACC-501 round 17: fix Codex REQUEST_CHANGES findings on PR #1625 (2nd pass)\ndd4e880a ACC-501 round 16: fix Codex REQUEST_CHANGES findings on PR #1625\n01d26e75 ACC-501 round 15: independent resync re-verification + pinned migration digest\n870b08f6 ACC-501 round 15: make the correlation gate verify what the producer claims\nd00943e0 (origin/main, origin/HEAD, bradleybond512/main, bradleybond512/HEAD) agentic: record claude review verdict for 901578d7\n\nexec\n/bin/zsh -lc 'git diff --check' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'git diff --stat' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'git diff -- src/services/correlation/bench-correlation-baseline.ts docs/validation/ACC-501-MUTATION-PROOFS.md' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'git show --stat --oneline --decorate HEAD' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 131ms:\n2c6e8834 (HEAD -> claude/acc-501-round-14, origin/claude/acc-501-round-14) ACC-501: fix round-19 findings — stale docstring + doc arithmetic error\n docs/validation/ACC-501-MUTATION-PROOFS.md             |  4 ++--\n src/services/correlation/bench-correlation-baseline.ts | 11 ++++++-----\n 2 files changed, 8 insertions(+), 7 deletions(-)\n\nexec\n/bin/zsh -lc 'git show --format=fuller --no-ext-diff HEAD -- src/services/correlation/bench-correlation-baseline.ts docs/validation/ACC-501-MUTATION-PROOFS.md' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\ncommit 2c6e8834b6a203f71983a9d6dcc94cd5767c72ce\nAuthor:     Brad Bond <bradley_bond@me.com>\nAuthorDate: Tue Aug 4 22:53:43 2026 -0500\nCommit:     Brad Bond <bradley_bond@me.com>\nCommitDate: Tue Aug 4 22:53:43 2026 -0500\n\n    ACC-501: fix round-19 findings — stale docstring + doc arithmetic error\n    \n    Codex round 19 REQUEST_CHANGES, both confirmed correct on inspection:\n    \n    - oneHopSchemaVersionCheckReason's docstring still claimed the check was\n      unreachable through the public validator via any caller-supplied\n      manifest — true before round 18b's guard restructuring, false now that\n      toSchemaVersion is deliberately excluded from the blanket identity\n      comparison specifically so this check is reachable. Updated to describe\n      the current, correct reachability.\n    - ACC-501-MUTATION-PROOFS.md's round 18b mutation-proof result was\n      transcribed as \"223 pass / 1 fail\", which is arithmetically impossible\n      for a 223-test suite with one failure. Corrected to \"223 tests / 222\n      pass / 1 fail\", matching the actual recorded test-runner output.\n    \n    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\n\ndiff --git a/docs/validation/ACC-501-MUTATION-PROOFS.md b/docs/validation/ACC-501-MUTATION-PROOFS.md\nindex f598c9f4..772af184 100644\n--- a/docs/validation/ACC-501-MUTATION-PROOFS.md\n+++ b/docs/validation/ACC-501-MUTATION-PROOFS.md\n@@ -652,8 +652,8 @@ Checksum before mutation: `4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d\n (the four lines pushing `oneHopReason` into `reasons`), leaving everything\n else, including `manifestToSchemaVersion`'s extraction, intact.\n \n-**Result**: `223 pass / 1 fail`. The new validator-level test failed exactly\n-as expected:\n+**Result**: `223 tests / 222 pass / 1 fail`. The new validator-level test\n+failed exactly as expected:\n \n ```\n ✖ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\ndiff --git a/src/services/correlation/bench-correlation-baseline.ts b/src/services/correlation/bench-correlation-baseline.ts\nindex 86fe060d..01ce4ac1 100644\n--- a/src/services/correlation/bench-correlation-baseline.ts\n+++ b/src/services/correlation/bench-correlation-baseline.ts\n@@ -347,11 +347,12 @@ export function benchTolerancesDigest(tolerances: CorrelationBenchBaseline['tole\n }\n \n /**\n- * Pure one-hop schema check, extracted so it is directly testable without\n- * routing through the manifest-identity guard (which makes the branch inside\n- * validateCorrelationBenchV12ToV13Migration unreachable via any caller-supplied\n- * manifest — the only way to exercise it there is to mutate the pinned constant\n- * itself). Returns the refusal reason string, or null when the manifest still\n+ * Pure one-hop schema check, extracted so it is directly testable in\n+ * isolation from the rest of validateCorrelationBenchV12ToV13Migration.\n+ * The validator's manifest-identity guard excludes toSchemaVersion from its\n+ * blanket comparison specifically so a caller-supplied manifest differing\n+ * only in this field reaches this check instead of being swallowed there.\n+ * Returns the refusal reason string, or null when the manifest still\n  * targets the compiled schema.\n  */\n export function oneHopSchemaVersionCheckReason(\n\nexec\n/bin/zsh -lc 'wc -l docs/validation/ACC-501-MUTATION-PROOFS.md src/services/correlation/bench-correlation-baseline.ts' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n     686 docs/validation/ACC-501-MUTATION-PROOFS.md\n    3599 src/services/correlation/bench-correlation-baseline.ts\n    4285 total\n\nexec\n/bin/zsh -lc 'rg -n -C 3 \"pass|fail|tests|test file|suite|Result|result\" docs/validation/ACC-501-MUTATION-PROOFS.md' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n6-tree on `claude/acc-501-round-14` — round 15 addressed a Codex REQUEST_CHANGES\n7-review of PR #1625.\n8-\n9:The focused suite was green before mutation at `182 pass / 0 fail` for the\n10:round-14 mutations below, and grew to `216 pass / 0 fail` (full\n11:`bench-correlation.test.mts`) plus `409 pass / 0 fail` (`npm run\n12-test:correlation`) by round 15. All mutations began from a `git status\n13---short` containing only this session's legitimate round-15 diff (no\n14-unrelated changes); each file's SHA-256 was recorded before and after every\n--\n24-  the original compare-the-candidate-to-itself laundering path.\n25-- Confirmed diff: `comparisonBaseline` changed from `{ ...previous, ...anchors }`\n26-  to `seedCorrelationBenchBaseline(report, ...)`.\n27:- Targeted result: `0 pass / 1 fail`.\n28-- Failing assertion: `true !== false` in “refuses a candidate that regresses\n29-  beyond the previous one-sided tolerance”.\n30-- Restored checksum: `09dc7554529763a1cbe911b98aff597fc3d394d47ae5d9ea6ce6d83b792fd38e`.\n--\n34-- File: `scripts/correlation-benchmark.mts`\n35-- Before checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`\n36-- Mutation: changed `if (!comparison.ok)` to\n37:  `if (false && !comparison.ok)`, bypassing the transition verdict.\n38-- Confirmed diff: the CLI refusal branch was disabled while the comparator call\n39-  remained present.\n40:- Targeted result: `0 pass / 1 fail`.\n41-- Failing assertion: the child process exited `0` instead of `1` and emitted a\n42-  seeded candidate for a known regression.\n43-- Restored checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`.\n--\n50-  merge-queue guard, causing the command to compare against the candidate\n51-  branch's tracked baseline instead of the base commit.\n52-- Confirmed diff: the extracted base baseline remained unused by the command.\n53:- Targeted result: `0 pass / 1 fail`.\n54-- Failing assertion: the workflow no longer matched the required\n55-  `--seed --previous-baseline \"$PREVIOUS_BASELINE\"` invocation.\n56-- Restored checksum: `e9f7ce86e207bc58c1bb83bddb0f1fbf53b36da123e7750e65d2cd6b2173aedc`.\n--\n62-- Mutation: restored the unsafe instruction\n63-  `npm run bench:correlation -- --seed > ${rel}`, which lets shell redirection\n64-  truncate the tracked baseline before the process reads it.\n65:- Confirmed diff: the failure guidance changed from `CANDIDATE_PATH` back to\n66-  the tracked relative baseline path.\n67:- Targeted result: `0 pass / 1 fail`.\n68-- Failing assertion: “operator guidance must never redirect stdout onto the\n69-  tracked baseline before validation”.\n70-- Restored checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`.\n--\n78-  disappear while aggregate metrics stayed healthy.\n79-- Confirmed diff: the preflight array no longer invoked the coverage subset\n80-  check.\n81:- Targeted result: `0 pass / 1 fail`.\n82-- Failing assertion: `true !== false` in “refuses to reseed when a previously\n83-  covered built-in rule goes dark”.\n84-- Restored checksum: `cb714f0f7b91f706b378221a02c486410c054b4c94fc2c16b3ba0d20ba2b4ffe`.\n--\n92-  exist on the base branch.\n93-- Confirmed diff: the workflow proceeded directly from `BASELINE_PATH` to the\n94-  baseline-change check without testing whether the base object exists.\n95:- Targeted result: `0 pass / 1 fail`.\n96-- Failing assertion: “the first baseline addition has no base-branch baseline\n97-  to compare against”.\n98-- Restored checksum: `a2f7d64dbbe6cdca5608fdbac4f23bb94c70f17c3a3989d7cdf4432b62f98efc`.\n99-\n100:## Unavailable base commit fails closed\n101-\n102-- File: `.github/workflows/smoke.yml`\n103-- Before checksum: `852aa154f9cd1eb8027abdc2d70f0ce4b9af0cef95bbf21fa209145e138353c5`\n--\n105-  commit, recreating the path where an invalid or unavailable SHA is mistaken\n106-  for a legitimate initial baseline.\n107-- Confirmed diff: the workflow again proceeded directly to the path lookup,\n108:  whose failure exits successfully.\n109:- Targeted result: `0 pass / 1 fail`.\n110:- Failing assertion: “an unavailable base commit must fail closed before\n111-  checking for an initial baseline”.\n112-- Restored checksum: `852aa154f9cd1eb8027abdc2d70f0ce4b9af0cef95bbf21fa209145e138353c5`.\n113-\n--\n130-- Mutation 1 alone: changed `probeLearnedRuleResync`'s `afterRetirement` from\n131-  `learnedIdsOf()` (a live `engine.getRules()` query) to\n132-  `installed.filter((id) => id !== retiredId)` (self-attested arithmetic).\n133:  Result: `npm run bench:correlation` still **PASSED** — the arithmetic\n134-  answer happens to equal the true engine state when the underlying removal\n135-  actually works, so this alone doesn't prove the removal path is exercised.\n136-  This matches the framing of the defect precisely: self-attestation isn't\n--\n143-  `engine.unregisterRule(existing.id)` call from `syncLearnedRules` in\n144-  `learned-rules.ts`, so retirement stops actually removing the rule from the\n145-  engine while `removed += 1` still increments.\n146:- Targeted result: `npm run bench:correlation` → **FAIL**.\n147-- Failing assertion: `the learned-rule resync probe does not reproduce: the\n148-  report says {...\"afterRetirement\":[\"learned:cyber->finance\", ...4 ids]...}\n149-  and re-running the retirement against a live engine gives\n--\n159-its own \"previous v12 baseline\" comparison object from data available at\n160-validation time, with nothing pinning that reconstruction to the actual\n161-reviewed v12 payload — a forged previous baseline that moved a value in\n162:lockstep on both sides of the comparison would pass. The fix added\n163-`previousPayloadDigest`, computed once from the real `origin/main` v12\n164-baseline and pinned in `CORRELATION_BENCH_V12_TO_V13_MIGRATION`, checked via\n165-`benchBaselinePayloadDigest()` against whatever baseline is actually being\n--\n172-  disabling it while leaving `previousPayloadDigest` itself and every other\n173-  check in place.\n174-- Confirmed diff: the digest comparison branch became dead code.\n175:- Targeted result: `node --import tsx --test\n176:  src/services/correlation/__tests__/bench-correlation.test.mts` →\n177:  `214 pass / 1 fail`.\n178-- Failing assertion: exactly `refuses a previous baseline whose payload is\n179:  not the reviewed one` failed; no other test in the 215-test file was\n180-  affected, confirming the disabled check — not some other path — was what\n181-  that test exercises.\n182-- Restored checksum:\n--\n186-\n187-Codex's third finding was a P2 on\n188-`refuses to re-seed over a broken tolerance without --force`: it ran\n189:`--seed` against the committed baseline (which passes today) and accepted\n190:either `Seeded from the current run` or `REFUSED` as a pass — a CLI that\n191-always printed one of those two strings regardless of what the comparator\n192-did would satisfy the assertion. It never constructed a baseline the live\n193-run actually violates, so it proved the CLI can print those words, not that\n194-`--seed` compares anything.\n195-\n196:The fix replaces it with two tests built on the CLI's own\n197-`--previous-baseline <path>` override:\n198-\n199-- **Refusal case**: writes a temp copy of the committed baseline with\n--\n205-- **Positive control**: writes an unmodified, internally consistent copy of\n206-  the committed baseline as `--previous-baseline` and asserts exit code `0`,\n207-  `Seeded from the current run` on stderr, no `REFUSED`, and a parseable\n208:  seeded object on stdout — proving the comparator ran and passed on its own\n209-  merits rather than the refusal path being unreachable.\n210-\n211:Two earlier attempts at the positive control failed for reasons worth\n212-recording: raising `couplingPrecision` alone past its drop tolerance without\n213-loosening the tolerance tripped the refusal (expected); loosening the\n214-tolerance to `1` tripped a *different* guard — `bench-correlation-baseline.ts`\n--\n221-version validates against an object with no manufactured internal\n222-inconsistency at all.\n223-\n224:- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n225-- No source mutation was needed here — the previous test was a coverage gap\n226-  in the *test*, not a defect in `bench-correlation.ts` or\n227-  `bench-correlation-baseline.ts`, so this section documents construction and\n228-  verification, not a mutation/restore round-trip. Verified by running both\n229:  new tests: refusal case exits `1` with the expected reason text; positive\n230:  control exits `0` with a parseable candidate. Full suite: `216 pass / 0\n231:  fail`.\n232-\n233-## Round 16 — Codex REQUEST_CHANGES on round 15 (four findings)\n234-\n--\n264-  new check with no prior code path to mutate; the negative test above\n265-  demonstrates it fires, and `accepts the real additive transition with its\n266-  real, unforged tolerances` demonstrates the pinned digest matches the real\n267:  reviewed value so genuine migrations still pass.\n268-\n269-### P2 — refusal test never reached `checkDrop`\n270-\n--\n279-(`report.meanTruePairConfidence` is provably below `1`). Asserts the actual\n280-`checkDrop` diagnostic format (`/exceeds .* tolerance/`), not just `REFUSED`.\n281-\n282:- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n283-- No source mutation needed — this was a test-construction defect, the same\n284-  category as the original round-15 finding it's fixing. Verified by running\n285-  the corrected test: exits `1`, stderr matches `mean true-pair confidence`\n--\n288-### P2 — positive control too permissive\n289-\n290-The round-15 positive-control test only checked `typeof seeded.schemaVersion\n291:=== 'number'`, which would pass even if the CLI dropped or corrupted every\n292-other field.\n293-\n294-Fix: the positive control now compares the seeded output against the\n--\n299-benchmark is deterministic, so a fresh seed against an unmodified previous\n300-baseline reproduces every graded field exactly.\n301-\n302:- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n303:- Verified: the strengthened assertion passes against the real CLI output\n304-  (`refuses to re-seed against a --previous-baseline whose tolerance the live\n305-  run actually violates` / `seeds cleanly against a --previous-baseline the\n306:  live run genuinely satisfies` both still pass; the latter now exercises the\n307-  field-by-field comparison, not just a `typeof` check).\n308-\n309-### P2 — one-hop migration not enforced at runtime\n--\n311-`manifest.toSchemaVersion === CORRELATION_BENCH_SCHEMA_VERSION` was only\n312-asserted by a unit test (`pins the migration to exactly one hop`), not by the\n313-validator function itself — a future schema bump that forgot to retire or rev\n314:this migration would pass every check in\n315-`validateCorrelationBenchV12ToV13Migration` and silently become a two-hop\n316-migration wearing a one-hop label.\n317-\n--\n324-- Note on testability: the function's manifest-identity guard\n325-  (`JSON.stringify(manifest) !== JSON.stringify(CORRELATION_BENCH_V12_TO_V13_MIGRATION)`)\n326-  refuses any manifest that isn't byte-identical to the pinned module\n327:  constant, which means this new check's only reachable failure mode in\n328-  production is the pinned constant itself drifting from\n329-  `CORRELATION_BENCH_SCHEMA_VERSION` — not independently mutable via a test\n330-  argument without also tripping the identity guard first (both reasons\n331:  would fire together; the identity guard firing is not a bypass, it's proof\n332-  the two constants can't drift independently). Verified via `the runtime\n333-  one-hop check is real code, not just a test-level assertion` — confirms\n334-  the real manifest and the real schema-version constant agree, and that\n--\n357-  ...Object.keys(before)])]` → `const keys = [...new\n358-  Set([...Object.keys(candidate)])]` (dropped the `before` half of the union).\n359-- Confirmed diff: `git diff` showed exactly the one-line key-union narrowing.\n360:- Targeted result: `node --import tsx --test\n361:  src/services/correlation/__tests__/bench-correlation.test.mts` →\n362:  `219 pass / 1 fail`.\n363-- Failing assertion: exactly `sees a field the reviewed baseline HAD that the\n364:  live candidate no longer produces at all` failed — actual reason was\n365-  `does not carry the reviewed v12 payload` (from the earlier payload-digest\n366-  guard, which also fires for this fixture) instead of the expected\n367-  `legacyRetiredField moved from 42 to undefined` (which only the\n368-  union-iterating field loop produces) — confirming the union half of the key\n369-  set is what makes that specific reason reachable. No other test in the\n370:  220-test file was affected.\n371-- Restored checksum: `04696770b63a5ba784692bb0ddbac66743f26b862d9ac9926177191974b3dbe6`\n372-  (matches before checksum exactly).\n373-\n374:Full suite after all four round-16 fixes: `220 pass / 0 fail`\n375:(`bench-correlation.test.mts`), `413 pass / 0 fail` (`npm run\n376-test:correlation`), `npm run bench:correlation` → PASS, `npx tsc --noEmit`\n377-(both tsconfig.json and tsconfig.api.json) → 0 errors.\n378-\n--\n389-`reportDigest`, `witnessed`) was wider than necessary — a fresh, unmodified\n390-reseed only ever legitimately changes `seededAt` (a wall-clock timestamp);\n391-`schemaVersion`, `note`, `reportDigest`, and `witnessed` are all reproduced\n392:exactly. With the wider set, the test would still pass against a CLI that\n393-emitted a forged `schemaVersion`, a stale `reportDigest`, or a missing\n394-`witnessed` block.\n395-\n396-Fix: narrowed `mayDiffer` to `{'seededAt'}` only.\n397-\n398:- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n399-- No source mutation needed — this was a test-construction defect (the\n400-  exclusion set was over-broad), the same category as the round-15/16\n401-  findings it's fixing. Verified by re-running the narrowed test against the\n402:  real CLI: still passes, confirming `schemaVersion`, `note`, `reportDigest`,\n403-  and `witnessed` are in fact reproduced exactly on a genuine reseed, and\n404:  that the test would now fail if any of them weren't.\n405-\n406-### P2 — runtime one-hop guard had no load-bearing mutation proof\n407-\n408-The round-16 test for the `toSchemaVersion` runtime check asserted only that\n409-the module constant and the live schema version agree — its comment claimed\n410:\"if the runtime check in the source were deleted, this still passes,\"\n411-treating the check as untestable given the earlier manifest-identity guard.\n412-Codex asked for an actual mutation, not that assumption.\n413-\n--\n415-*value* of `CORRELATION_BENCH_V12_TO_V13_MIGRATION.toSchemaVersion` (not\n416-deleting the runtime check) leaves the manifest self-identical (it's compared\n417-against itself via the same imported constant), so the identity guard still\n418:passes — and the runtime check then correctly refuses, proving it IS\n419-reachable through the real validator, just not via a caller-supplied\n420-mismatched manifest object.\n421-\n422-- File: `src/services/correlation/bench-correlation-baseline.ts`.\n423-- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`.\n424-- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\n425:  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (bypasses the TS literal type\n426-  since `tsx` transpiles without type-checking; the mismatch is real at\n427-  runtime).\n428-- Confirmed diff: `git diff` showed exactly the one-line value change.\n429:- Targeted result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n430:- Failing tests: `accepts the real additive transition` — failed with reason\n431-  `v12→v13 migration manifest targets schemaVersion 99, but the compiled gate\n432-  is schemaVersion 13 — this migration is no longer the one-hop path to the\n433-  live schema` (the real validator's refusal, proving the check fires\n434-  through the public function); `pins the migration to exactly one hop` and\n435-  `the runtime one-hop check is real code, not just a test-level assertion`\n436:  — both failed on `99 !== 13`, as expected for assertions against the\n437:  mutated constant. No other test in the 220-test file was affected.\n438-- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n439-  (matches before checksum exactly).\n440-- The test's comment was corrected to remove the disproven \"if deleted, this\n441:  still passes\" claim and point to this proof instead of asserting an\n442-  untested assumption.\n443-\n444-### P2 — tolerances-authentication check (P1 fix) lacked mutation proof\n--\n458-  benchTolerancesDigest(...) !== manifest.previousTolerancesDigest)`\n459-  (short-circuits the check to never fire).\n460-- Confirmed diff: `git diff` showed exactly the one-line `false &&` insertion.\n461:- Targeted result: `bench-correlation.test.mts` → `219 pass / 1 fail`.\n462-- Failing test: exactly `refuses a previous baseline whose tolerances are not\n463:  the reviewed ones — the shared-gate forgery Codex found` failed on\n464-  `true !== false` (the test's `assert.equal(ok, false)` — with the check\n465-  short-circuited, the forged-tolerance fixture is accepted as `ok:true`). No\n466:  other test in the 220-test file was affected, confirming this is the sole\n467-  test depending on the check.\n468-- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n469-  (matches before checksum exactly).\n470-\n471:Full suite after all three round-17 fixes: `220 pass / 0 fail`\n472-(`bench-correlation.test.mts`), `npx tsc --noEmit` (both tsconfig.json and\n473-tsconfig.api.json) → 0 errors.\n474-\n--\n480-> \"[P2] The one-hop guard still lacks the required revert-the-fix mutation\n481-> proof. Round 17 mutated the manifest value, not the guard at\n482-> bench-correlation-baseline.ts:383. I independently short-circuited that\n483:> guard; all three relevant tests remained green (3 pass / 0 fail).\n484-> Therefore the test comment and the evidence write-up incorrectly say guard\n485-> deletion was performed and disproved. The toSchemaVersion: 99 mutation\n486-> does prove the negative path is reachable, but not that removing the fix\n487:> turns the suite red. Add a test that asserts the one-hop refusal reason\n488:> through the validator and fails when this guard is disabled.\"\n489-\n490-This is correct. Round 17 mutated only the pinned constant's *value*\n491-(`toSchemaVersion: 99`) with the guard's *code* intact, and confirmed 3\n492:tests failed. But with the real, unmutated inputs the constant and the\n493-compiled schema version are always in sync (13 === 13), so the guard's\n494-branch condition (`manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION`)\n495-is never true whether or not the guard code exists — disabling the guard\n--\n511-- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\n512-  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (line 288). Guard code at line\n513-  383 untouched.\n514:- Result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n515:- Failing tests, with exact assertion text:\n516-  1. `round 15 — the v12→v13 schema bump is a pinned migration, not a\n517-     skipped check` — `false !== true` against the expected refusal reason\n518-     `\"v12→v13 migration manifest targets schemaVersion 99, but the\n--\n529-  CORRELATION_BENCH_SCHEMA_VERSION) {` → `if (false &&\n530-  manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION) {` (line\n531-  383), applied on top of the still-active `toSchemaVersion: 99` mutation.\n532:- Result: `bench-correlation.test.mts` → `218 pass / 2 fail`.\n533:- The two remaining failures are exactly `pins the migration to exactly one\n534-  hop` and `the runtime one-hop check is real code, not just a test-level\n535:  assertion` — both fail on the raw constant-value assertion (`99 !== 13`),\n536:  which is unrelated to the guard's presence and would fail regardless.\n537-- Critically, `round 15 — the v12→v13 schema bump is a pinned migration, not\n538:  a skipped check` (test 1 above) — which had failed on `false !== true` in\n539:  Step 1 — now **passes** in Step 2. With the guard's code disabled, the\n540:  validator's `ok` result flips back to `true` even though the manifest\n541-  still targets a stale schema (99 vs the compiled 13). This is the\n542-  false-positive accept: disabling the guard's *code*, not just having a\n543-  drifted value, is what causes the migration to be silently accepted. This\n--\n546-  `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n547-  (matches before checksum exactly). `git diff --stat` on the file showed no\n548-  diff.\n549:- Full suite after restoration: `bench-correlation.test.mts` →\n550:  `220 pass / 0 fail`.\n551-\n552-### Permanent test (not just a doc-recorded mutation experiment)\n553-\n554-Codex explicitly asked for \"a test that asserts the one-hop refusal reason\n555:through the validator and fails when this guard is disabled.\" Because the\n556-manifest-identity guard at line 355 makes the `toSchemaVersion` branch\n557-unreachable via any caller-supplied manifest — the only way to exercise it\n558-through `validateCorrelationBenchV12ToV13Migration` is to mutate the pinned\n--\n560-unit test — the comparison was extracted into a new exported pure function,\n561-`oneHopSchemaVersionCheckReason(manifestToSchemaVersion, compiledSchemaVersion)`,\n562-and the validator now calls it instead of inlining the check. Two new\n563:permanent tests in `bench-correlation.test.mts` call this real function\n564-directly (not a re-implementation):\n565-\n566-- `oneHopSchemaVersionCheckReason refuses a manifest that no longer targets\n--\n569-- `oneHopSchemaVersionCheckReason accepts a manifest that targets the\n570-  compiled schema` — asserts `null` for matching versions.\n571-\n572:These tests call the exact function the validator invokes, so deleting or\n573:weakening `oneHopSchemaVersionCheckReason`'s own logic fails them directly.\n574:Full suite after this change: `222 pass / 0 fail`. `npm run typecheck:all` →\n575-0 errors.\n576-\n577-**This claim turned out to be incomplete** — see round 18b below.\n--\n580-\n581-> [P2] The permanent regression test still does not exercise validator\n582-> wiring. Deleting the call at bench-correlation-baseline.ts:405 leaves both\n583:> helper tests and the matching-input validator test green. Thus the claim\n584-> at ACC-501-MUTATION-PROOFS.md:572 is false, and round 17's requested\n585-> validator-level regression test remains missing. The combined manual\n586-> mutation is valid evidence, but it is not permanent coverage.\n--\n590-`validateCorrelationBenchV12ToV13Migration` still *calls* it — removing a\n591-refusal check only produces false negatives (missed refusals), never false\n592-positives on already-correct input, so deleting the call site left every\n593:existing test green (the helper's own tests, and the validator's\n594-matching-input `ok === true` test).\n595-\n596-### Root cause: the manifest-identity guard, again\n--\n644-reachable through the public validator for the first time, closing the gap\n645-round 18b identified.\n646-\n647:### Mutation proof — deleting the call site fails the new test\n648-\n649-Checksum before mutation: `4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc`.\n650-\n--\n652-(the four lines pushing `oneHopReason` into `reasons`), leaving everything\n653-else, including `manifestToSchemaVersion`'s extraction, intact.\n654-\n655:**Result**: `223 tests / 222 pass / 1 fail`. The new validator-level test\n656:failed exactly as expected:\n657-\n658-```\n659-✖ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n--\n662-```\n663-\n664-`round 15 — the v12→v13 schema bump is a pinned migration, not a skipped\n665:check` also failed, matching round 18's earlier combined-mutation proof —\n666-consistent evidence that this is the same load-bearing check, now reachable\n667-without mutating a module constant.\n668-\n--\n673-\n674-**Full verification chain after this fix**:\n675-\n676:- `bench-correlation.test.mts` — `223 pass / 0 fail`\n677-- `npm run typecheck:all` — 0 errors\n678-- `npm run bench:correlation` — PASS, within tolerance of the committed baseline\n679:- `npm run test:correlation` — `416 pass / 0 fail`\n680-\n681-## Restoration\n682-\n\nexec\n/bin/zsh -lc 'rg -n -C 4 \"unreach|reach|one-hop|oneHop|toSchemaVersion|manifest-identity|identity guard|blanket\" docs/validation/ACC-501-MUTATION-PROOFS.md src/services/correlation/bench-correlation-baseline.ts' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\nsrc/services/correlation/bench-correlation-baseline.ts-253- * be waved through by a check that never executed.\nsrc/services/correlation/bench-correlation-baseline.ts-254- */\nsrc/services/correlation/bench-correlation-baseline.ts-255-export interface CorrelationBenchMigrationV12ToV13 {\nsrc/services/correlation/bench-correlation-baseline.ts-256-  fromSchemaVersion: 12;\nsrc/services/correlation/bench-correlation-baseline.ts:257:  toSchemaVersion: 13;\nsrc/services/correlation/bench-correlation-baseline.ts-258-  previousCorpusDigest: string;\nsrc/services/correlation/bench-correlation-baseline.ts-259-  previousReportDigest: string;\nsrc/services/correlation/bench-correlation-baseline.ts-260-  /**\nsrc/services/correlation/bench-correlation-baseline.ts-261-   * Digest over the previous baseline's own carried-over payload.\n--\nsrc/services/correlation/bench-correlation-baseline.ts-284-}\nsrc/services/correlation/bench-correlation-baseline.ts-285-\nsrc/services/correlation/bench-correlation-baseline.ts-286-export const CORRELATION_BENCH_V12_TO_V13_MIGRATION: CorrelationBenchMigrationV12ToV13 = {\nsrc/services/correlation/bench-correlation-baseline.ts-287-  fromSchemaVersion: 12,\nsrc/services/correlation/bench-correlation-baseline.ts:288:  toSchemaVersion: 13,\nsrc/services/correlation/bench-correlation-baseline.ts-289-  previousCorpusDigest: '1a0377333099795fe0ccff8dc5a7a2cf',\nsrc/services/correlation/bench-correlation-baseline.ts-290-  previousReportDigest: '354a7790613214a893698b1882eda0ae',\nsrc/services/correlation/bench-correlation-baseline.ts-291-  previousPayloadDigest: '129ab5924c521a1897a907059de7ffac',\nsrc/services/correlation/bench-correlation-baseline.ts-292-  previousTolerancesDigest: benchTolerancesDigest({\n--\nsrc/services/correlation/bench-correlation-baseline.ts-346-  return digestRecords(records);\nsrc/services/correlation/bench-correlation-baseline.ts-347-}\nsrc/services/correlation/bench-correlation-baseline.ts-348-\nsrc/services/correlation/bench-correlation-baseline.ts-349-/**\nsrc/services/correlation/bench-correlation-baseline.ts:350: * Pure one-hop schema check, extracted so it is directly testable in\nsrc/services/correlation/bench-correlation-baseline.ts-351- * isolation from the rest of validateCorrelationBenchV12ToV13Migration.\nsrc/services/correlation/bench-correlation-baseline.ts:352: * The validator's manifest-identity guard excludes toSchemaVersion from its\nsrc/services/correlation/bench-correlation-baseline.ts:353: * blanket comparison specifically so a caller-supplied manifest differing\nsrc/services/correlation/bench-correlation-baseline.ts:354: * only in this field reaches this check instead of being swallowed there.\nsrc/services/correlation/bench-correlation-baseline.ts-355- * Returns the refusal reason string, or null when the manifest still\nsrc/services/correlation/bench-correlation-baseline.ts-356- * targets the compiled schema.\nsrc/services/correlation/bench-correlation-baseline.ts-357- */\nsrc/services/correlation/bench-correlation-baseline.ts:358:export function oneHopSchemaVersionCheckReason(\nsrc/services/correlation/bench-correlation-baseline.ts-359-  manifestToSchemaVersion: number,\nsrc/services/correlation/bench-correlation-baseline.ts-360-  compiledSchemaVersion: number,\nsrc/services/correlation/bench-correlation-baseline.ts-361-): string | null {\nsrc/services/correlation/bench-correlation-baseline.ts-362-  if (manifestToSchemaVersion !== compiledSchemaVersion) {\nsrc/services/correlation/bench-correlation-baseline.ts-363-    return (\nsrc/services/correlation/bench-correlation-baseline.ts-364-      `v12→v13 migration manifest targets schemaVersion ${String(manifestToSchemaVersion)}, but the ` +\nsrc/services/correlation/bench-correlation-baseline.ts-365-      `compiled gate is schemaVersion ${String(compiledSchemaVersion)} — this migration is ` +\nsrc/services/correlation/bench-correlation-baseline.ts:366:      'no longer the one-hop path to the live schema'\nsrc/services/correlation/bench-correlation-baseline.ts-367-    );\nsrc/services/correlation/bench-correlation-baseline.ts-368-  }\nsrc/services/correlation/bench-correlation-baseline.ts-369-  return null;\nsrc/services/correlation/bench-correlation-baseline.ts-370-}\n--\nsrc/services/correlation/bench-correlation-baseline.ts-374-  previousV12: CorrelationBenchBaseline,\nsrc/services/correlation/bench-correlation-baseline.ts-375-  manifest: CorrelationBenchMigrationV12ToV13,\nsrc/services/correlation/bench-correlation-baseline.ts-376-): CorrelationBenchComparison {\nsrc/services/correlation/bench-correlation-baseline.ts-377-  const reasons: string[] = [];\nsrc/services/correlation/bench-correlation-baseline.ts:378:  // toSchemaVersion is deliberately excluded from this identity comparison —\nsrc/services/correlation/bench-correlation-baseline.ts:379:  // it gets its OWN check below via oneHopSchemaVersionCheckReason(), so a\nsrc/services/correlation/bench-correlation-baseline.ts-380-  // caller-supplied manifest that matches the pinned migration in every\nsrc/services/correlation/bench-correlation-baseline.ts:381:  // OTHER field still reaches that check instead of being swallowed here.\nsrc/services/correlation/bench-correlation-baseline.ts:382:  // Round 18: Codex found that folding toSchemaVersion into this blanket\nsrc/services/correlation/bench-correlation-baseline.ts:383:  // identity guard made the schema check unreachable through the public\nsrc/services/correlation/bench-correlation-baseline.ts-384-  // validator — the only way to exercise it was to mutate the pinned module\nsrc/services/correlation/bench-correlation-baseline.ts-385-  // constant, which can't be a permanent regression test.\nsrc/services/correlation/bench-correlation-baseline.ts:386:  const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\nsrc/services/correlation/bench-correlation-baseline.ts-387-  const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\nsrc/services/correlation/bench-correlation-baseline.ts:388:  delete pinnedRest.toSchemaVersion;\nsrc/services/correlation/bench-correlation-baseline.ts-389-  if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\nsrc/services/correlation/bench-correlation-baseline.ts-390-    reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\nsrc/services/correlation/bench-correlation-baseline.ts-391-    return { ok: false, reasons };\nsrc/services/correlation/bench-correlation-baseline.ts-392-  }\nsrc/services/correlation/bench-correlation-baseline.ts:393:  const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\nsrc/services/correlation/bench-correlation-baseline.ts:394:  if (oneHopReason !== null) {\nsrc/services/correlation/bench-correlation-baseline.ts:395:    reasons.push(oneHopReason);\nsrc/services/correlation/bench-correlation-baseline.ts-396-  }\nsrc/services/correlation/bench-correlation-baseline.ts-397-  if (previousV12.schemaVersion !== 12) {\nsrc/services/correlation/bench-correlation-baseline.ts-398-    reasons.push(`v12→v13 migration source is schemaVersion ${previousV12.schemaVersion}, not 12`);\nsrc/services/correlation/bench-correlation-baseline.ts-399-  }\n--\nsrc/services/correlation/bench-correlation-baseline.ts-452-}\nsrc/services/correlation/bench-correlation-baseline.ts-453-\nsrc/services/correlation/bench-correlation-baseline.ts-454-export interface CorrelationBenchMigrationV11ToV12 {\nsrc/services/correlation/bench-correlation-baseline.ts-455-  fromSchemaVersion: 11;\nsrc/services/correlation/bench-correlation-baseline.ts:456:  toSchemaVersion: 12;\nsrc/services/correlation/bench-correlation-baseline.ts-457-  previousCorpusDigest: string;\nsrc/services/correlation/bench-correlation-baseline.ts-458-  previousReportDigest: string;\nsrc/services/correlation/bench-correlation-baseline.ts-459-  changedStream: {\nsrc/services/correlation/bench-correlation-baseline.ts-460-    id: 'inhibitory-pair';\n--\nsrc/services/correlation/bench-correlation-baseline.ts-527-);\nsrc/services/correlation/bench-correlation-baseline.ts-528-\nsrc/services/correlation/bench-correlation-baseline.ts-529-export const CORRELATION_BENCH_V11_TO_V12_MIGRATION: CorrelationBenchMigrationV11ToV12 = {\nsrc/services/correlation/bench-correlation-baseline.ts-530-  fromSchemaVersion: 11,\nsrc/services/correlation/bench-correlation-baseline.ts:531:  toSchemaVersion: 12,\nsrc/services/correlation/bench-correlation-baseline.ts-532-  previousCorpusDigest: CORRELATION_BENCH_V11_ANCHORS.corpusDigest,\nsrc/services/correlation/bench-correlation-baseline.ts-533-  previousReportDigest: CORRELATION_BENCH_V11_ANCHORS.reportDigest,\nsrc/services/correlation/bench-correlation-baseline.ts-534-  changedStream: {\nsrc/services/correlation/bench-correlation-baseline.ts-535-    id: 'inhibitory-pair',\n--\nsrc/services/correlation/bench-correlation-baseline.ts-922-  }\nsrc/services/correlation/bench-correlation-baseline.ts-923-  for (const [key, value] of Object.entries(raw as Record<string, unknown>)) {\nsrc/services/correlation/bench-correlation-baseline.ts-924-    // An own-property test, not `key in tol`: `in` walks the prototype chain,\nsrc/services/correlation/bench-correlation-baseline.ts-925-    // so \"constructor\" / \"toString\" / \"__proto__\" would pass as known gates —\nsrc/services/correlation/bench-correlation-baseline.ts:926:    // and `__proto__` assignment would reach the setter rather than the object.\nsrc/services/correlation/bench-correlation-baseline.ts-927-    // (`Object.hasOwn` is ES2022; this tsconfig's lib predates it.)\nsrc/services/correlation/bench-correlation-baseline.ts-928-    if (!Object.prototype.hasOwnProperty.call(tol, key)) {\nsrc/services/correlation/bench-correlation-baseline.ts-929-      reasons.push(`tolerance \"${key}\" is not a known gate — the baseline is stale or corrupt`);\nsrc/services/correlation/bench-correlation-baseline.ts-930-      continue;\n--\nsrc/services/correlation/bench-correlation-baseline.ts-2431-      reasons.push(`report is internally inconsistent: ${where} has no distinct endpoints`);\nsrc/services/correlation/bench-correlation-baseline.ts-2432-      continue;\nsrc/services/correlation/bench-correlation-baseline.ts-2433-    }\nsrc/services/correlation/bench-correlation-baseline.ts-2434-    // An endpoint the corpus never observed grades as 'unplanted' — which is\nsrc/services/correlation/bench-correlation-baseline.ts:2435:    // precisely the verdict a fabricated name is reaching for. Renaming the 14\nsrc/services/correlation/bench-correlation-baseline.ts-2436-    // unplanted rows to invented domains reconciled against every summary,\nsrc/services/correlation/bench-correlation-baseline.ts-2437-    // because \"not in the planted index\" and \"not in the corpus\" were the same\nsrc/services/correlation/bench-correlation-baseline.ts-2438-    // answer. The miner can only name domains it observed.\nsrc/services/correlation/bench-correlation-baseline.ts-2439-    const strays = [e.from, e.to].filter((d) => !domains.has(d));\n--\nsrc/services/correlation/bench-correlation-baseline.ts-2933-        `but the edge ledger grades ${actual} row(s) as '${verdict}'`,\nsrc/services/correlation/bench-correlation-baseline.ts-2934-      );\nsrc/services/correlation/bench-correlation-baseline.ts-2935-    }\nsrc/services/correlation/bench-correlation-baseline.ts-2936-  }\nsrc/services/correlation/bench-correlation-baseline.ts:2937:  // Recall's numerator, reached a third way: the summary says it, the missing\nsrc/services/correlation/bench-correlation-baseline.ts-2938-  // list implies it, and now the ledger's own causal rows count it.\nsrc/services/correlation/bench-correlation-baseline.ts-2939-  const recovered = report.plantedCausalCount - report.missingCouplings.length;\nsrc/services/correlation/bench-correlation-baseline.ts-2940-  if (causalRows !== recovered) {\nsrc/services/correlation/bench-correlation-baseline.ts-2941-    reasons.push(\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-196-The fix replaces it with two tests built on the CLI's own\ndocs/validation/ACC-501-MUTATION-PROOFS.md-197-`--previous-baseline <path>` override:\ndocs/validation/ACC-501-MUTATION-PROOFS.md-198-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-199-- **Refusal case**: writes a temp copy of the committed baseline with\ndocs/validation/ACC-501-MUTATION-PROOFS.md:200:  `couplingPrecision: 1.5` (unreachable — precision is a fraction) and\ndocs/validation/ACC-501-MUTATION-PROOFS.md-201-  `tolerances.couplingPrecisionDrop: 0`, runs `--seed --previous-baseline\ndocs/validation/ACC-501-MUTATION-PROOFS.md-202-  <temp>`, and asserts exit code `1`, `REFUSED` plus the specific `miner\ndocs/validation/ACC-501-MUTATION-PROOFS.md-203-  coupling precision` reason on stderr, and that stdout carries no seeded\ndocs/validation/ACC-501-MUTATION-PROOFS.md-204-  JSON (`\"schemaVersion\"` absent).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-205-- **Positive control**: writes an unmodified, internally consistent copy of\ndocs/validation/ACC-501-MUTATION-PROOFS.md-206-  the committed baseline as `--previous-baseline` and asserts exit code `0`,\ndocs/validation/ACC-501-MUTATION-PROOFS.md-207-  `Seeded from the current run` on stderr, no `REFUSED`, and a parseable\ndocs/validation/ACC-501-MUTATION-PROOFS.md-208-  seeded object on stdout — proving the comparator ran and passed on its own\ndocs/validation/ACC-501-MUTATION-PROOFS.md:209:  merits rather than the refusal path being unreachable.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-210-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-211-Two earlier attempts at the positive control failed for reasons worth\ndocs/validation/ACC-501-MUTATION-PROOFS.md-212-recording: raising `couplingPrecision` alone past its drop tolerance without\ndocs/validation/ACC-501-MUTATION-PROOFS.md-213-loosening the tolerance tripped the refusal (expected); loosening the\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-265-  demonstrates it fires, and `accepts the real additive transition with its\ndocs/validation/ACC-501-MUTATION-PROOFS.md-266-  real, unforged tolerances` demonstrates the pinned digest matches the real\ndocs/validation/ACC-501-MUTATION-PROOFS.md-267-  reviewed value so genuine migrations still pass.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-268-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:269:### P2 — refusal test never reached `checkDrop`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-270-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-271-The round-15 refusal test used `couplingPrecision: 1.5`, which trips an\ndocs/validation/ACC-501-MUTATION-PROOFS.md:272:earlier baseline range-validity guard (`[0,1]`) before ever reaching the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-273-tolerance-comparison logic (`checkDrop`) the test claimed to exercise — so it\ndocs/validation/ACC-501-MUTATION-PROOFS.md-274-proved the CLI refuses invalid input, not that the tolerance gate works.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-275-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-276-Fix: rewrote the test to use `meanTruePairConfidence: 1` (valid — sits at the\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-294-Fix: the positive control now compares the seeded output against the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-295-committed baseline field-by-field (union of both objects' keys, so a dropped\ndocs/validation/ACC-501-MUTATION-PROOFS.md-296-field is caught too), excluding only the bookkeeping fields an ordinary\ndocs/validation/ACC-501-MUTATION-PROOFS.md-297-reseed is allowed to touch (`schemaVersion`, `seededAt`, `note`,\ndocs/validation/ACC-501-MUTATION-PROOFS.md:298:`reportDigest`, `witnessed`) — reachable because the corpus is frozen and the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-299-benchmark is deterministic, so a fresh seed against an unmodified previous\ndocs/validation/ACC-501-MUTATION-PROOFS.md-300-baseline reproduces every graded field exactly.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-301-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-302-- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-305-  run actually violates` / `seeds cleanly against a --previous-baseline the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-306-  live run genuinely satisfies` both still pass; the latter now exercises the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-307-  field-by-field comparison, not just a `typeof` check).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-308-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:309:### P2 — one-hop migration not enforced at runtime\ndocs/validation/ACC-501-MUTATION-PROOFS.md-310-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:311:`manifest.toSchemaVersion === CORRELATION_BENCH_SCHEMA_VERSION` was only\ndocs/validation/ACC-501-MUTATION-PROOFS.md-312-asserted by a unit test (`pins the migration to exactly one hop`), not by the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-313-validator function itself — a future schema bump that forgot to retire or rev\ndocs/validation/ACC-501-MUTATION-PROOFS.md-314-this migration would pass every check in\ndocs/validation/ACC-501-MUTATION-PROOFS.md-315-`validateCorrelationBenchV12ToV13Migration` and silently become a two-hop\ndocs/validation/ACC-501-MUTATION-PROOFS.md:316:migration wearing a one-hop label.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-317-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-318-Fix: added a runtime check inside `validateCorrelationBenchV12ToV13Migration`\ndocs/validation/ACC-501-MUTATION-PROOFS.md:319:comparing `manifest.toSchemaVersion` against the live\ndocs/validation/ACC-501-MUTATION-PROOFS.md-320-`CORRELATION_BENCH_SCHEMA_VERSION` constant, pushing a refusal reason if they\ndocs/validation/ACC-501-MUTATION-PROOFS.md-321-diverge.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-322-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-323-- File: `src/services/correlation/bench-correlation-baseline.ts`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md:324:- Note on testability: the function's manifest-identity guard\ndocs/validation/ACC-501-MUTATION-PROOFS.md-325-  (`JSON.stringify(manifest) !== JSON.stringify(CORRELATION_BENCH_V12_TO_V13_MIGRATION)`)\ndocs/validation/ACC-501-MUTATION-PROOFS.md-326-  refuses any manifest that isn't byte-identical to the pinned module\ndocs/validation/ACC-501-MUTATION-PROOFS.md:327:  constant, which means this new check's only reachable failure mode in\ndocs/validation/ACC-501-MUTATION-PROOFS.md-328-  production is the pinned constant itself drifting from\ndocs/validation/ACC-501-MUTATION-PROOFS.md-329-  `CORRELATION_BENCH_SCHEMA_VERSION` — not independently mutable via a test\ndocs/validation/ACC-501-MUTATION-PROOFS.md:330:  argument without also tripping the identity guard first (both reasons\ndocs/validation/ACC-501-MUTATION-PROOFS.md:331:  would fire together; the identity guard firing is not a bypass, it's proof\ndocs/validation/ACC-501-MUTATION-PROOFS.md-332-  the two constants can't drift independently). Verified via `the runtime\ndocs/validation/ACC-501-MUTATION-PROOFS.md:333:  one-hop check is real code, not just a test-level assertion` — confirms\ndocs/validation/ACC-501-MUTATION-PROOFS.md-334-  the real manifest and the real schema-version constant agree, and that\ndocs/validation/ACC-501-MUTATION-PROOFS.md-335-  `validateCorrelationBenchV12ToV13Migration` returns `ok:true` under that\ndocs/validation/ACC-501-MUTATION-PROOFS.md-336-  agreement, so the added check does not falsely refuse the real migration.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-337-\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-365-  `does not carry the reviewed v12 payload` (from the earlier payload-digest\ndocs/validation/ACC-501-MUTATION-PROOFS.md-366-  guard, which also fires for this fixture) instead of the expected\ndocs/validation/ACC-501-MUTATION-PROOFS.md-367-  `legacyRetiredField moved from 42 to undefined` (which only the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-368-  union-iterating field loop produces) — confirming the union half of the key\ndocs/validation/ACC-501-MUTATION-PROOFS.md:369:  set is what makes that specific reason reachable. No other test in the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-370-  220-test file was affected.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-371-- Restored checksum: `04696770b63a5ba784692bb0ddbac66743f26b862d9ac9926177191974b3dbe6`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-372-  (matches before checksum exactly).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-373-\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-402-  real CLI: still passes, confirming `schemaVersion`, `note`, `reportDigest`,\ndocs/validation/ACC-501-MUTATION-PROOFS.md-403-  and `witnessed` are in fact reproduced exactly on a genuine reseed, and\ndocs/validation/ACC-501-MUTATION-PROOFS.md-404-  that the test would now fail if any of them weren't.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-405-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:406:### P2 — runtime one-hop guard had no load-bearing mutation proof\ndocs/validation/ACC-501-MUTATION-PROOFS.md-407-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:408:The round-16 test for the `toSchemaVersion` runtime check asserted only that\ndocs/validation/ACC-501-MUTATION-PROOFS.md-409-the module constant and the live schema version agree — its comment claimed\ndocs/validation/ACC-501-MUTATION-PROOFS.md-410-\"if the runtime check in the source were deleted, this still passes,\"\ndocs/validation/ACC-501-MUTATION-PROOFS.md:411:treating the check as untestable given the earlier manifest-identity guard.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-412-Codex asked for an actual mutation, not that assumption.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-413-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-414-Performing the mutation disproved the comment's own claim: mutating the\ndocs/validation/ACC-501-MUTATION-PROOFS.md:415:*value* of `CORRELATION_BENCH_V12_TO_V13_MIGRATION.toSchemaVersion` (not\ndocs/validation/ACC-501-MUTATION-PROOFS.md-416-deleting the runtime check) leaves the manifest self-identical (it's compared\ndocs/validation/ACC-501-MUTATION-PROOFS.md:417:against itself via the same imported constant), so the identity guard still\ndocs/validation/ACC-501-MUTATION-PROOFS.md-418-passes — and the runtime check then correctly refuses, proving it IS\ndocs/validation/ACC-501-MUTATION-PROOFS.md:419:reachable through the real validator, just not via a caller-supplied\ndocs/validation/ACC-501-MUTATION-PROOFS.md-420-mismatched manifest object.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-421-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-422-- File: `src/services/correlation/bench-correlation-baseline.ts`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-423-- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md:424:- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-425-  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (bypasses the TS literal type\ndocs/validation/ACC-501-MUTATION-PROOFS.md-426-  since `tsx` transpiles without type-checking; the mismatch is real at\ndocs/validation/ACC-501-MUTATION-PROOFS.md-427-  runtime).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-428-- Confirmed diff: `git diff` showed exactly the one-line value change.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-429-- Targeted result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-430-- Failing tests: `accepts the real additive transition` — failed with reason\ndocs/validation/ACC-501-MUTATION-PROOFS.md-431-  `v12→v13 migration manifest targets schemaVersion 99, but the compiled gate\ndocs/validation/ACC-501-MUTATION-PROOFS.md:432:  is schemaVersion 13 — this migration is no longer the one-hop path to the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-433-  live schema` (the real validator's refusal, proving the check fires\ndocs/validation/ACC-501-MUTATION-PROOFS.md-434-  through the public function); `pins the migration to exactly one hop` and\ndocs/validation/ACC-501-MUTATION-PROOFS.md:435:  `the runtime one-hop check is real code, not just a test-level assertion`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-436-  — both failed on `99 !== 13`, as expected for assertions against the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-437-  mutated constant. No other test in the 220-test file was affected.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-438-- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-439-  (matches before checksum exactly).\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-450-any other check in this function.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-451-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-452-- File: `src/services/correlation/bench-correlation-baseline.ts`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-453-- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\ndocs/validation/ACC-501-MUTATION-PROOFS.md:454:  (same file identity as the toSchemaVersion proof above, run as a separate\ndocs/validation/ACC-501-MUTATION-PROOFS.md-455-  mutation after restoring that one).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-456-- Mutation: `if (benchTolerancesDigest(previousV12.tolerances) !==\ndocs/validation/ACC-501-MUTATION-PROOFS.md-457-  manifest.previousTolerancesDigest)` → `if (false &&\ndocs/validation/ACC-501-MUTATION-PROOFS.md-458-  benchTolerancesDigest(...) !== manifest.previousTolerancesDigest)`\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-476-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-477-Codex's third review of PR #1625 found a real flaw in the round-17 proof\ndocs/validation/ACC-501-MUTATION-PROOFS.md-478-above:\ndocs/validation/ACC-501-MUTATION-PROOFS.md-479-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:480:> \"[P2] The one-hop guard still lacks the required revert-the-fix mutation\ndocs/validation/ACC-501-MUTATION-PROOFS.md-481-> proof. Round 17 mutated the manifest value, not the guard at\ndocs/validation/ACC-501-MUTATION-PROOFS.md-482-> bench-correlation-baseline.ts:383. I independently short-circuited that\ndocs/validation/ACC-501-MUTATION-PROOFS.md-483-> guard; all three relevant tests remained green (3 pass / 0 fail).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-484-> Therefore the test comment and the evidence write-up incorrectly say guard\ndocs/validation/ACC-501-MUTATION-PROOFS.md:485:> deletion was performed and disproved. The toSchemaVersion: 99 mutation\ndocs/validation/ACC-501-MUTATION-PROOFS.md:486:> does prove the negative path is reachable, but not that removing the fix\ndocs/validation/ACC-501-MUTATION-PROOFS.md:487:> turns the suite red. Add a test that asserts the one-hop refusal reason\ndocs/validation/ACC-501-MUTATION-PROOFS.md-488-> through the validator and fails when this guard is disabled.\"\ndocs/validation/ACC-501-MUTATION-PROOFS.md-489-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-490-This is correct. Round 17 mutated only the pinned constant's *value*\ndocs/validation/ACC-501-MUTATION-PROOFS.md:491:(`toSchemaVersion: 99`) with the guard's *code* intact, and confirmed 3\ndocs/validation/ACC-501-MUTATION-PROOFS.md-492-tests failed. But with the real, unmutated inputs the constant and the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-493-compiled schema version are always in sync (13 === 13), so the guard's\ndocs/validation/ACC-501-MUTATION-PROOFS.md:494:branch condition (`manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION`)\ndocs/validation/ACC-501-MUTATION-PROOFS.md-495-is never true whether or not the guard code exists — disabling the guard\ndocs/validation/ACC-501-MUTATION-PROOFS.md-496-alone, with in-sync inputs, is a no-op test that proves nothing about the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-497-guard's necessity.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-498-\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-507-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-508-**Step 1 — guard restored, constant drifted only** (reproduces round 17's\ndocs/validation/ACC-501-MUTATION-PROOFS.md-509-proof, captured with exact identities this time):\ndocs/validation/ACC-501-MUTATION-PROOFS.md-510-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:511:- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-512-  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (line 288). Guard code at line\ndocs/validation/ACC-501-MUTATION-PROOFS.md-513-  383 untouched.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-514-- Result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-515-- Failing tests, with exact assertion text:\ndocs/validation/ACC-501-MUTATION-PROOFS.md-516-  1. `round 15 — the v12→v13 schema bump is a pinned migration, not a\ndocs/validation/ACC-501-MUTATION-PROOFS.md-517-     skipped check` — `false !== true` against the expected refusal reason\ndocs/validation/ACC-501-MUTATION-PROOFS.md-518-     `\"v12→v13 migration manifest targets schemaVersion 99, but the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-519-     compiled gate is schemaVersion 13 — this migration is no longer the\ndocs/validation/ACC-501-MUTATION-PROOFS.md:520:     one-hop path to the live schema\"`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-521-  2. `pins the migration to exactly one hop` — `99 !== 13`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md:522:  3. `the runtime one-hop check is real code, not just a test-level\ndocs/validation/ACC-501-MUTATION-PROOFS.md-523-     assertion` — `99 !== 13`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-524-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-525-**Step 2 — guard ALSO disabled, constant still drifted** (the genuine\ndocs/validation/ACC-501-MUTATION-PROOFS.md-526-revert-the-fix proof):\ndocs/validation/ACC-501-MUTATION-PROOFS.md-527-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:528:- Additional mutation: `if (manifest.toSchemaVersion !==\ndocs/validation/ACC-501-MUTATION-PROOFS.md-529-  CORRELATION_BENCH_SCHEMA_VERSION) {` → `if (false &&\ndocs/validation/ACC-501-MUTATION-PROOFS.md:530:  manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION) {` (line\ndocs/validation/ACC-501-MUTATION-PROOFS.md:531:  383), applied on top of the still-active `toSchemaVersion: 99` mutation.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-532-- Result: `bench-correlation.test.mts` → `218 pass / 2 fail`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-533-- The two remaining failures are exactly `pins the migration to exactly one\ndocs/validation/ACC-501-MUTATION-PROOFS.md:534:  hop` and `the runtime one-hop check is real code, not just a test-level\ndocs/validation/ACC-501-MUTATION-PROOFS.md-535-  assertion` — both fail on the raw constant-value assertion (`99 !== 13`),\ndocs/validation/ACC-501-MUTATION-PROOFS.md-536-  which is unrelated to the guard's presence and would fail regardless.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-537-- Critically, `round 15 — the v12→v13 schema bump is a pinned migration, not\ndocs/validation/ACC-501-MUTATION-PROOFS.md-538-  a skipped check` (test 1 above) — which had failed on `false !== true` in\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-550-  `220 pass / 0 fail`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-551-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-552-### Permanent test (not just a doc-recorded mutation experiment)\ndocs/validation/ACC-501-MUTATION-PROOFS.md-553-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:554:Codex explicitly asked for \"a test that asserts the one-hop refusal reason\ndocs/validation/ACC-501-MUTATION-PROOFS.md-555-through the validator and fails when this guard is disabled.\" Because the\ndocs/validation/ACC-501-MUTATION-PROOFS.md:556:manifest-identity guard at line 355 makes the `toSchemaVersion` branch\ndocs/validation/ACC-501-MUTATION-PROOFS.md:557:unreachable via any caller-supplied manifest — the only way to exercise it\ndocs/validation/ACC-501-MUTATION-PROOFS.md-558-through `validateCorrelationBenchV12ToV13Migration` is to mutate the pinned\ndocs/validation/ACC-501-MUTATION-PROOFS.md-559-module constant itself, which cannot be part of a permanent, always-runnable\ndocs/validation/ACC-501-MUTATION-PROOFS.md-560-unit test — the comparison was extracted into a new exported pure function,\ndocs/validation/ACC-501-MUTATION-PROOFS.md:561:`oneHopSchemaVersionCheckReason(manifestToSchemaVersion, compiledSchemaVersion)`,\ndocs/validation/ACC-501-MUTATION-PROOFS.md-562-and the validator now calls it instead of inlining the check. Two new\ndocs/validation/ACC-501-MUTATION-PROOFS.md-563-permanent tests in `bench-correlation.test.mts` call this real function\ndocs/validation/ACC-501-MUTATION-PROOFS.md-564-directly (not a re-implementation):\ndocs/validation/ACC-501-MUTATION-PROOFS.md-565-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:566:- `oneHopSchemaVersionCheckReason refuses a manifest that no longer targets\ndocs/validation/ACC-501-MUTATION-PROOFS.md-567-  the compiled schema` — asserts the exact refusal reason string for\ndocs/validation/ACC-501-MUTATION-PROOFS.md-568-  `(99, CORRELATION_BENCH_SCHEMA_VERSION)`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md:569:- `oneHopSchemaVersionCheckReason accepts a manifest that targets the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-570-  compiled schema` — asserts `null` for matching versions.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-571-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-572-These tests call the exact function the validator invokes, so deleting or\ndocs/validation/ACC-501-MUTATION-PROOFS.md:573:weakening `oneHopSchemaVersionCheckReason`'s own logic fails them directly.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-574-Full suite after this change: `222 pass / 0 fail`. `npm run typecheck:all` →\ndocs/validation/ACC-501-MUTATION-PROOFS.md-575-0 errors.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-576-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-577-**This claim turned out to be incomplete** — see round 18b below.\n--\ndocs/validation/ACC-501-MUTATION-PROOFS.md-584-> at ACC-501-MUTATION-PROOFS.md:572 is false, and round 17's requested\ndocs/validation/ACC-501-MUTATION-PROOFS.md-585-> validator-level regression test remains missing. The combined manual\ndocs/validation/ACC-501-MUTATION-PROOFS.md-586-> mutation is valid evidence, but it is not permanent coverage.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-587-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:588:Correct. Testing `oneHopSchemaVersionCheckReason()` directly proves the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-589-check's own logic is right, but proves nothing about whether\ndocs/validation/ACC-501-MUTATION-PROOFS.md-590-`validateCorrelationBenchV12ToV13Migration` still *calls* it — removing a\ndocs/validation/ACC-501-MUTATION-PROOFS.md-591-refusal check only produces false negatives (missed refusals), never false\ndocs/validation/ACC-501-MUTATION-PROOFS.md-592-positives on already-correct input, so deleting the call site left every\ndocs/validation/ACC-501-MUTATION-PROOFS.md-593-existing test green (the helper's own tests, and the validator's\ndocs/validation/ACC-501-MUTATION-PROOFS.md-594-matching-input `ok === true` test).\ndocs/validation/ACC-501-MUTATION-PROOFS.md-595-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:596:### Root cause: the manifest-identity guard, again\ndocs/validation/ACC-501-MUTATION-PROOFS.md-597-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-598-The reason this was untestable through the public validator is the same\ndocs/validation/ACC-501-MUTATION-PROOFS.md-599-guard flagged in round 15/17: `validateCorrelationBenchV12ToV13Migration`\ndocs/validation/ACC-501-MUTATION-PROOFS.md:600:did a blanket `JSON.stringify(manifest) !== JSON.stringify(pinned)`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-601-comparison that early-returned before ANY caller-supplied manifest\ndocs/validation/ACC-501-MUTATION-PROOFS.md:602:differing in even one field — including `toSchemaVersion` — could reach a\ndocs/validation/ACC-501-MUTATION-PROOFS.md-603-field-specific check. A manifest built to differ from the pinned constant\ndocs/validation/ACC-501-MUTATION-PROOFS.md:604:only in `toSchemaVersion` was swallowed by the generic \"manifest does not\ndocs/validation/ACC-501-MUTATION-PROOFS.md:605:match\" refusal before ever reaching the one-hop check.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-606-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:607:### Fix: exclude `toSchemaVersion` from the blanket identity comparison\ndocs/validation/ACC-501-MUTATION-PROOFS.md-608-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:609:`validateCorrelationBenchV12ToV13Migration` now destructures `toSchemaVersion`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-610-out of the caller manifest, and strips it from a shallow copy of the pinned\ndocs/validation/ACC-501-MUTATION-PROOFS.md-611-constant, before the identity comparison — then checks it separately via\ndocs/validation/ACC-501-MUTATION-PROOFS.md:612:`oneHopSchemaVersionCheckReason` immediately after. (An initial destructuring\ndocs/validation/ACC-501-MUTATION-PROOFS.md:613:form of the pinned-side omission, `const { toSchemaVersion: _pinned, ...pinnedRest }`,\ndocs/validation/ACC-501-MUTATION-PROOFS.md-614-tripped `@typescript-eslint/no-unused-vars` / `sonarjs/no-unused-vars`; the\ndocs/validation/ACC-501-MUTATION-PROOFS.md-615-`delete`-based form below is what actually shipped.)\ndocs/validation/ACC-501-MUTATION-PROOFS.md-616-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-617-```ts\ndocs/validation/ACC-501-MUTATION-PROOFS.md:618:const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\ndocs/validation/ACC-501-MUTATION-PROOFS.md-619-const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\ndocs/validation/ACC-501-MUTATION-PROOFS.md:620:delete pinnedRest.toSchemaVersion;\ndocs/validation/ACC-501-MUTATION-PROOFS.md-621-if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\ndocs/validation/ACC-501-MUTATION-PROOFS.md-622-  reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\ndocs/validation/ACC-501-MUTATION-PROOFS.md-623-  return { ok: false, reasons };\ndocs/validation/ACC-501-MUTATION-PROOFS.md-624-}\ndocs/validation/ACC-501-MUTATION-PROOFS.md:625:const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\ndocs/validation/ACC-501-MUTATION-PROOFS.md:626:if (oneHopReason !== null) {\ndocs/validation/ACC-501-MUTATION-PROOFS.md:627:  reasons.push(oneHopReason);\ndocs/validation/ACC-501-MUTATION-PROOFS.md-628-}\ndocs/validation/ACC-501-MUTATION-PROOFS.md-629-```\ndocs/validation/ACC-501-MUTATION-PROOFS.md-630-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-631-The comparison is against the independently-compiled `CORRELATION_BENCH_SCHEMA_VERSION`\ndocs/validation/ACC-501-MUTATION-PROOFS.md:632:constant, not the pinned constant's own `toSchemaVersion` field — comparing\ndocs/validation/ACC-501-MUTATION-PROOFS.md-633-against the latter would silently defeat the check's purpose, since it would\ndocs/validation/ACC-501-MUTATION-PROOFS.md-634-just compare the pinned constant against a caller manifest copying it,\ndocs/validation/ACC-501-MUTATION-PROOFS.md-635-never catching the pinned constant itself drifting from the compiled schema.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-636-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-637-### New permanent test — through the real validator\ndocs/validation/ACC-501-MUTATION-PROOFS.md-638-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:639:`refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\ndocs/validation/ACC-501-MUTATION-PROOFS.md-640-the compiled schema — validator wiring, not just the helper` builds\ndocs/validation/ACC-501-MUTATION-PROOFS.md:641:`{ ...manifest, toSchemaVersion: 99 }` (identical to the pinned migration in\ndocs/validation/ACC-501-MUTATION-PROOFS.md-642-every other field) and calls `validateCorrelationBenchV12ToV13Migration`\ndocs/validation/ACC-501-MUTATION-PROOFS.md-643-directly, asserting `ok === false` and the exact refusal text. This is now\ndocs/validation/ACC-501-MUTATION-PROOFS.md:644:reachable through the public validator for the first time, closing the gap\ndocs/validation/ACC-501-MUTATION-PROOFS.md-645-round 18b identified.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-646-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-647-### Mutation proof — deleting the call site fails the new test\ndocs/validation/ACC-501-MUTATION-PROOFS.md-648-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-649-Checksum before mutation: `4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc`.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-650-\ndocs/validation/ACC-501-MUTATION-PROOFS.md:651:**Mutation** — removed the validator's call to `oneHopSchemaVersionCheckReason`\ndocs/validation/ACC-501-MUTATION-PROOFS.md:652:(the four lines pushing `oneHopReason` into `reasons`), leaving everything\ndocs/validation/ACC-501-MUTATION-PROOFS.md-653-else, including `manifestToSchemaVersion`'s extraction, intact.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-654-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-655-**Result**: `223 tests / 222 pass / 1 fail`. The new validator-level test\ndocs/validation/ACC-501-MUTATION-PROOFS.md-656-failed exactly as expected:\ndocs/validation/ACC-501-MUTATION-PROOFS.md-657-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-658-```\ndocs/validation/ACC-501-MUTATION-PROOFS.md:659:✖ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\ndocs/validation/ACC-501-MUTATION-PROOFS.md-660-  the compiled schema — validator wiring, not just the helper\ndocs/validation/ACC-501-MUTATION-PROOFS.md-661-  AssertionError: Expected values to be strictly equal: true !== false\ndocs/validation/ACC-501-MUTATION-PROOFS.md-662-```\ndocs/validation/ACC-501-MUTATION-PROOFS.md-663-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-664-`round 15 — the v12→v13 schema bump is a pinned migration, not a skipped\ndocs/validation/ACC-501-MUTATION-PROOFS.md-665-check` also failed, matching round 18's earlier combined-mutation proof —\ndocs/validation/ACC-501-MUTATION-PROOFS.md:666:consistent evidence that this is the same load-bearing check, now reachable\ndocs/validation/ACC-501-MUTATION-PROOFS.md-667-without mutating a module constant.\ndocs/validation/ACC-501-MUTATION-PROOFS.md-668-\ndocs/validation/ACC-501-MUTATION-PROOFS.md-669-**Restoration**: re-added the four-line call site. Checksum after restore:\ndocs/validation/ACC-501-MUTATION-PROOFS.md-670-`4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc` — exact\n\nexec\n/bin/zsh -lc 'rg -n \"v12|v13|V12|V13|migration|schemaVersion|one-hop|oneHop|toSchemaVersion|manifest\" src/services/correlation/bench-correlation-baseline.ts' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n165:  schemaVersion: number;\n240: * The v12 → v13 transition: ADDITIVE ONLY.\n246: * statement of this migration is that EVERY graded number and every corpus\n255:export interface CorrelationBenchMigrationV12ToV13 {\n257:  toSchemaVersion: 13;\n263:   * `previousReportDigest` names the reviewed v12 report but does not BIND the\n265:   * report and the supplied previous baseline, left this manifest's report\n274:   * `tolerances` sits in `V13_MAY_MOVE` because an ordinary (non-migration)\n276:   * supplied `previousV12.tolerances` flow into `checkMigrationSharedGates`\n286:export const CORRELATION_BENCH_V12_TO_V13_MIGRATION: CorrelationBenchMigrationV12ToV13 = {\n288:  toSchemaVersion: 13,\n313: * Fields the migration allows to differ. Everything else in the baseline has to\n317:const V13_MAY_MOVE: ReadonlySet<string> = new Set([\n318:  'schemaVersion', 'seededAt', 'note', 'reportDigest', 'witnessed', 'tolerances',\n322: * Digest over exactly the fields the migration forbids to move.\n324: * Pinned in the manifest, so the previous baseline handed to the validator has\n331:    if (V13_MAY_MOVE.has(key)) continue;\n341: * independently of `V13_MAY_MOVE` exempting it from `benchBaselinePayloadDigest`.\n350: * Pure one-hop schema check, extracted so it is directly testable in\n351: * isolation from the rest of validateCorrelationBenchV12ToV13Migration.\n352: * The validator's manifest-identity guard excludes toSchemaVersion from its\n353: * blanket comparison specifically so a caller-supplied manifest differing\n355: * Returns the refusal reason string, or null when the manifest still\n358:export function oneHopSchemaVersionCheckReason(\n359:  manifestToSchemaVersion: number,\n362:  if (manifestToSchemaVersion !== compiledSchemaVersion) {\n364:      `v12→v13 migration manifest targets schemaVersion ${String(manifestToSchemaVersion)}, but the ` +\n365:      `compiled gate is schemaVersion ${String(compiledSchemaVersion)} — this migration is ` +\n366:      'no longer the one-hop path to the live schema'\n372:export function validateCorrelationBenchV12ToV13Migration(\n374:  previousV12: CorrelationBenchBaseline,\n375:  manifest: CorrelationBenchMigrationV12ToV13,\n378:  // toSchemaVersion is deliberately excluded from this identity comparison —\n379:  // it gets its OWN check below via oneHopSchemaVersionCheckReason(), so a\n380:  // caller-supplied manifest that matches the pinned migration in every\n382:  // Round 18: Codex found that folding toSchemaVersion into this blanket\n386:  const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n387:  const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n388:  delete pinnedRest.toSchemaVersion;\n389:  if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n390:    reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n393:  const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n394:  if (oneHopReason !== null) {\n395:    reasons.push(oneHopReason);\n397:  if (previousV12.schemaVersion !== 12) {\n398:    reasons.push(`v12→v13 migration source is schemaVersion ${previousV12.schemaVersion}, not 12`);\n400:  if (previousV12.corpusDigest !== manifest.previousCorpusDigest) {\n401:    reasons.push('v12→v13 migration source is not the reviewed v12 corpus');\n403:  if (previousV12.reportDigest !== manifest.previousReportDigest) {\n404:    reasons.push('v12→v13 migration source is not the reviewed v12 report');\n406:  if (benchBaselinePayloadDigest(previousV12) !== manifest.previousPayloadDigest) {\n408:      'v12→v13 migration source does not carry the reviewed v12 payload — the baseline being ' +\n413:  if (benchTolerancesDigest(previousV12.tolerances) !== manifest.previousTolerancesDigest) {\n415:      'v12→v13 migration source does not carry the reviewed v12 tolerances — tolerances are ' +\n421:  if (report.corpusDigest !== manifest.previousCorpusDigest) {\n423:      'v13 corpus digest moved — this migration is additive-only and permits no corpus edit',\n430:    { note: previousV12.note, tolerances: previousV12.tolerances },\n431:    previousV12.seededAt,\n433:  const before = previousV12 as unknown as Record<string, unknown>;\n439:    if (V13_MAY_MOVE.has(key)) continue;\n442:        `v12→v13 is additive-only, but ${key} moved from `\n448:    reasons.push('v12→v13 report digest did not move — nothing was actually added');\n454:export interface CorrelationBenchMigrationV11ToV12 {\n456:  toSchemaVersion: 12;\n469:  schemaVersion: 11,\n522:const CORRELATION_BENCH_V12_CORPUS_DIGEST = '1a0377333099795fe0ccff8dc5a7a2cf';\n529:export const CORRELATION_BENCH_V11_TO_V12_MIGRATION: CorrelationBenchMigrationV11ToV12 = {\n531:  toSchemaVersion: 12,\n548:export function validateCorrelationBenchV11ToV12Migration(\n551:  manifest: CorrelationBenchMigrationV11ToV12,\n555:  checkV11MigrationManifest(reasons, report, manifest);\n556:  checkV12MigrationEvidence(reasons, report, previousV11);\n565:    ['schemaVersion', CORRELATION_BENCH_V11_ANCHORS.schemaVersion, previous.schemaVersion],\n584:        `v11 migration source ${field} is not the pinned reviewed anchor ` +\n594:  manifest: CorrelationBenchMigrationV11ToV12,\n596:  if (JSON.stringify(manifest) !== JSON.stringify(CORRELATION_BENCH_V11_TO_V12_MIGRATION)) {\n597:    reasons.push('v11→v12 migration manifest does not match the pinned S9-only transition');\n603:    reasons.push('v12 streamDigests does not name exactly the ten reviewed streams');\n605:  for (const [id, digest] of Object.entries(manifest.unchangedStreamDigests)) {\n607:      reasons.push(`v12 changed non-S9 stream ${id}; migration permits only inhibitory-pair`);\n610:  if (manifest.changedStream.beforeDigest === manifest.changedStream.afterDigest) {\n611:    reasons.push('v11→v12 changed stream digest did not change');\n613:  if (manifest.changedStream.reason.trim() === '') {\n614:    reasons.push('v11→v12 changed stream requires a nonempty reason');\n616:  if (report.streamDigests['inhibitory-pair'] !== manifest.changedStream.afterDigest) {\n617:    reasons.push('v12 inhibitory-pair digest does not match the reviewed afterDigest');\n619:  if (report.corpusDigest !== CORRELATION_BENCH_V12_CORPUS_DIGEST) {\n620:    reasons.push('v12 corpus digest does not match the reviewed S9-only corpus transition');\n624:function checkV12MigrationEvidence(\n630:    reasons.push('v12 migration replayMode must be offline-whole-corpus');\n636:    reasons.push('v12 migration must recover the one planted inhibitory coupling');\n639:    reasons.push('v12 migration cannot seed null inhibitory precision from no discoveries');\n641:    reasons.push('v12 migration requires inhibitory precision 1 with zero false positives');\n651:  // does not demand fields that the migration source never carried.\n672:    reasons.push('v12 multiple-testing family is invalid or not exactly reproducible');\n1621:  if (baseline.schemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION) {\n1623:      `baseline schemaVersion is ${String(baseline.schemaVersion)}, expected ` +\n3549:    schemaVersion: CORRELATION_BENCH_SCHEMA_VERSION,\n\nexec\n/bin/zsh -lc 'rg -n \"[0-9]+ (tests? / )?[0-9]+ pass / [0-9]+ fail|[0-9]+ pass / [0-9]+ fail|[0-9]+-test|[0-9]+ tests\" docs/validation/ACC-501-MUTATION-PROOFS.md' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n9:The focused suite was green before mutation at `182 pass / 0 fail` for the\n10:round-14 mutations below, and grew to `216 pass / 0 fail` (full\n11:`bench-correlation.test.mts`) plus `409 pass / 0 fail` (`npm run\n27:- Targeted result: `0 pass / 1 fail`.\n40:- Targeted result: `0 pass / 1 fail`.\n53:- Targeted result: `0 pass / 1 fail`.\n67:- Targeted result: `0 pass / 1 fail`.\n81:- Targeted result: `0 pass / 1 fail`.\n95:- Targeted result: `0 pass / 1 fail`.\n109:- Targeted result: `0 pass / 1 fail`.\n177:  `214 pass / 1 fail`.\n179:  not the reviewed one` failed; no other test in the 215-test file was\n362:  `219 pass / 1 fail`.\n370:  220-test file was affected.\n374:Full suite after all four round-16 fixes: `220 pass / 0 fail`\n375:(`bench-correlation.test.mts`), `413 pass / 0 fail` (`npm run\n429:- Targeted result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n437:  mutated constant. No other test in the 220-test file was affected.\n461:- Targeted result: `bench-correlation.test.mts` → `219 pass / 1 fail`.\n466:  other test in the 220-test file was affected, confirming this is the sole\n471:Full suite after all three round-17 fixes: `220 pass / 0 fail`\n483:> guard; all three relevant tests remained green (3 pass / 0 fail).\n514:- Result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n532:- Result: `bench-correlation.test.mts` → `218 pass / 2 fail`.\n550:  `220 pass / 0 fail`.\n574:Full suite after this change: `222 pass / 0 fail`. `npm run typecheck:all` →\n655:**Result**: `223 tests / 222 pass / 1 fail`. The new validator-level test\n676:- `bench-correlation.test.mts` — `223 pass / 0 fail`\n679:- `npm run test:correlation` — `416 pass / 0 fail`\n\nexec\n/bin/zsh -lc \"nl -ba src/services/correlation/bench-correlation-baseline.ts | sed -n '130,470p'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n   130\t  couplingRecallDrop: 0.1,\n   131\t  pairPrecisionDrop: 0.1,\n   132\t  pairRecallDrop: 0.1,\n   133\t  edgeEvidenceSeparationDrop: 3,\n   134\t  meanTruePairConfidenceDrop: 0.1,\n   135\t  falseEdgeGrowth: 3,\n   136\t  causalLearnedRuleShrink: 1,\n   137\t  learnedRuleFalsePositiveGrowth: 3,\n   138\t  learnedRulePairGrowth: 25,\n   139\t  causalLearnedRulePairShrinkRatio: 0.5,\n   140\t  enginePairShrink: 2,\n   141\t  minedEdgeCountShrink: 25,\n   142\t};\n   143\t\n   144\t/**\n   145\t * The baseline shape this module knows how to compare. A baseline from an older\n   146\t * schema is missing fields that now feed gates, and every missing field reads as\n   147\t * `undefined` — which the operand checks catch one at a time, with a pile of\n   148\t * confusing reasons, only for the fields that happen to be gated. Pin the\n   149\t * version and say so once, plainly.\n   150\t */\n   151\texport const CORRELATION_BENCH_SCHEMA_VERSION = 13;\n   152\t\n   153\t/**\n   154\t * `goldenCorpusDigest()` emits exactly 32 lowercase hex characters. Comparing\n   155\t * the two digests with a bare `!==` treats \"both sides absent\" as agreement:\n   156\t * delete the field from the baseline JSON and from the report and\n   157\t * `undefined === undefined` passes corpus identity, which is the one check that\n   158\t * makes every number below comparable at all. Both operands must LOOK like a\n   159\t * digest before their equality means anything.\n   160\t */\n   161\tconst DIGEST_PATTERN = /^[\\da-f]{32}$/;\n   162\t\n   163\texport interface CorrelationBenchBaseline {\n   164\t  /** Bumped only when the baseline's own shape changes. */\n   165\t  schemaVersion: number;\n   166\t  /** ISO date the baseline was seeded — informational, never compared. */\n   167\t  seededAt: string;\n   168\t  /** Human note on what the numbers meant when they were frozen. */\n   169\t  note: string;\n   170\t  replayMode: 'offline-whole-corpus';\n   171\t\n   172\t  // corpus identity — exact equality\n   173\t  streamCount: number;\n   174\t  observationCount: number;\n   175\t  plantedCausalCount: number;\n   176\t  truePairUniverse: number;\n   177\t  corpusDigest: string;\n   178\t  streamDigests: Record<string, string>;\n   179\t  /**\n   180\t   * Digest of the ENTIRE report the last reviewed run produced — see\n   181\t   * `benchReportDigest`. This is the only pin in the file that a change to\n   182\t   * `bench-correlation.ts` cannot move along with itself.\n   183\t   */\n   184\t  reportDigest: string;\n   185\t  /** Every built-in rule id in the graded pass, sorted — pinned by set equality. */\n   186\t  builtInRuleIds: string[];\n   187\t  /**\n   188\t   * The built-in rules that actually EMIT over this corpus, sorted — also\n   189\t   * pinned by set equality. Inventory pins which rules ship; this pins which of\n   190\t   * them the graded pass exercises, so a rule going dark is a benchmark\n   191\t   * failure rather than a rate too small to trip a tolerance.\n   192\t   */\n   193\t  ruleCoverage: string[];\n   194\t  multipleTestingFamily: MultipleTestingFamily;\n   195\t\n   196\t  // graded metrics\n   197\t  couplingPrecision: number;\n   198\t  couplingRecall: number;\n   199\t  minedEdgeCount: number;\n   200\t  significantEdgeCount: number;\n   201\t  confoundedFalsePositives: number;\n   202\t  mediatedFalsePositives: number;\n   203\t  independentFalsePositives: number;\n   204\t  inhibitoryEdgesReported: number;\n   205\t  unplantedFalsePositives: number;\n   206\t  falseEdgeCount: number;\n   207\t  /**\n   208\t   * `null` when the run this baseline was seeded from reported NO false edges —\n   209\t   * the perfect miner ACC-502..504 are aiming at, which has nothing to separate\n   210\t   * from. Requiring a number here made that outcome un-seedable: it passes the\n   211\t   * gate as an improvement, and then cannot become the next baseline.\n   212\t   */\n   213\t  edgeEvidenceSeparation: number | null;\n   214\t  fixedCandidateEvidenceSeparation: number;\n   215\t  plantedInhibitoryCount: number;\n   216\t  inhibitoryTruePositiveCount: number;\n   217\t  inhibitoryFalsePositiveCount: number;\n   218\t  inhibitoryPrecision: number | null;\n   219\t  inhibitoryRecall: number;\n   220\t  learnedRuleCount: number;\n   221\t  learnedRuleFalsePositives: number;\n   222\t  causalLearnedRuleCount: number;\n   223\t  enginePairCount: number;\n   224\t  distinctEnginePairCount: number;\n   225\t  pairPrecision: number;\n   226\t  pairRecall: number;\n   227\t  decoyPairsEmitted: number;\n   228\t  meanTruePairConfidence: number;\n   229\t  learnedRulePairCount: number;\n   230\t  causalLearnedRulePairCount: number;\n   231\t  minCausalLearnedRulePairCount: number;\n   232\t\n   233\t  /** Advertised-but-ungated measurements + per-ledger digests, pinned by value. */\n   234\t  witnessed: CorrelationBenchWitnessed;\n   235\t\n   236\t  tolerances: CorrelationBenchTolerances;\n   237\t}\n   238\t\n   239\t/**\n   240\t * The v12 → v13 transition: ADDITIVE ONLY.\n   241\t *\n   242\t * Round 15 adds observations to the report (independent probe re-execution,\n   243\t * reversed positives, per-branch disjunct positives, the real `pairToEdge()`\n   244\t * projection, a learned-rule retirement probe, `detectedAt` and the confidence\n   245\t * breakdown). It changes no corpus and no engine behaviour, so the honest\n   246\t * statement of this migration is that EVERY graded number and every corpus\n   247\t * digest is byte-identical and only `reportDigest` moves — because the report\n   248\t * now carries fields it did not carry before.\n   249\t *\n   250\t * That is a stronger claim than the ordinary one-sided tolerances make, and it\n   251\t * is the only claim that fits: a schema bump short-circuits the normal\n   252\t * comparator before its tolerance loop runs, so without this the re-seed would\n   253\t * be waved through by a check that never executed.\n   254\t */\n   255\texport interface CorrelationBenchMigrationV12ToV13 {\n   256\t  fromSchemaVersion: 12;\n   257\t  toSchemaVersion: 13;\n   258\t  previousCorpusDigest: string;\n   259\t  previousReportDigest: string;\n   260\t  /**\n   261\t   * Digest over the previous baseline's own carried-over payload.\n   262\t   *\n   263\t   * `previousReportDigest` names the reviewed v12 report but does not BIND the\n   264\t   * baseline handed in to it: the reviewer moved `minedEdgeCount` in both the\n   265\t   * report and the supplied previous baseline, left this manifest's report\n   266\t   * digest alone, and the field-by-field loop compared the moved value to the\n   267\t   * moved value and returned ok. The pin has to cover the payload the loop\n   268\t   * actually reads, not a sibling string.\n   269\t   */\n   270\t  previousPayloadDigest: string;\n   271\t  /**\n   272\t   * Digest over the previous baseline's `tolerances` block specifically.\n   273\t   *\n   274\t   * `tolerances` sits in `V13_MAY_MOVE` because an ordinary (non-migration)\n   275\t   * reseed legitimately edits it — but that same exemption let a caller-\n   276\t   * supplied `previousV12.tolerances` flow into `checkMigrationSharedGates`\n   277\t   * ungated: widening `couplingPrecisionDrop` from 0.02 to 0.1 still returned\n   278\t   * `{ok:true}` because nothing pinned tolerances to the reviewed values.\n   279\t   * This digest authenticates the tolerance block used for grading\n   280\t   * independently of whether tolerances are allowed to move at reseed time.\n   281\t   */\n   282\t  previousTolerancesDigest: string;\n   283\t  reason: string;\n   284\t}\n   285\t\n   286\texport const CORRELATION_BENCH_V12_TO_V13_MIGRATION: CorrelationBenchMigrationV12ToV13 = {\n   287\t  fromSchemaVersion: 12,\n   288\t  toSchemaVersion: 13,\n   289\t  previousCorpusDigest: '1a0377333099795fe0ccff8dc5a7a2cf',\n   290\t  previousReportDigest: '354a7790613214a893698b1882eda0ae',\n   291\t  previousPayloadDigest: '129ab5924c521a1897a907059de7ffac',\n   292\t  previousTolerancesDigest: benchTolerancesDigest({\n   293\t    couplingPrecisionDrop: 0.02,\n   294\t    couplingRecallDrop: 0,\n   295\t    pairPrecisionDrop: 0,\n   296\t    pairRecallDrop: 0,\n   297\t    edgeEvidenceSeparationDrop: 1,\n   298\t    meanTruePairConfidenceDrop: 0.05,\n   299\t    falseEdgeGrowth: 0,\n   300\t    causalLearnedRuleShrink: 0,\n   301\t    learnedRuleFalsePositiveGrowth: 0,\n   302\t    learnedRulePairGrowth: 5,\n   303\t    causalLearnedRulePairShrinkRatio: 0.5,\n   304\t    enginePairShrink: 0,\n   305\t    minedEdgeCountShrink: 10,\n   306\t  }),\n   307\t  reason:\n   308\t    'ACC-501 round 15 adds probe/emission observations to the report. No corpus edit, '\n   309\t    + 'no engine change, no graded metric movement.',\n   310\t};\n   311\t\n   312\t/**\n   313\t * Fields the migration allows to differ. Everything else in the baseline has to\n   314\t * match the previous one exactly — an allowlist, so a field added later is\n   315\t * refused by default rather than silently exempted.\n   316\t */\n   317\tconst V13_MAY_MOVE: ReadonlySet<string> = new Set([\n   318\t  'schemaVersion', 'seededAt', 'note', 'reportDigest', 'witnessed', 'tolerances',\n   319\t]);\n   320\t\n   321\t/**\n   322\t * Digest over exactly the fields the migration forbids to move.\n   323\t *\n   324\t * Pinned in the manifest, so the previous baseline handed to the validator has\n   325\t * to BE the reviewed one rather than merely claim its report digest.\n   326\t */\n   327\texport function benchBaselinePayloadDigest(baseline: CorrelationBenchBaseline): string {\n   328\t  const src = baseline as unknown as Record<string, unknown>;\n   329\t  const payload: Record<string, unknown> = {};\n   330\t  for (const key of Object.keys(src).sort(compareStrings)) {\n   331\t    if (V13_MAY_MOVE.has(key)) continue;\n   332\t    payload[key] = src[key];\n   333\t  }\n   334\t  const records: string[] = [];\n   335\t  canonicalRecords(payload, '', records);\n   336\t  return digestRecords(records);\n   337\t}\n   338\t\n   339\t/**\n   340\t * Digest over a `tolerances` block alone, so it can be authenticated\n   341\t * independently of `V13_MAY_MOVE` exempting it from `benchBaselinePayloadDigest`.\n   342\t */\n   343\texport function benchTolerancesDigest(tolerances: CorrelationBenchBaseline['tolerances']): string {\n   344\t  const records: string[] = [];\n   345\t  canonicalRecords(tolerances, '', records);\n   346\t  return digestRecords(records);\n   347\t}\n   348\t\n   349\t/**\n   350\t * Pure one-hop schema check, extracted so it is directly testable in\n   351\t * isolation from the rest of validateCorrelationBenchV12ToV13Migration.\n   352\t * The validator's manifest-identity guard excludes toSchemaVersion from its\n   353\t * blanket comparison specifically so a caller-supplied manifest differing\n   354\t * only in this field reaches this check instead of being swallowed there.\n   355\t * Returns the refusal reason string, or null when the manifest still\n   356\t * targets the compiled schema.\n   357\t */\n   358\texport function oneHopSchemaVersionCheckReason(\n   359\t  manifestToSchemaVersion: number,\n   360\t  compiledSchemaVersion: number,\n   361\t): string | null {\n   362\t  if (manifestToSchemaVersion !== compiledSchemaVersion) {\n   363\t    return (\n   364\t      `v12→v13 migration manifest targets schemaVersion ${String(manifestToSchemaVersion)}, but the ` +\n   365\t      `compiled gate is schemaVersion ${String(compiledSchemaVersion)} — this migration is ` +\n   366\t      'no longer the one-hop path to the live schema'\n   367\t    );\n   368\t  }\n   369\t  return null;\n   370\t}\n   371\t\n   372\texport function validateCorrelationBenchV12ToV13Migration(\n   373\t  report: CorrelationBenchReport,\n   374\t  previousV12: CorrelationBenchBaseline,\n   375\t  manifest: CorrelationBenchMigrationV12ToV13,\n   376\t): CorrelationBenchComparison {\n   377\t  const reasons: string[] = [];\n   378\t  // toSchemaVersion is deliberately excluded from this identity comparison —\n   379\t  // it gets its OWN check below via oneHopSchemaVersionCheckReason(), so a\n   380\t  // caller-supplied manifest that matches the pinned migration in every\n   381\t  // OTHER field still reaches that check instead of being swallowed here.\n   382\t  // Round 18: Codex found that folding toSchemaVersion into this blanket\n   383\t  // identity guard made the schema check unreachable through the public\n   384\t  // validator — the only way to exercise it was to mutate the pinned module\n   385\t  // constant, which can't be a permanent regression test.\n   386\t  const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n   387\t  const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n   388\t  delete pinnedRest.toSchemaVersion;\n   389\t  if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n   390\t    reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n   391\t    return { ok: false, reasons };\n   392\t  }\n   393\t  const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n   394\t  if (oneHopReason !== null) {\n   395\t    reasons.push(oneHopReason);\n   396\t  }\n   397\t  if (previousV12.schemaVersion !== 12) {\n   398\t    reasons.push(`v12→v13 migration source is schemaVersion ${previousV12.schemaVersion}, not 12`);\n   399\t  }\n   400\t  if (previousV12.corpusDigest !== manifest.previousCorpusDigest) {\n   401\t    reasons.push('v12→v13 migration source is not the reviewed v12 corpus');\n   402\t  }\n   403\t  if (previousV12.reportDigest !== manifest.previousReportDigest) {\n   404\t    reasons.push('v12→v13 migration source is not the reviewed v12 report');\n   405\t  }\n   406\t  if (benchBaselinePayloadDigest(previousV12) !== manifest.previousPayloadDigest) {\n   407\t    reasons.push(\n   408\t      'v12→v13 migration source does not carry the reviewed v12 payload — the baseline being ' +\n   409\t      'migrated FROM is not the one that was reviewed, so every field-by-field comparison below ' +\n   410\t      'is against an unvouched-for number',\n   411\t    );\n   412\t  }\n   413\t  if (benchTolerancesDigest(previousV12.tolerances) !== manifest.previousTolerancesDigest) {\n   414\t    reasons.push(\n   415\t      'v12→v13 migration source does not carry the reviewed v12 tolerances — tolerances are ' +\n   416\t      'exempt from the payload digest so ordinary reseeds can edit them, but the shared gates ' +\n   417\t      'grade the live report against whatever tolerances the caller supplies, so an unpinned ' +\n   418\t      'tolerance here would grade leniently against a value nobody reviewed',\n   419\t    );\n   420\t  }\n   421\t  if (report.corpusDigest !== manifest.previousCorpusDigest) {\n   422\t    reasons.push(\n   423\t      'v13 corpus digest moved — this migration is additive-only and permits no corpus edit',\n   424\t    );\n   425\t  }\n   426\t  // Seed the candidate from the live report and demand exact equality field by\n   427\t  // field. Anything that moved is a behaviour change wearing a schema bump.\n   428\t  const candidate = seedCorrelationBenchBaseline(\n   429\t    report,\n   430\t    { note: previousV12.note, tolerances: previousV12.tolerances },\n   431\t    previousV12.seededAt,\n   432\t  ) as unknown as Record<string, unknown>;\n   433\t  const before = previousV12 as unknown as Record<string, unknown>;\n   434\t  // Union, not the candidate's keys: iterating one side makes a field DROPPED\n   435\t  // by the bump invisible, and a dropped measurement is the same defect as a\n   436\t  // moved one.\n   437\t  const keys = [...new Set([...Object.keys(candidate), ...Object.keys(before)])].sort(compareStrings);\n   438\t  for (const key of keys) {\n   439\t    if (V13_MAY_MOVE.has(key)) continue;\n   440\t    if (JSON.stringify(candidate[key]) !== JSON.stringify(before[key])) {\n   441\t      reasons.push(\n   442\t        `v12→v13 is additive-only, but ${key} moved from `\n   443\t        + `${JSON.stringify(before[key])} to ${JSON.stringify(candidate[key])}`,\n   444\t      );\n   445\t    }\n   446\t  }\n   447\t  if (candidate.reportDigest === before.reportDigest) {\n   448\t    reasons.push('v12→v13 report digest did not move — nothing was actually added');\n   449\t  }\n   450\t  checkReportConsistency(reasons, report);\n   451\t  return { ok: reasons.length === 0, reasons };\n   452\t}\n   453\t\n   454\texport interface CorrelationBenchMigrationV11ToV12 {\n   455\t  fromSchemaVersion: 11;\n   456\t  toSchemaVersion: 12;\n   457\t  previousCorpusDigest: string;\n   458\t  previousReportDigest: string;\n   459\t  changedStream: {\n   460\t    id: 'inhibitory-pair';\n   461\t    beforeDigest: string;\n   462\t    afterDigest: string;\n   463\t    reason: string;\n   464\t  };\n   465\t  unchangedStreamDigests: Record<string, string>;\n   466\t}\n   467\t\n   468\texport const CORRELATION_BENCH_V11_ANCHORS = {\n   469\t  schemaVersion: 11,\n   470\t  streamCount: 10,\n\nexec\n/bin/zsh -lc \"nl -ba docs/validation/ACC-501-MUTATION-PROOFS.md | sed -n '1,240p'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n     1\t# ACC-501 mutation proofs\n     2\t\n     3\tDate: 2026-08-04\n     4\t\n     5\tReviewed implementation commit: `870b08f6` (round 14) plus the round-15 working\n     6\ttree on `claude/acc-501-round-14` — round 15 addressed a Codex REQUEST_CHANGES\n     7\treview of PR #1625.\n     8\t\n     9\tThe focused suite was green before mutation at `182 pass / 0 fail` for the\n    10\tround-14 mutations below, and grew to `216 pass / 0 fail` (full\n    11\t`bench-correlation.test.mts`) plus `409 pass / 0 fail` (`npm run\n    12\ttest:correlation`) by round 15. All mutations began from a `git status\n    13\t--short` containing only this session's legitimate round-15 diff (no\n    14\tunrelated changes); each file's SHA-256 was recorded before and after every\n    15\tmutation to confirm exact restoration, and each edit was confirmed with `git\n    16\tdiff`/`git status --short` before its targeted command ran.\n    17\t\n    18\t## Previous-baseline quality pin\n    19\t\n    20\t- File: `src/services/correlation/bench-correlation-baseline.ts`\n    21\t- Before checksum: `09dc7554529763a1cbe911b98aff597fc3d394d47ae5d9ea6ce6d83b792fd38e`\n    22\t- Mutation: replaced the private previous-baseline comparison view with a\n    23\t  freshly seeded baseline built entirely from the live report. This recreates\n    24\t  the original compare-the-candidate-to-itself laundering path.\n    25\t- Confirmed diff: `comparisonBaseline` changed from `{ ...previous, ...anchors }`\n    26\t  to `seedCorrelationBenchBaseline(report, ...)`.\n    27\t- Targeted result: `0 pass / 1 fail`.\n    28\t- Failing assertion: `true !== false` in “refuses a candidate that regresses\n    29\t  beyond the previous one-sided tolerance”.\n    30\t- Restored checksum: `09dc7554529763a1cbe911b98aff597fc3d394d47ae5d9ea6ce6d83b792fd38e`.\n    31\t\n    32\t## CLI refusal guard\n    33\t\n    34\t- File: `scripts/correlation-benchmark.mts`\n    35\t- Before checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`\n    36\t- Mutation: changed `if (!comparison.ok)` to\n    37\t  `if (false && !comparison.ok)`, bypassing the transition verdict.\n    38\t- Confirmed diff: the CLI refusal branch was disabled while the comparator call\n    39\t  remained present.\n    40\t- Targeted result: `0 pass / 1 fail`.\n    41\t- Failing assertion: the child process exited `0` instead of `1` and emitted a\n    42\t  seeded candidate for a known regression.\n    43\t- Restored checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`.\n    44\t\n    45\t## Base-branch CI comparison\n    46\t\n    47\t- File: `.github/workflows/smoke.yml`\n    48\t- Before checksum: `e9f7ce86e207bc58c1bb83bddb0f1fbf53b36da123e7750e65d2cd6b2173aedc`\n    49\t- Mutation: removed `--previous-baseline \"$PREVIOUS_BASELINE\"` from the PR and\n    50\t  merge-queue guard, causing the command to compare against the candidate\n    51\t  branch's tracked baseline instead of the base commit.\n    52\t- Confirmed diff: the extracted base baseline remained unused by the command.\n    53\t- Targeted result: `0 pass / 1 fail`.\n    54\t- Failing assertion: the workflow no longer matched the required\n    55\t  `--seed --previous-baseline \"$PREVIOUS_BASELINE\"` invocation.\n    56\t- Restored checksum: `e9f7ce86e207bc58c1bb83bddb0f1fbf53b36da123e7750e65d2cd6b2173aedc`.\n    57\t\n    58\t## Safe candidate destination\n    59\t\n    60\t- File: `scripts/correlation-benchmark.mts`\n    61\t- Before checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`\n    62\t- Mutation: restored the unsafe instruction\n    63\t  `npm run bench:correlation -- --seed > ${rel}`, which lets shell redirection\n    64\t  truncate the tracked baseline before the process reads it.\n    65\t- Confirmed diff: the failure guidance changed from `CANDIDATE_PATH` back to\n    66\t  the tracked relative baseline path.\n    67\t- Targeted result: `0 pass / 1 fail`.\n    68\t- Failing assertion: “operator guidance must never redirect stdout onto the\n    69\t  tracked baseline before validation”.\n    70\t- Restored checksum: `30f83fd2642cb48f1a02da85647dd1b0d741ea20b42a69db518f707a4c53c3a9`.\n    71\t\n    72\t## Covered-rule liveness floor\n    73\t\n    74\t- File: `src/services/correlation/bench-correlation-baseline.ts`\n    75\t- Before checksum: `cb714f0f7b91f706b378221a02c486410c054b4c94fc2c16b3ba0d20ba2b4ffe`\n    76\t- Mutation: removed `checkReseedRuleCoverage(previous, report)` from reseed\n    77\t  preflight, recreating the path where a previously firing built-in rule could\n    78\t  disappear while aggregate metrics stayed healthy.\n    79\t- Confirmed diff: the preflight array no longer invoked the coverage subset\n    80\t  check.\n    81\t- Targeted result: `0 pass / 1 fail`.\n    82\t- Failing assertion: `true !== false` in “refuses to reseed when a previously\n    83\t  covered built-in rule goes dark”.\n    84\t- Restored checksum: `cb714f0f7b91f706b378221a02c486410c054b4c94fc2c16b3ba0d20ba2b4ffe`.\n    85\t\n    86\t## Initial baseline CI path\n    87\t\n    88\t- File: `.github/workflows/smoke.yml`\n    89\t- Before checksum: `a2f7d64dbbe6cdca5608fdbac4f23bb94c70f17c3a3989d7cdf4432b62f98efc`\n    90\t- Mutation: removed the missing-base `git cat-file` guard, recreating the path\n    91\t  where the first baseline addition attempts to extract a file that does not\n    92\t  exist on the base branch.\n    93\t- Confirmed diff: the workflow proceeded directly from `BASELINE_PATH` to the\n    94\t  baseline-change check without testing whether the base object exists.\n    95\t- Targeted result: `0 pass / 1 fail`.\n    96\t- Failing assertion: “the first baseline addition has no base-branch baseline\n    97\t  to compare against”.\n    98\t- Restored checksum: `a2f7d64dbbe6cdca5608fdbac4f23bb94c70f17c3a3989d7cdf4432b62f98efc`.\n    99\t\n   100\t## Unavailable base commit fails closed\n   101\t\n   102\t- File: `.github/workflows/smoke.yml`\n   103\t- Before checksum: `852aa154f9cd1eb8027abdc2d70f0ce4b9af0cef95bbf21fa209145e138353c5`\n   104\t- Mutation: removed the independent `git cat-file` validation for the base\n   105\t  commit, recreating the path where an invalid or unavailable SHA is mistaken\n   106\t  for a legitimate initial baseline.\n   107\t- Confirmed diff: the workflow again proceeded directly to the path lookup,\n   108\t  whose failure exits successfully.\n   109\t- Targeted result: `0 pass / 1 fail`.\n   110\t- Failing assertion: “an unavailable base commit must fail closed before\n   111\t  checking for an initial baseline”.\n   112\t- Restored checksum: `852aa154f9cd1eb8027abdc2d70f0ce4b9af0cef95bbf21fa209145e138353c5`.\n   113\t\n   114\t## Round 15 — learned-rule resync self-attestation\n   115\t\n   116\tCodex's review of PR #1625 found `checkLearnedRuleResync` validated only the\n   117\treport's own internal consistency, never re-deriving `afterRetirement`\n   118\tagainst a live engine — so a producer that started computing it\n   119\tarithmetically (`installed.filter(id => id !== retiredId)`) instead of\n   120\tcalling `engine.getRules()` would satisfy every check while the underlying\n   121\tremoval path went unmeasured. The fix added `verifyResyncProbe()`\n   122\t(`__bench__/rule-probe-verify.ts`), which constructs its own fresh\n   123\t`CorrelateEngine`, runs the same install→retire→resync sequence independently\n   124\tof the producer, and the gate now demands the two agree.\n   125\t\n   126\t- Files: `src/services/correlation/bench-correlation.ts` (before\n   127\t  `62b008e868e5e7e0e73ee59b645670828149181727f510a1881e217c8a03020f`),\n   128\t  `src/services/correlation/learned-rules.ts` (before\n   129\t  `5f96ebbd460397ef75aef59ec50023d4b4e5d6a3420aed5975b6943a2bb38546`).\n   130\t- Mutation 1 alone: changed `probeLearnedRuleResync`'s `afterRetirement` from\n   131\t  `learnedIdsOf()` (a live `engine.getRules()` query) to\n   132\t  `installed.filter((id) => id !== retiredId)` (self-attested arithmetic).\n   133\t  Result: `npm run bench:correlation` still **PASSED** — the arithmetic\n   134\t  answer happens to equal the true engine state when the underlying removal\n   135\t  actually works, so this alone doesn't prove the removal path is exercised.\n   136\t  This matches the framing of the defect precisely: self-attestation isn't\n   137\t  wrong on the happy path, only silent on the path where the system it claims\n   138\t  to have queried actually breaks.\n   139\t- Mutation 2 (combined with mutation 1, recreating the historical bug the\n   140\t  file's own doc comment describes — \"Deleting `engine.unregisterRule` from\n   141\t  `syncLearnedRules()` left a retired coupling permanently installed... while\n   142\t  the function REPORTED it removed\"): removed the\n   143\t  `engine.unregisterRule(existing.id)` call from `syncLearnedRules` in\n   144\t  `learned-rules.ts`, so retirement stops actually removing the rule from the\n   145\t  engine while `removed += 1` still increments.\n   146\t- Targeted result: `npm run bench:correlation` → **FAIL**.\n   147\t- Failing assertion: `the learned-rule resync probe does not reproduce: the\n   148\t  report says {...\"afterRetirement\":[\"learned:cyber->finance\", ...4 ids]...}\n   149\t  and re-running the retirement against a live engine gives\n   150\t  {...\"afterRetirement\":[...5 ids, including the one reported retired]...}` —\n   151\t  `verifyResyncProbe()`'s independently-constructed engine still has the rule\n   152\t  installed; the producer's self-attested field claims it is gone.\n   153\t- Restored checksums: both files matched their pre-mutation SHA-256 exactly;\n   154\t  `git status --short` returned only the pre-existing round-15 diff.\n   155\t\n   156\t## Round 15 — v12→v13 migration self-consistent forgery\n   157\t\n   158\tCodex also found `validateCorrelationBenchV12ToV13Migration` reconstructed\n   159\tits own \"previous v12 baseline\" comparison object from data available at\n   160\tvalidation time, with nothing pinning that reconstruction to the actual\n   161\treviewed v12 payload — a forged previous baseline that moved a value in\n   162\tlockstep on both sides of the comparison would pass. The fix added\n   163\t`previousPayloadDigest`, computed once from the real `origin/main` v12\n   164\tbaseline and pinned in `CORRELATION_BENCH_V12_TO_V13_MIGRATION`, checked via\n   165\t`benchBaselinePayloadDigest()` against whatever baseline is actually being\n   166\tmigrated from.\n   167\t\n   168\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   169\t- Before checksum:\n   170\t  `dd078f9e8d6044b3eb1d7e454f8867a37032d6f27397d52b026d96a1675b7d6c`.\n   171\t- Mutation: short-circuited the payload-digest check with `if (false && ...)`,\n   172\t  disabling it while leaving `previousPayloadDigest` itself and every other\n   173\t  check in place.\n   174\t- Confirmed diff: the digest comparison branch became dead code.\n   175\t- Targeted result: `node --import tsx --test\n   176\t  src/services/correlation/__tests__/bench-correlation.test.mts` →\n   177\t  `214 pass / 1 fail`.\n   178\t- Failing assertion: exactly `refuses a previous baseline whose payload is\n   179\t  not the reviewed one` failed; no other test in the 215-test file was\n   180\t  affected, confirming the disabled check — not some other path — was what\n   181\t  that test exercises.\n   182\t- Restored checksum:\n   183\t  `dd078f9e8d6044b3eb1d7e454f8867a37032d6f27397d52b026d96a1675b7d6c`.\n   184\t\n   185\t## Round 15 — the \"broken tolerance\" re-seed test was not constructed\n   186\t\n   187\tCodex's third finding was a P2 on\n   188\t`refuses to re-seed over a broken tolerance without --force`: it ran\n   189\t`--seed` against the committed baseline (which passes today) and accepted\n   190\teither `Seeded from the current run` or `REFUSED` as a pass — a CLI that\n   191\talways printed one of those two strings regardless of what the comparator\n   192\tdid would satisfy the assertion. It never constructed a baseline the live\n   193\trun actually violates, so it proved the CLI can print those words, not that\n   194\t`--seed` compares anything.\n   195\t\n   196\tThe fix replaces it with two tests built on the CLI's own\n   197\t`--previous-baseline <path>` override:\n   198\t\n   199\t- **Refusal case**: writes a temp copy of the committed baseline with\n   200\t  `couplingPrecision: 1.5` (unreachable — precision is a fraction) and\n   201\t  `tolerances.couplingPrecisionDrop: 0`, runs `--seed --previous-baseline\n   202\t  <temp>`, and asserts exit code `1`, `REFUSED` plus the specific `miner\n   203\t  coupling precision` reason on stderr, and that stdout carries no seeded\n   204\t  JSON (`\"schemaVersion\"` absent).\n   205\t- **Positive control**: writes an unmodified, internally consistent copy of\n   206\t  the committed baseline as `--previous-baseline` and asserts exit code `0`,\n   207\t  `Seeded from the current run` on stderr, no `REFUSED`, and a parseable\n   208\t  seeded object on stdout — proving the comparator ran and passed on its own\n   209\t  merits rather than the refusal path being unreachable.\n   210\t\n   211\tTwo earlier attempts at the positive control failed for reasons worth\n   212\trecording: raising `couplingPrecision` alone past its drop tolerance without\n   213\tloosening the tolerance tripped the refusal (expected); loosening the\n   214\ttolerance to `1` tripped a *different* guard — `bench-correlation-baseline.ts`\n   215\trefuses any tolerance width above a `0.1` ceiling (\"a tolerance wide enough\n   216\tto absorb the whole measurement disarms its gate while still looking\n   217\tarmed\"); and setting `couplingPrecision: 0` with a `0.05` tolerance tripped a\n   218\tthird guard against non-positive baseline metrics (\"a non-positive baseline\n   219\tpermanently disarms the gate it feeds\"). All three are the gate's own\n   220\tdefenses working correctly, not obstacles to route around — the final\n   221\tversion validates against an object with no manufactured internal\n   222\tinconsistency at all.\n   223\t\n   224\t- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n   225\t- No source mutation was needed here — the previous test was a coverage gap\n   226\t  in the *test*, not a defect in `bench-correlation.ts` or\n   227\t  `bench-correlation-baseline.ts`, so this section documents construction and\n   228\t  verification, not a mutation/restore round-trip. Verified by running both\n   229\t  new tests: refusal case exits `1` with the expected reason text; positive\n   230\t  control exits `0` with a parseable candidate. Full suite: `216 pass / 0\n   231\t  fail`.\n   232\t\n   233\t## Round 16 — Codex REQUEST_CHANGES on round 15 (four findings)\n   234\t\n   235\tRound 15 was sent for cross-agent review and came back `Verdict:\n   236\tREQUEST_CHANGES` with one P1 and three P2 findings. All four are fixed here.\n   237\t\n   238\t### P1 — migration source remained forgeable via unpinned tolerances\n   239\t\n   240\t`V13_MAY_MOVE` excludes `tolerances` from `benchBaselinePayloadDigest` so an\n\nexec\n/bin/zsh -lc \"nl -ba docs/validation/ACC-501-MUTATION-PROOFS.md | sed -n '240,500p'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n   240\t`V13_MAY_MOVE` excludes `tolerances` from `benchBaselinePayloadDigest` so an\n   241\tordinary (non-migration) reseed can edit tolerances freely — but\n   242\t`validateCorrelationBenchV12ToV13Migration` carried the caller-supplied\n   243\t`previousV12.tolerances` straight into the seeded candidate and, via\n   244\t`checkMigrationSharedGates`, into grading, with nothing authenticating it.\n   245\tCodex's repro: widening `couplingPrecisionDrop` from `0.02` to `0.1` in the\n   246\tsupplied previous baseline still returned `{ok:true,reasons:[]}`.\n   247\t\n   248\tFix: added `previousTolerancesDigest` to `CorrelationBenchMigrationV12ToV13`,\n   249\tpinned once via a new `benchTolerancesDigest()` helper computed over the real\n   250\treviewed v12 `tolerances` block, and validated inside\n   251\t`validateCorrelationBenchV12ToV13Migration` independently of the\n   252\t`V13_MAY_MOVE` exemption — so tolerances remain editable at ordinary reseed\n   253\ttime but a migration can't be graded against unreviewed tolerances.\n   254\t\n   255\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   256\t- Before checksum: `dd078f9e8d6044b3eb1d7e454f8867a37032d6f27397d52b026d96a1675b7d6c`\n   257\t  (same file identity as the round-15 entry above, prior to this round's edits).\n   258\t- Verified via `refuses a previous baseline whose tolerances are not the\n   259\t  reviewed ones — the shared-gate forgery Codex found` in\n   260\t  `bench-correlation.test.mts`: reproduces Codex's exact repro\n   261\t  (`couplingPrecisionDrop: 0.02 → 0.1`) against the fixed code and confirms\n   262\t  `ok:false` with the new `does not carry the reviewed v12 tolerances`\n   263\t  reason. No source mutation/restore round-trip needed for this one — it's a\n   264\t  new check with no prior code path to mutate; the negative test above\n   265\t  demonstrates it fires, and `accepts the real additive transition with its\n   266\t  real, unforged tolerances` demonstrates the pinned digest matches the real\n   267\t  reviewed value so genuine migrations still pass.\n   268\t\n   269\t### P2 — refusal test never reached `checkDrop`\n   270\t\n   271\tThe round-15 refusal test used `couplingPrecision: 1.5`, which trips an\n   272\tearlier baseline range-validity guard (`[0,1]`) before ever reaching the\n   273\ttolerance-comparison logic (`checkDrop`) the test claimed to exercise — so it\n   274\tproved the CLI refuses invalid input, not that the tolerance gate works.\n   275\t\n   276\tFix: rewrote the test to use `meanTruePairConfidence: 1` (valid — sits at the\n   277\tmetric's own ceiling) with `meanTruePairConfidenceDrop: 0`, forcing\n   278\t`checkDrop`'s own `baseline - live > tolerance` branch to fire for real\n   279\t(`report.meanTruePairConfidence` is provably below `1`). Asserts the actual\n   280\t`checkDrop` diagnostic format (`/exceeds .* tolerance/`), not just `REFUSED`.\n   281\t\n   282\t- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n   283\t- No source mutation needed — this was a test-construction defect, the same\n   284\t  category as the original round-15 finding it's fixing. Verified by running\n   285\t  the corrected test: exits `1`, stderr matches `mean true-pair confidence`\n   286\t  and `exceeds .* tolerance`, stdout carries no seeded JSON.\n   287\t\n   288\t### P2 — positive control too permissive\n   289\t\n   290\tThe round-15 positive-control test only checked `typeof seeded.schemaVersion\n   291\t=== 'number'`, which would pass even if the CLI dropped or corrupted every\n   292\tother field.\n   293\t\n   294\tFix: the positive control now compares the seeded output against the\n   295\tcommitted baseline field-by-field (union of both objects' keys, so a dropped\n   296\tfield is caught too), excluding only the bookkeeping fields an ordinary\n   297\treseed is allowed to touch (`schemaVersion`, `seededAt`, `note`,\n   298\t`reportDigest`, `witnessed`) — reachable because the corpus is frozen and the\n   299\tbenchmark is deterministic, so a fresh seed against an unmodified previous\n   300\tbaseline reproduces every graded field exactly.\n   301\t\n   302\t- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n   303\t- Verified: the strengthened assertion passes against the real CLI output\n   304\t  (`refuses to re-seed against a --previous-baseline whose tolerance the live\n   305\t  run actually violates` / `seeds cleanly against a --previous-baseline the\n   306\t  live run genuinely satisfies` both still pass; the latter now exercises the\n   307\t  field-by-field comparison, not just a `typeof` check).\n   308\t\n   309\t### P2 — one-hop migration not enforced at runtime\n   310\t\n   311\t`manifest.toSchemaVersion === CORRELATION_BENCH_SCHEMA_VERSION` was only\n   312\tasserted by a unit test (`pins the migration to exactly one hop`), not by the\n   313\tvalidator function itself — a future schema bump that forgot to retire or rev\n   314\tthis migration would pass every check in\n   315\t`validateCorrelationBenchV12ToV13Migration` and silently become a two-hop\n   316\tmigration wearing a one-hop label.\n   317\t\n   318\tFix: added a runtime check inside `validateCorrelationBenchV12ToV13Migration`\n   319\tcomparing `manifest.toSchemaVersion` against the live\n   320\t`CORRELATION_BENCH_SCHEMA_VERSION` constant, pushing a refusal reason if they\n   321\tdiverge.\n   322\t\n   323\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   324\t- Note on testability: the function's manifest-identity guard\n   325\t  (`JSON.stringify(manifest) !== JSON.stringify(CORRELATION_BENCH_V12_TO_V13_MIGRATION)`)\n   326\t  refuses any manifest that isn't byte-identical to the pinned module\n   327\t  constant, which means this new check's only reachable failure mode in\n   328\t  production is the pinned constant itself drifting from\n   329\t  `CORRELATION_BENCH_SCHEMA_VERSION` — not independently mutable via a test\n   330\t  argument without also tripping the identity guard first (both reasons\n   331\t  would fire together; the identity guard firing is not a bypass, it's proof\n   332\t  the two constants can't drift independently). Verified via `the runtime\n   333\t  one-hop check is real code, not just a test-level assertion` — confirms\n   334\t  the real manifest and the real schema-version constant agree, and that\n   335\t  `validateCorrelationBenchV12ToV13Migration` returns `ok:true` under that\n   336\t  agreement, so the added check does not falsely refuse the real migration.\n   337\t\n   338\t### P2 — union-of-keys drop detection was untested (mutation proof)\n   339\t\n   340\tThe migration validator iterates the union of `candidate` and `before` keys\n   341\tspecifically so a field present in the reviewed baseline but no longer\n   342\tproduced by the live seed function is visible (`before`-only keys) — not just\n   343\tfields the candidate happens to still carry. No existing test forced that\n   344\tdistinction: the existing \"sees a forbidden field that was DROPPED\" test\n   345\tdeletes a field from `before`, but `candidate` still carries that same key\n   346\t(sourced live from `report`), so candidate-only iteration would already catch\n   347\tit — that test doesn't actually prove the union is load-bearing.\n   348\t\n   349\tAdded a complementary test — `sees a field the reviewed baseline HAD that the\n   350\tlive candidate no longer produces at all` — using a `before` with an extra\n   351\tfield (`legacyRetiredField: 42`) that the candidate-producing path never\n   352\temits, so only union iteration visits it.\n   353\t\n   354\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   355\t- Before checksum: `04696770b63a5ba784692bb0ddbac66743f26b862d9ac9926177191974b3dbe6`.\n   356\t- Mutation: `const keys = [...new Set([...Object.keys(candidate),\n   357\t  ...Object.keys(before)])]` → `const keys = [...new\n   358\t  Set([...Object.keys(candidate)])]` (dropped the `before` half of the union).\n   359\t- Confirmed diff: `git diff` showed exactly the one-line key-union narrowing.\n   360\t- Targeted result: `node --import tsx --test\n   361\t  src/services/correlation/__tests__/bench-correlation.test.mts` →\n   362\t  `219 pass / 1 fail`.\n   363\t- Failing assertion: exactly `sees a field the reviewed baseline HAD that the\n   364\t  live candidate no longer produces at all` failed — actual reason was\n   365\t  `does not carry the reviewed v12 payload` (from the earlier payload-digest\n   366\t  guard, which also fires for this fixture) instead of the expected\n   367\t  `legacyRetiredField moved from 42 to undefined` (which only the\n   368\t  union-iterating field loop produces) — confirming the union half of the key\n   369\t  set is what makes that specific reason reachable. No other test in the\n   370\t  220-test file was affected.\n   371\t- Restored checksum: `04696770b63a5ba784692bb0ddbac66743f26b862d9ac9926177191974b3dbe6`\n   372\t  (matches before checksum exactly).\n   373\t\n   374\tFull suite after all four round-16 fixes: `220 pass / 0 fail`\n   375\t(`bench-correlation.test.mts`), `413 pass / 0 fail` (`npm run\n   376\ttest:correlation`), `npm run bench:correlation` → PASS, `npx tsc --noEmit`\n   377\t(both tsconfig.json and tsconfig.api.json) → 0 errors.\n   378\t\n   379\t## Round 17 — Codex REQUEST_CHANGES on round 16 (three findings)\n   380\t\n   381\tRound 16 was sent for a second cross-agent review and came back a second\n   382\tgenuine `Verdict: REQUEST_CHANGES` with three P2 findings, all fixed here.\n   383\t\n   384\t### P2 — positive control still permitted corrupted seed output\n   385\t\n   386\tCodex re-ran the positive-control test (`seeds cleanly against a\n   387\t--previous-baseline the live run genuinely satisfies`) and found the round-16\n   388\t`mayDiffer` exclusion set (`schemaVersion`, `seededAt`, `note`,\n   389\t`reportDigest`, `witnessed`) was wider than necessary — a fresh, unmodified\n   390\treseed only ever legitimately changes `seededAt` (a wall-clock timestamp);\n   391\t`schemaVersion`, `note`, `reportDigest`, and `witnessed` are all reproduced\n   392\texactly. With the wider set, the test would still pass against a CLI that\n   393\temitted a forged `schemaVersion`, a stale `reportDigest`, or a missing\n   394\t`witnessed` block.\n   395\t\n   396\tFix: narrowed `mayDiffer` to `{'seededAt'}` only.\n   397\t\n   398\t- File: `src/services/correlation/__tests__/bench-correlation.test.mts`.\n   399\t- No source mutation needed — this was a test-construction defect (the\n   400\t  exclusion set was over-broad), the same category as the round-15/16\n   401\t  findings it's fixing. Verified by re-running the narrowed test against the\n   402\t  real CLI: still passes, confirming `schemaVersion`, `note`, `reportDigest`,\n   403\t  and `witnessed` are in fact reproduced exactly on a genuine reseed, and\n   404\t  that the test would now fail if any of them weren't.\n   405\t\n   406\t### P2 — runtime one-hop guard had no load-bearing mutation proof\n   407\t\n   408\tThe round-16 test for the `toSchemaVersion` runtime check asserted only that\n   409\tthe module constant and the live schema version agree — its comment claimed\n   410\t\"if the runtime check in the source were deleted, this still passes,\"\n   411\ttreating the check as untestable given the earlier manifest-identity guard.\n   412\tCodex asked for an actual mutation, not that assumption.\n   413\t\n   414\tPerforming the mutation disproved the comment's own claim: mutating the\n   415\t*value* of `CORRELATION_BENCH_V12_TO_V13_MIGRATION.toSchemaVersion` (not\n   416\tdeleting the runtime check) leaves the manifest self-identical (it's compared\n   417\tagainst itself via the same imported constant), so the identity guard still\n   418\tpasses — and the runtime check then correctly refuses, proving it IS\n   419\treachable through the real validator, just not via a caller-supplied\n   420\tmismatched manifest object.\n   421\t\n   422\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   423\t- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`.\n   424\t- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\n   425\t  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (bypasses the TS literal type\n   426\t  since `tsx` transpiles without type-checking; the mismatch is real at\n   427\t  runtime).\n   428\t- Confirmed diff: `git diff` showed exactly the one-line value change.\n   429\t- Targeted result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n   430\t- Failing tests: `accepts the real additive transition` — failed with reason\n   431\t  `v12→v13 migration manifest targets schemaVersion 99, but the compiled gate\n   432\t  is schemaVersion 13 — this migration is no longer the one-hop path to the\n   433\t  live schema` (the real validator's refusal, proving the check fires\n   434\t  through the public function); `pins the migration to exactly one hop` and\n   435\t  `the runtime one-hop check is real code, not just a test-level assertion`\n   436\t  — both failed on `99 !== 13`, as expected for assertions against the\n   437\t  mutated constant. No other test in the 220-test file was affected.\n   438\t- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n   439\t  (matches before checksum exactly).\n   440\t- The test's comment was corrected to remove the disproven \"if deleted, this\n   441\t  still passes\" claim and point to this proof instead of asserting an\n   442\t  untested assumption.\n   443\t\n   444\t### P2 — tolerances-authentication check (P1 fix) lacked mutation proof\n   445\t\n   446\tRound 16's write-up said the new `previousTolerancesDigest` check had \"no\n   447\tprior code path to mutate\" since it was newly added. Codex correctly pointed\n   448\tout this reasoning doesn't hold — a newly added check still has code that can\n   449\tbe short-circuited to prove the corresponding test depends on it, same as\n   450\tany other check in this function.\n   451\t\n   452\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   453\t- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n   454\t  (same file identity as the toSchemaVersion proof above, run as a separate\n   455\t  mutation after restoring that one).\n   456\t- Mutation: `if (benchTolerancesDigest(previousV12.tolerances) !==\n   457\t  manifest.previousTolerancesDigest)` → `if (false &&\n   458\t  benchTolerancesDigest(...) !== manifest.previousTolerancesDigest)`\n   459\t  (short-circuits the check to never fire).\n   460\t- Confirmed diff: `git diff` showed exactly the one-line `false &&` insertion.\n   461\t- Targeted result: `bench-correlation.test.mts` → `219 pass / 1 fail`.\n   462\t- Failing test: exactly `refuses a previous baseline whose tolerances are not\n   463\t  the reviewed ones — the shared-gate forgery Codex found` failed on\n   464\t  `true !== false` (the test's `assert.equal(ok, false)` — with the check\n   465\t  short-circuited, the forged-tolerance fixture is accepted as `ok:true`). No\n   466\t  other test in the 220-test file was affected, confirming this is the sole\n   467\t  test depending on the check.\n   468\t- Restored checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n   469\t  (matches before checksum exactly).\n   470\t\n   471\tFull suite after all three round-17 fixes: `220 pass / 0 fail`\n   472\t(`bench-correlation.test.mts`), `npx tsc --noEmit` (both tsconfig.json and\n   473\ttsconfig.api.json) → 0 errors.\n   474\t\n   475\t## Round 18 (Codex round-3 REQUEST_CHANGES)\n   476\t\n   477\tCodex's third review of PR #1625 found a real flaw in the round-17 proof\n   478\tabove:\n   479\t\n   480\t> \"[P2] The one-hop guard still lacks the required revert-the-fix mutation\n   481\t> proof. Round 17 mutated the manifest value, not the guard at\n   482\t> bench-correlation-baseline.ts:383. I independently short-circuited that\n   483\t> guard; all three relevant tests remained green (3 pass / 0 fail).\n   484\t> Therefore the test comment and the evidence write-up incorrectly say guard\n   485\t> deletion was performed and disproved. The toSchemaVersion: 99 mutation\n   486\t> does prove the negative path is reachable, but not that removing the fix\n   487\t> turns the suite red. Add a test that asserts the one-hop refusal reason\n   488\t> through the validator and fails when this guard is disabled.\"\n   489\t\n   490\tThis is correct. Round 17 mutated only the pinned constant's *value*\n   491\t(`toSchemaVersion: 99`) with the guard's *code* intact, and confirmed 3\n   492\ttests failed. But with the real, unmutated inputs the constant and the\n   493\tcompiled schema version are always in sync (13 === 13), so the guard's\n   494\tbranch condition (`manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION`)\n   495\tis never true whether or not the guard code exists — disabling the guard\n   496\talone, with in-sync inputs, is a no-op test that proves nothing about the\n   497\tguard's necessity.\n   498\t\n   499\t### Combined mutation — the genuine revert-the-fix proof\n   500\t\n\nexec\n/bin/zsh -lc \"nl -ba docs/validation/ACC-501-MUTATION-PROOFS.md | sed -n '500,686p'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\n   500\t\n   501\tTo prove the guard's *code* (not just the drifted value) is load-bearing,\n   502\tboth mutations were applied simultaneously: drift the constant AND disable\n   503\tthe guard that would have caught the drift.\n   504\t\n   505\t- File: `src/services/correlation/bench-correlation-baseline.ts`.\n   506\t- Before checksum: `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`.\n   507\t\n   508\t**Step 1 — guard restored, constant drifted only** (reproduces round 17's\n   509\tproof, captured with exact identities this time):\n   510\t\n   511\t- Mutation: `toSchemaVersion: 13,` → `toSchemaVersion: 99 as unknown as 13,`\n   512\t  on `CORRELATION_BENCH_V12_TO_V13_MIGRATION` (line 288). Guard code at line\n   513\t  383 untouched.\n   514\t- Result: `bench-correlation.test.mts` → `217 pass / 3 fail`.\n   515\t- Failing tests, with exact assertion text:\n   516\t  1. `round 15 — the v12→v13 schema bump is a pinned migration, not a\n   517\t     skipped check` — `false !== true` against the expected refusal reason\n   518\t     `\"v12→v13 migration manifest targets schemaVersion 99, but the\n   519\t     compiled gate is schemaVersion 13 — this migration is no longer the\n   520\t     one-hop path to the live schema\"`.\n   521\t  2. `pins the migration to exactly one hop` — `99 !== 13`.\n   522\t  3. `the runtime one-hop check is real code, not just a test-level\n   523\t     assertion` — `99 !== 13`.\n   524\t\n   525\t**Step 2 — guard ALSO disabled, constant still drifted** (the genuine\n   526\trevert-the-fix proof):\n   527\t\n   528\t- Additional mutation: `if (manifest.toSchemaVersion !==\n   529\t  CORRELATION_BENCH_SCHEMA_VERSION) {` → `if (false &&\n   530\t  manifest.toSchemaVersion !== CORRELATION_BENCH_SCHEMA_VERSION) {` (line\n   531\t  383), applied on top of the still-active `toSchemaVersion: 99` mutation.\n   532\t- Result: `bench-correlation.test.mts` → `218 pass / 2 fail`.\n   533\t- The two remaining failures are exactly `pins the migration to exactly one\n   534\t  hop` and `the runtime one-hop check is real code, not just a test-level\n   535\t  assertion` — both fail on the raw constant-value assertion (`99 !== 13`),\n   536\t  which is unrelated to the guard's presence and would fail regardless.\n   537\t- Critically, `round 15 — the v12→v13 schema bump is a pinned migration, not\n   538\t  a skipped check` (test 1 above) — which had failed on `false !== true` in\n   539\t  Step 1 — now **passes** in Step 2. With the guard's code disabled, the\n   540\t  validator's `ok` result flips back to `true` even though the manifest\n   541\t  still targets a stale schema (99 vs the compiled 13). This is the\n   542\t  false-positive accept: disabling the guard's *code*, not just having a\n   543\t  drifted value, is what causes the migration to be silently accepted. This\n   544\t  is the genuine revert-the-fix proof Codex asked for.\n   545\t- Restored checksum after reverting both mutations:\n   546\t  `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n   547\t  (matches before checksum exactly). `git diff --stat` on the file showed no\n   548\t  diff.\n   549\t- Full suite after restoration: `bench-correlation.test.mts` →\n   550\t  `220 pass / 0 fail`.\n   551\t\n   552\t### Permanent test (not just a doc-recorded mutation experiment)\n   553\t\n   554\tCodex explicitly asked for \"a test that asserts the one-hop refusal reason\n   555\tthrough the validator and fails when this guard is disabled.\" Because the\n   556\tmanifest-identity guard at line 355 makes the `toSchemaVersion` branch\n   557\tunreachable via any caller-supplied manifest — the only way to exercise it\n   558\tthrough `validateCorrelationBenchV12ToV13Migration` is to mutate the pinned\n   559\tmodule constant itself, which cannot be part of a permanent, always-runnable\n   560\tunit test — the comparison was extracted into a new exported pure function,\n   561\t`oneHopSchemaVersionCheckReason(manifestToSchemaVersion, compiledSchemaVersion)`,\n   562\tand the validator now calls it instead of inlining the check. Two new\n   563\tpermanent tests in `bench-correlation.test.mts` call this real function\n   564\tdirectly (not a re-implementation):\n   565\t\n   566\t- `oneHopSchemaVersionCheckReason refuses a manifest that no longer targets\n   567\t  the compiled schema` — asserts the exact refusal reason string for\n   568\t  `(99, CORRELATION_BENCH_SCHEMA_VERSION)`.\n   569\t- `oneHopSchemaVersionCheckReason accepts a manifest that targets the\n   570\t  compiled schema` — asserts `null` for matching versions.\n   571\t\n   572\tThese tests call the exact function the validator invokes, so deleting or\n   573\tweakening `oneHopSchemaVersionCheckReason`'s own logic fails them directly.\n   574\tFull suite after this change: `222 pass / 0 fail`. `npm run typecheck:all` →\n   575\t0 errors.\n   576\t\n   577\t**This claim turned out to be incomplete** — see round 18b below.\n   578\t\n   579\t## Round 18b (Codex round-4 REQUEST_CHANGES)\n   580\t\n   581\t> [P2] The permanent regression test still does not exercise validator\n   582\t> wiring. Deleting the call at bench-correlation-baseline.ts:405 leaves both\n   583\t> helper tests and the matching-input validator test green. Thus the claim\n   584\t> at ACC-501-MUTATION-PROOFS.md:572 is false, and round 17's requested\n   585\t> validator-level regression test remains missing. The combined manual\n   586\t> mutation is valid evidence, but it is not permanent coverage.\n   587\t\n   588\tCorrect. Testing `oneHopSchemaVersionCheckReason()` directly proves the\n   589\tcheck's own logic is right, but proves nothing about whether\n   590\t`validateCorrelationBenchV12ToV13Migration` still *calls* it — removing a\n   591\trefusal check only produces false negatives (missed refusals), never false\n   592\tpositives on already-correct input, so deleting the call site left every\n   593\texisting test green (the helper's own tests, and the validator's\n   594\tmatching-input `ok === true` test).\n   595\t\n   596\t### Root cause: the manifest-identity guard, again\n   597\t\n   598\tThe reason this was untestable through the public validator is the same\n   599\tguard flagged in round 15/17: `validateCorrelationBenchV12ToV13Migration`\n   600\tdid a blanket `JSON.stringify(manifest) !== JSON.stringify(pinned)`\n   601\tcomparison that early-returned before ANY caller-supplied manifest\n   602\tdiffering in even one field — including `toSchemaVersion` — could reach a\n   603\tfield-specific check. A manifest built to differ from the pinned constant\n   604\tonly in `toSchemaVersion` was swallowed by the generic \"manifest does not\n   605\tmatch\" refusal before ever reaching the one-hop check.\n   606\t\n   607\t### Fix: exclude `toSchemaVersion` from the blanket identity comparison\n   608\t\n   609\t`validateCorrelationBenchV12ToV13Migration` now destructures `toSchemaVersion`\n   610\tout of the caller manifest, and strips it from a shallow copy of the pinned\n   611\tconstant, before the identity comparison — then checks it separately via\n   612\t`oneHopSchemaVersionCheckReason` immediately after. (An initial destructuring\n   613\tform of the pinned-side omission, `const { toSchemaVersion: _pinned, ...pinnedRest }`,\n   614\ttripped `@typescript-eslint/no-unused-vars` / `sonarjs/no-unused-vars`; the\n   615\t`delete`-based form below is what actually shipped.)\n   616\t\n   617\t```ts\n   618\tconst { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n   619\tconst pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n   620\tdelete pinnedRest.toSchemaVersion;\n   621\tif (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n   622\t  reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n   623\t  return { ok: false, reasons };\n   624\t}\n   625\tconst oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n   626\tif (oneHopReason !== null) {\n   627\t  reasons.push(oneHopReason);\n   628\t}\n   629\t```\n   630\t\n   631\tThe comparison is against the independently-compiled `CORRELATION_BENCH_SCHEMA_VERSION`\n   632\tconstant, not the pinned constant's own `toSchemaVersion` field — comparing\n   633\tagainst the latter would silently defeat the check's purpose, since it would\n   634\tjust compare the pinned constant against a caller manifest copying it,\n   635\tnever catching the pinned constant itself drifting from the compiled schema.\n   636\t\n   637\t### New permanent test — through the real validator\n   638\t\n   639\t`refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n   640\tthe compiled schema — validator wiring, not just the helper` builds\n   641\t`{ ...manifest, toSchemaVersion: 99 }` (identical to the pinned migration in\n   642\tevery other field) and calls `validateCorrelationBenchV12ToV13Migration`\n   643\tdirectly, asserting `ok === false` and the exact refusal text. This is now\n   644\treachable through the public validator for the first time, closing the gap\n   645\tround 18b identified.\n   646\t\n   647\t### Mutation proof — deleting the call site fails the new test\n   648\t\n   649\tChecksum before mutation: `4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc`.\n   650\t\n   651\t**Mutation** — removed the validator's call to `oneHopSchemaVersionCheckReason`\n   652\t(the four lines pushing `oneHopReason` into `reasons`), leaving everything\n   653\telse, including `manifestToSchemaVersion`'s extraction, intact.\n   654\t\n   655\t**Result**: `223 tests / 222 pass / 1 fail`. The new validator-level test\n   656\tfailed exactly as expected:\n   657\t\n   658\t```\n   659\t✖ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n   660\t  the compiled schema — validator wiring, not just the helper\n   661\t  AssertionError: Expected values to be strictly equal: true !== false\n   662\t```\n   663\t\n   664\t`round 15 — the v12→v13 schema bump is a pinned migration, not a skipped\n   665\tcheck` also failed, matching round 18's earlier combined-mutation proof —\n   666\tconsistent evidence that this is the same load-bearing check, now reachable\n   667\twithout mutating a module constant.\n   668\t\n   669\t**Restoration**: re-added the four-line call site. Checksum after restore:\n   670\t`4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc` — exact\n   671\tmatch. `git diff --stat` showed only the legitimate (committed) guard\n   672\trestructuring, no residual mutation.\n   673\t\n   674\t**Full verification chain after this fix**:\n   675\t\n   676\t- `bench-correlation.test.mts` — `223 pass / 0 fail`\n   677\t- `npm run typecheck:all` — 0 errors\n   678\t- `npm run bench:correlation` — PASS, within tolerance of the committed baseline\n   679\t- `npm run test:correlation` — `416 pass / 0 fail`\n   680\t\n   681\t## Restoration\n   682\t\n   683\tFinal checksums matched all pre-mutation values for every mutated file across\n   684\tround 14, round 15, round 16, round 17, round 18, and round 18b, and\n   685\t`git status --short` after each restoration showed only the legitimate\n   686\tsource diff for that round — no residual mutation.\n\ncodex\nThe two round-19 edits themselves match the current validator structure and fix the stated total. I’m now reconciling every historical count and reachability statement against the committed test structure; one passage needs special scrutiny because Node reports a failed parent suite in addition to its single failed leaf test.\nexec\n/bin/zsh -lc 'git show --format=fuller --stat 235331b2' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 111ms:\ncommit 235331b2014fc9b61f19607cab48ac0585b36d3f\nAuthor:     Brad Bond <bradley_bond@me.com>\nAuthorDate: Tue Aug 4 22:44:30 2026 -0500\nCommit:     Brad Bond <bradley_bond@me.com>\nCommitDate: Tue Aug 4 22:44:30 2026 -0500\n\n    ACC-501: close round 18b gap — validator-level test for one-hop schema check\n    \n    Codex round 18b found that testing oneHopSchemaVersionCheckReason() directly\n    proved the check's own logic but not that the validator still calls it —\n    deleting the call site left every existing test green. Root cause: the\n    manifest-identity guard's blanket comparison swallowed any caller-supplied\n    manifest differing in toSchemaVersion before it could reach the specific\n    check, making it unreachable through the public validator without mutating\n    the pinned module constant.\n    \n    Restructured validateCorrelationBenchV12ToV13Migration to exclude\n    toSchemaVersion from that blanket identity comparison and check it\n    separately, making a manifest that differs from the pinned migration only\n    in toSchemaVersion reachable for the first time. Added a validator-level\n    regression test exercising this directly, confirmed via mutation/restore\n    (deleting the call site fails the new test; checksum matches after restore).\n    \n    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\n\n docs/validation/ACC-501-MUTATION-PROOFS.md         | 119 +++++++++++++++++++--\n .../__tests__/bench-correlation.test.mts           |  37 ++++++-\n .../correlation/bench-correlation-baseline.ts      |  21 +++-\n 3 files changed, 161 insertions(+), 16 deletions(-)\n\nexec\n/bin/zsh -lc 'git show --format=fuller --no-ext-diff 235331b2 -- docs/validation/ACC-501-MUTATION-PROOFS.md src/services/correlation/bench-correlation-baseline.ts src/services/correlation/__tests__/bench-correlation.test.mts' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\ncommit 235331b2014fc9b61f19607cab48ac0585b36d3f\nAuthor:     Brad Bond <bradley_bond@me.com>\nAuthorDate: Tue Aug 4 22:44:30 2026 -0500\nCommit:     Brad Bond <bradley_bond@me.com>\nCommitDate: Tue Aug 4 22:44:30 2026 -0500\n\n    ACC-501: close round 18b gap — validator-level test for one-hop schema check\n    \n    Codex round 18b found that testing oneHopSchemaVersionCheckReason() directly\n    proved the check's own logic but not that the validator still calls it —\n    deleting the call site left every existing test green. Root cause: the\n    manifest-identity guard's blanket comparison swallowed any caller-supplied\n    manifest differing in toSchemaVersion before it could reach the specific\n    check, making it unreachable through the public validator without mutating\n    the pinned module constant.\n    \n    Restructured validateCorrelationBenchV12ToV13Migration to exclude\n    toSchemaVersion from that blanket identity comparison and check it\n    separately, making a manifest that differs from the pinned migration only\n    in toSchemaVersion reachable for the first time. Added a validator-level\n    regression test exercising this directly, confirmed via mutation/restore\n    (deleting the call site fails the new test; checksum matches after restore).\n    \n    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\n\ndiff --git a/docs/validation/ACC-501-MUTATION-PROOFS.md b/docs/validation/ACC-501-MUTATION-PROOFS.md\nindex 2c6ee7f1..f598c9f4 100644\n--- a/docs/validation/ACC-501-MUTATION-PROOFS.md\n+++ b/docs/validation/ACC-501-MUTATION-PROOFS.md\n@@ -569,15 +569,118 @@ directly (not a re-implementation):\n - `oneHopSchemaVersionCheckReason accepts a manifest that targets the\n   compiled schema` — asserts `null` for matching versions.\n \n-Because these tests call the exact function the validator invokes, deleting\n-the guard's call-site, weakening its condition, or changing its return value\n-fails these tests directly on every run — no manual mutation experiment\n-required to catch a future regression. Full suite after this change:\n-`222 pass / 0 fail`. `npm run typecheck:all` → 0 errors.\n+These tests call the exact function the validator invokes, so deleting or\n+weakening `oneHopSchemaVersionCheckReason`'s own logic fails them directly.\n+Full suite after this change: `222 pass / 0 fail`. `npm run typecheck:all` →\n+0 errors.\n+\n+**This claim turned out to be incomplete** — see round 18b below.\n+\n+## Round 18b (Codex round-4 REQUEST_CHANGES)\n+\n+> [P2] The permanent regression test still does not exercise validator\n+> wiring. Deleting the call at bench-correlation-baseline.ts:405 leaves both\n+> helper tests and the matching-input validator test green. Thus the claim\n+> at ACC-501-MUTATION-PROOFS.md:572 is false, and round 17's requested\n+> validator-level regression test remains missing. The combined manual\n+> mutation is valid evidence, but it is not permanent coverage.\n+\n+Correct. Testing `oneHopSchemaVersionCheckReason()` directly proves the\n+check's own logic is right, but proves nothing about whether\n+`validateCorrelationBenchV12ToV13Migration` still *calls* it — removing a\n+refusal check only produces false negatives (missed refusals), never false\n+positives on already-correct input, so deleting the call site left every\n+existing test green (the helper's own tests, and the validator's\n+matching-input `ok === true` test).\n+\n+### Root cause: the manifest-identity guard, again\n+\n+The reason this was untestable through the public validator is the same\n+guard flagged in round 15/17: `validateCorrelationBenchV12ToV13Migration`\n+did a blanket `JSON.stringify(manifest) !== JSON.stringify(pinned)`\n+comparison that early-returned before ANY caller-supplied manifest\n+differing in even one field — including `toSchemaVersion` — could reach a\n+field-specific check. A manifest built to differ from the pinned constant\n+only in `toSchemaVersion` was swallowed by the generic \"manifest does not\n+match\" refusal before ever reaching the one-hop check.\n+\n+### Fix: exclude `toSchemaVersion` from the blanket identity comparison\n+\n+`validateCorrelationBenchV12ToV13Migration` now destructures `toSchemaVersion`\n+out of the caller manifest, and strips it from a shallow copy of the pinned\n+constant, before the identity comparison — then checks it separately via\n+`oneHopSchemaVersionCheckReason` immediately after. (An initial destructuring\n+form of the pinned-side omission, `const { toSchemaVersion: _pinned, ...pinnedRest }`,\n+tripped `@typescript-eslint/no-unused-vars` / `sonarjs/no-unused-vars`; the\n+`delete`-based form below is what actually shipped.)\n+\n+```ts\n+const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n+const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n+delete pinnedRest.toSchemaVersion;\n+if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n+  reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n+  return { ok: false, reasons };\n+}\n+const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n+if (oneHopReason !== null) {\n+  reasons.push(oneHopReason);\n+}\n+```\n+\n+The comparison is against the independently-compiled `CORRELATION_BENCH_SCHEMA_VERSION`\n+constant, not the pinned constant's own `toSchemaVersion` field — comparing\n+against the latter would silently defeat the check's purpose, since it would\n+just compare the pinned constant against a caller manifest copying it,\n+never catching the pinned constant itself drifting from the compiled schema.\n+\n+### New permanent test — through the real validator\n+\n+`refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n+the compiled schema — validator wiring, not just the helper` builds\n+`{ ...manifest, toSchemaVersion: 99 }` (identical to the pinned migration in\n+every other field) and calls `validateCorrelationBenchV12ToV13Migration`\n+directly, asserting `ok === false` and the exact refusal text. This is now\n+reachable through the public validator for the first time, closing the gap\n+round 18b identified.\n+\n+### Mutation proof — deleting the call site fails the new test\n+\n+Checksum before mutation: `4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc`.\n+\n+**Mutation** — removed the validator's call to `oneHopSchemaVersionCheckReason`\n+(the four lines pushing `oneHopReason` into `reasons`), leaving everything\n+else, including `manifestToSchemaVersion`'s extraction, intact.\n+\n+**Result**: `223 pass / 1 fail`. The new validator-level test failed exactly\n+as expected:\n+\n+```\n+✖ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from\n+  the compiled schema — validator wiring, not just the helper\n+  AssertionError: Expected values to be strictly equal: true !== false\n+```\n+\n+`round 15 — the v12→v13 schema bump is a pinned migration, not a skipped\n+check` also failed, matching round 18's earlier combined-mutation proof —\n+consistent evidence that this is the same load-bearing check, now reachable\n+without mutating a module constant.\n+\n+**Restoration**: re-added the four-line call site. Checksum after restore:\n+`4eb7e5a3c4f7d18f35cccca85980db7857e685328c8311367586d566a8ee07dc` — exact\n+match. `git diff --stat` showed only the legitimate (committed) guard\n+restructuring, no residual mutation.\n+\n+**Full verification chain after this fix**:\n+\n+- `bench-correlation.test.mts` — `223 pass / 0 fail`\n+- `npm run typecheck:all` — 0 errors\n+- `npm run bench:correlation` — PASS, within tolerance of the committed baseline\n+- `npm run test:correlation` — `416 pass / 0 fail`\n \n ## Restoration\n \n Final checksums matched all pre-mutation values for every mutated file across\n-round 14, round 15, round 16, round 17, and round 18, and `git status --short`\n-after each restoration showed only the legitimate source diff for that round\n-— no residual mutation.\n+round 14, round 15, round 16, round 17, round 18, and round 18b, and\n+`git status --short` after each restoration showed only the legitimate\n+source diff for that round — no residual mutation.\ndiff --git a/src/services/correlation/__tests__/bench-correlation.test.mts b/src/services/correlation/__tests__/bench-correlation.test.mts\nindex 5b358a5a..a3660fc3 100644\n--- a/src/services/correlation/__tests__/bench-correlation.test.mts\n+++ b/src/services/correlation/__tests__/bench-correlation.test.mts\n@@ -3540,9 +3540,15 @@ describe('round 15 — the v12→v13 schema bump is a pinned migration, not a sk\n     // logic — not just the module constant — permanently, directly testable\n     // without any source mutation, the comparison was extracted into\n     // oneHopSchemaVersionCheckReason(). The tests immediately below call\n-    // that function directly: this is the real code path the validator\n-    // calls, not a re-implementation, so deleting or breaking the guard's\n-    // logic fails these tests without needing a manual mutation experiment.\n+    // that function directly, proving the check's own logic is correct.\n+    // Round 18b (Codex): that alone doesn't prove the VALIDATOR still calls\n+    // it — deleting the call site left those helper tests, and this test,\n+    // both green (removing a refusal only produces false negatives). The\n+    // manifest-identity guard was restructured to exclude toSchemaVersion\n+    // from its blanket comparison so a manifest differing ONLY in that\n+    // field reaches the check through the real validator; see the\n+    // \"validator wiring, not just the helper\" test below for the\n+    // permanent, mutation-free proof that the call site itself is intact.\n     assert.equal(\n       correlationBaseline.CORRELATION_BENCH_V12_TO_V13_MIGRATION.toSchemaVersion,\n       CORRELATION_BENCH_SCHEMA_VERSION,\n@@ -3574,6 +3580,31 @@ describe('round 15 — the v12→v13 schema bump is a pinned migration, not a sk\n     );\n   });\n \n+  it('refuses a caller-supplied manifest whose toSchemaVersion alone drifts from the compiled schema — validator wiring, not just the helper', () => {\n+    // Round 18b (Codex): testing oneHopSchemaVersionCheckReason() directly\n+    // proves the CHECK is correct, but not that the VALIDATOR still CALLS\n+    // it — deleting that call site left both the helper tests and the\n+    // \"matching-input\" validator test green. The old manifest-identity\n+    // guard made this untestable through the public validator (any\n+    // toSchemaVersion mismatch was swallowed by the blanket identity\n+    // refusal before reaching the specific check), so the guard was\n+    // restructured to exclude toSchemaVersion from that blanket comparison\n+    // and check it separately. This test constructs a manifest identical to\n+    // the pinned migration in every OTHER field and calls the real\n+    // validator — no source mutation required.\n+    const staleManifest = { ...manifest, toSchemaVersion: 99 };\n+    const { ok, reasons } = correlationBaseline.validateCorrelationBenchV12ToV13Migration(\n+      report,\n+      previousV12(),\n+      staleManifest,\n+    );\n+    assert.equal(ok, false);\n+    assert.match(\n+      reasons.join(' '),\n+      /targets schemaVersion 99, but the compiled gate is schemaVersion/,\n+    );\n+  });\n+\n   it('refuses a previous baseline whose tolerances are not the reviewed ones — the shared-gate forgery Codex found', () => {\n     // P1: `tolerances` sits in V13_MAY_MOVE so `benchBaselinePayloadDigest`\n     // skips it, letting an ordinary reseed edit tolerances freely. But\ndiff --git a/src/services/correlation/bench-correlation-baseline.ts b/src/services/correlation/bench-correlation-baseline.ts\nindex 9548c8e3..86fe060d 100644\n--- a/src/services/correlation/bench-correlation-baseline.ts\n+++ b/src/services/correlation/bench-correlation-baseline.ts\n@@ -374,10 +374,25 @@ export function validateCorrelationBenchV12ToV13Migration(\n   manifest: CorrelationBenchMigrationV12ToV13,\n ): CorrelationBenchComparison {\n   const reasons: string[] = [];\n-  if (JSON.stringify(manifest) !== JSON.stringify(CORRELATION_BENCH_V12_TO_V13_MIGRATION)) {\n+  // toSchemaVersion is deliberately excluded from this identity comparison —\n+  // it gets its OWN check below via oneHopSchemaVersionCheckReason(), so a\n+  // caller-supplied manifest that matches the pinned migration in every\n+  // OTHER field still reaches that check instead of being swallowed here.\n+  // Round 18: Codex found that folding toSchemaVersion into this blanket\n+  // identity guard made the schema check unreachable through the public\n+  // validator — the only way to exercise it was to mutate the pinned module\n+  // constant, which can't be a permanent regression test.\n+  const { toSchemaVersion: manifestToSchemaVersion, ...manifestRest } = manifest;\n+  const pinnedRest: Partial<CorrelationBenchMigrationV12ToV13> = { ...CORRELATION_BENCH_V12_TO_V13_MIGRATION };\n+  delete pinnedRest.toSchemaVersion;\n+  if (JSON.stringify(manifestRest) !== JSON.stringify(pinnedRest)) {\n     reasons.push('v12→v13 migration manifest does not match the pinned additive transition');\n     return { ok: false, reasons };\n   }\n+  const oneHopReason = oneHopSchemaVersionCheckReason(manifestToSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n+  if (oneHopReason !== null) {\n+    reasons.push(oneHopReason);\n+  }\n   if (previousV12.schemaVersion !== 12) {\n     reasons.push(`v12→v13 migration source is schemaVersion ${previousV12.schemaVersion}, not 12`);\n   }\n@@ -402,10 +417,6 @@ export function validateCorrelationBenchV12ToV13Migration(\n       'tolerance here would grade leniently against a value nobody reviewed',\n     );\n   }\n-  const oneHopReason = oneHopSchemaVersionCheckReason(manifest.toSchemaVersion, CORRELATION_BENCH_SCHEMA_VERSION);\n-  if (oneHopReason !== null) {\n-    reasons.push(oneHopReason);\n-  }\n   if (report.corpusDigest !== manifest.previousCorpusDigest) {\n     reasons.push(\n       'v13 corpus digest moved — this migration is additive-only and permits no corpus edit',\n\nexec\n/bin/zsh -lc 'git show 235331b2''^:docs/validation/ACC-501-MUTATION-PROOFS.md | nl -ba | sed -n '\"'540,690p'\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 237ms:\n   540\t  validator's `ok` result flips back to `true` even though the manifest\n   541\t  still targets a stale schema (99 vs the compiled 13). This is the\n   542\t  false-positive accept: disabling the guard's *code*, not just having a\n   543\t  drifted value, is what causes the migration to be silently accepted. This\n   544\t  is the genuine revert-the-fix proof Codex asked for.\n   545\t- Restored checksum after reverting both mutations:\n   546\t  `081b535902de7c75eeb090b8c18083af17ccbdf32af6d227202cc1a50577a511`\n   547\t  (matches before checksum exactly). `git diff --stat` on the file showed no\n   548\t  diff.\n   549\t- Full suite after restoration: `bench-correlation.test.mts` →\n   550\t  `220 pass / 0 fail`.\n   551\t\n   552\t### Permanent test (not just a doc-recorded mutation experiment)\n   553\t\n   554\tCodex explicitly asked for \"a test that asserts the one-hop refusal reason\n   555\tthrough the validator and fails when this guard is disabled.\" Because the\n   556\tmanifest-identity guard at line 355 makes the `toSchemaVersion` branch\n   557\tunreachable via any caller-supplied manifest — the only way to exercise it\n   558\tthrough `validateCorrelationBenchV12ToV13Migration` is to mutate the pinned\n   559\tmodule constant itself, which cannot be part of a permanent, always-runnable\n   560\tunit test — the comparison was extracted into a new exported pure function,\n   561\t`oneHopSchemaVersionCheckReason(manifestToSchemaVersion, compiledSchemaVersion)`,\n   562\tand the validator now calls it instead of inlining the check. Two new\n   563\tpermanent tests in `bench-correlation.test.mts` call this real function\n   564\tdirectly (not a re-implementation):\n   565\t\n   566\t- `oneHopSchemaVersionCheckReason refuses a manifest that no longer targets\n   567\t  the compiled schema` — asserts the exact refusal reason string for\n   568\t  `(99, CORRELATION_BENCH_SCHEMA_VERSION)`.\n   569\t- `oneHopSchemaVersionCheckReason accepts a manifest that targets the\n   570\t  compiled schema` — asserts `null` for matching versions.\n   571\t\n   572\tBecause these tests call the exact function the validator invokes, deleting\n   573\tthe guard's call-site, weakening its condition, or changing its return value\n   574\tfails these tests directly on every run — no manual mutation experiment\n   575\trequired to catch a future regression. Full suite after this change:\n   576\t`222 pass / 0 fail`. `npm run typecheck:all` → 0 errors.\n   577\t\n   578\t## Restoration\n   579\t\n   580\tFinal checksums matched all pre-mutation values for every mutated file across\n   581\tround 14, round 15, round 16, round 17, and round 18, and `git status --short`\n   582\tafter each restoration showed only the legitimate source diff for that round\n   583\t— no residual mutation.\n\nexec\n/bin/zsh -lc \"rg -n \\\"describe\\\\('round 15|it\\\\('accepts the real additive|it\\\\('refuses a caller-supplied\\\" src/services/correlation/__tests__/bench-correlation.test.mts\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 105ms:\n3393:describe('round 15 — the v12→v13 schema bump is a pinned migration, not a skipped check', () => {\n3462:  it('accepts the real additive transition', () => {\n3583:  it('refuses a caller-supplied manifest whose toSchemaVersion alone drifts from the compiled schema — validator wiring, not just the helper', () => {\n3632:  it('accepts the real additive transition with its real, unforged tolerances', () => {\n\nexec\n/bin/zsh -lc 'npx tsx --test src/services/correlation/__tests__/bench-correlation.test.mts' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n exited 1 in 672ms:\nnode:net:1986\n      const error = new UVExceptionWithHostPort(rval, 'listen', address, port);\n                    ^\n\nError: listen EPERM: operation not permitted /var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/tsx-501/1312.pipe\n    at Server.setupListenHandle [as _listen2] (node:net:1986:21)\n    at listenInCluster (node:net:2065:12)\n    at Server.listen (node:net:2187:5)\n    at file:///Users/bradleybond/Developer/crystalball/.worktrees/acc401/node_modules/tsx/dist/cli.mjs:53:31537\n    at new Promise (<anonymous>)\n    at createIpcServer (file:///Users/bradleybond/Developer/crystalball/.worktrees/acc401/node_modules/tsx/dist/cli.mjs:53:31515)\n    at async file:///Users/bradleybond/Developer/crystalball/.worktrees/acc401/node_modules/tsx/dist/cli.mjs:55:459 {\n  code: 'EPERM',\n  errno: -1,\n  syscall: 'listen',\n  address: '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/tsx-501/1312.pipe',\n  port: -1\n}\n\nNode.js v26.3.0\n\ncodex\nThe exact focused command was blocked before test discovery by the sandbox: `tsx` could not open its IPC pipe (`listen EPERM .../tsx-501/1312.pipe`). The QA/QC workflow therefore triggers `systematic-debugging`; I’m checking the equivalent no-IPC Node invocation used elsewhere in this repository so the test result is still independently observable, while recording the exact requested command as not runnable in this environment.\nexec\n/bin/zsh -lc \"sed -n '1,320p' /Users/bradleybond/.codex/skills/systematic-debugging/SKILL.md\" in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 233ms:\n---\nname: systematic-debugging\ndescription: Use when encountering any bug, test failure, or unexpected behavior, before proposing fixes\n---\n\n# Systematic Debugging\n\n## Overview\n\nRandom fixes waste time and create new bugs. Quick patches mask underlying issues.\n\n**Core principle:** ALWAYS find root cause before attempting fixes. Symptom fixes are failure.\n\n**Violating the letter of this process is violating the spirit of debugging.**\n\n## The Iron Law\n\n```\nNO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST\n```\n\nIf you haven't completed Phase 1, you cannot propose fixes.\n\n## When to Use\n\nUse for ANY technical issue:\n- Test failures\n- Bugs in production\n- Unexpected behavior\n- Performance problems\n- Build failures\n- Integration issues\n\n**Use this ESPECIALLY when:**\n- Under time pressure (emergencies make guessing tempting)\n- \"Just one quick fix\" seems obvious\n- You've already tried multiple fixes\n- Previous fix didn't work\n- You don't fully understand the issue\n\n**Don't skip when:**\n- Issue seems simple (simple bugs have root causes too)\n- You're in a hurry (rushing guarantees rework)\n- Manager wants it fixed NOW (systematic is faster than thrashing)\n\n## The Four Phases\n\nYou MUST complete each phase before proceeding to the next.\n\n### Phase 1: Root Cause Investigation\n\n**BEFORE attempting ANY fix:**\n\n1. **Read Error Messages Carefully**\n   - Don't skip past errors or warnings\n   - They often contain the exact solution\n   - Read stack traces completely\n   - Note line numbers, file paths, error codes\n\n2. **Reproduce Consistently**\n   - Can you trigger it reliably?\n   - What are the exact steps?\n   - Does it happen every time?\n   - If not reproducible → gather more data, don't guess\n\n3. **Check Recent Changes**\n   - What changed that could cause this?\n   - Git diff, recent commits\n   - New dependencies, config changes\n   - Environmental differences\n\n4. **Gather Evidence in Multi-Component Systems**\n\n   **WHEN system has multiple components (CI → build → signing, API → service → database):**\n\n   **BEFORE proposing fixes, add diagnostic instrumentation:**\n   ```\n   For EACH component boundary:\n     - Log what data enters component\n     - Log what data exits component\n     - Verify environment/config propagation\n     - Check state at each layer\n\n   Run once to gather evidence showing WHERE it breaks\n   THEN analyze evidence to identify failing component\n   THEN investigate that specific component\n   ```\n\n   **Example (multi-layer system):**\n   ```bash\n   # Layer 1: Workflow\n   echo \"=== Secrets available in workflow: ===\"\n   echo \"IDENTITY: ${IDENTITY:+SET}${IDENTITY:-UNSET}\"\n\n   # Layer 2: Build script\n   echo \"=== Env vars in build script: ===\"\n   env | grep IDENTITY || echo \"IDENTITY not in environment\"\n\n   # Layer 3: Signing script\n   echo \"=== Keychain state: ===\"\n   security list-keychains\n   security find-identity -v\n\n   # Layer 4: Actual signing\n   codesign --sign \"$IDENTITY\" --verbose=4 \"$APP\"\n   ```\n\n   **This reveals:** Which layer fails (secrets → workflow ✓, workflow → build ✗)\n\n5. **Trace Data Flow**\n\n   **WHEN error is deep in call stack:**\n\n   See `root-cause-tracing.md` in this directory for the complete backward tracing technique.\n\n   **Quick version:**\n   - Where does bad value originate?\n   - What called this with bad value?\n   - Keep tracing up until you find the source\n   - Fix at source, not at symptom\n\n### Phase 2: Pattern Analysis\n\n**Find the pattern before fixing:**\n\n1. **Find Working Examples**\n   - Locate similar working code in same codebase\n   - What works that's similar to what's broken?\n\n2. **Compare Against References**\n   - If implementing pattern, read reference implementation COMPLETELY\n   - Don't skim - read every line\n   - Understand the pattern fully before applying\n\n3. **Identify Differences**\n   - What's different between working and broken?\n   - List every difference, however small\n   - Don't assume \"that can't matter\"\n\n4. **Understand Dependencies**\n   - What other components does this need?\n   - What settings, config, environment?\n   - What assumptions does it make?\n\n### Phase 3: Hypothesis and Testing\n\n**Scientific method:**\n\n1. **Form Single Hypothesis**\n   - State clearly: \"I think X is the root cause because Y\"\n   - Write it down\n   - Be specific, not vague\n\n2. **Test Minimally**\n   - Make the SMALLEST possible change to test hypothesis\n   - One variable at a time\n   - Don't fix multiple things at once\n\n3. **Verify Before Continuing**\n   - Did it work? Yes → Phase 4\n   - Didn't work? Form NEW hypothesis\n   - DON'T add more fixes on top\n\n4. **When You Don't Know**\n   - Say \"I don't understand X\"\n   - Don't pretend to know\n   - Ask for help\n   - Research more\n\n### Phase 4: Implementation\n\n**Fix the root cause, not the symptom:**\n\n1. **Create Failing Test Case**\n   - Simplest possible reproduction\n   - Automated test if possible\n   - One-off test script if no framework\n   - MUST have before fixing\n   - Use the `test-driven-development` skill for writing the failing test\n\n2. **Implement Single Fix**\n   - Address the root cause identified\n   - ONE change at a time\n   - No \"while I'm here\" improvements\n   - No bundled refactoring\n\n3. **Verify Fix**\n   - Test passes now?\n   - No other tests broken?\n   - Issue actually resolved?\n\n4. **If Fix Doesn't Work**\n   - STOP\n   - Count: How many fixes have you tried?\n   - If < 3: Return to Phase 1, re-analyze with new information\n   - **If ≥ 3: STOP and question the architecture (step 5 below)**\n   - DON'T attempt Fix #4 without architectural discussion\n\n5. **If 3+ Fixes Failed: Question Architecture**\n\n   **Pattern indicating architectural problem:**\n   - Each fix reveals new shared state/coupling/problem in different place\n   - Fixes require \"massive refactoring\" to implement\n   - Each fix creates new symptoms elsewhere\n\n   **STOP and question fundamentals:**\n   - Is this pattern fundamentally sound?\n   - Are we \"sticking with it through sheer inertia\"?\n   - Should we refactor architecture vs. continue fixing symptoms?\n\n   **Discuss with the user before attempting more fixes**\n\n   This is NOT a failed hypothesis - this is a wrong architecture.\n\n## Red Flags - STOP and Follow Process\n\nIf you catch yourself thinking:\n- \"Quick fix for now, investigate later\"\n- \"Just try changing X and see if it works\"\n- \"Add multiple changes, run tests\"\n- \"Skip the test, I'll manually verify\"\n- \"It's probably X, let me fix that\"\n- \"I don't fully understand but this might work\"\n- \"Pattern says X but I'll adapt it differently\"\n- \"Here are the main problems: [lists fixes without investigation]\"\n- Proposing solutions before tracing data flow\n- **\"One more fix attempt\" (when already tried 2+)**\n- **Each fix reveals new problem in different place**\n\n**ALL of these mean: STOP. Return to Phase 1.**\n\n**If 3+ fixes failed:** Question the architecture (see Phase 4.5)\n\n## Signals You're Doing It Wrong\n\n**Watch for these redirections:**\n- \"Is that not happening?\" - You assumed without verifying\n- \"Will it show us...?\" - You should have added evidence gathering\n- \"Stop guessing\" - You're proposing fixes without understanding\n- \"Ultrathink this\" - Question fundamentals, not just symptoms\n- \"We're stuck?\" (frustrated) - Your approach isn't working\n\n**When you see these:** STOP. Return to Phase 1.\n\n## Common Rationalizations\n\n| Excuse | Reality |\n|--------|---------|\n| \"Issue is simple, don't need process\" | Simple issues have root causes too. Process is fast for simple bugs. |\n| \"Emergency, no time for process\" | Systematic debugging is FASTER than guess-and-check thrashing. |\n| \"Just try this first, then investigate\" | First fix sets the pattern. Do it right from the start. |\n| \"I'll write test after confirming fix works\" | Untested fixes don't stick. Test first proves it. |\n| \"Multiple fixes at once saves time\" | Can't isolate what worked. Causes new bugs. |\n| \"Reference too long, I'll adapt the pattern\" | Partial understanding guarantees bugs. Read it completely. |\n| \"I see the problem, let me fix it\" | Seeing symptoms ≠ understanding root cause. |\n| \"One more fix attempt\" (after 2+ failures) | 3+ failures = architectural problem. Question pattern, don't fix again. |\n\n## Quick Reference\n\n| Phase | Key Activities | Success Criteria |\n|-------|---------------|------------------|\n| **1. Root Cause** | Read errors, reproduce, check changes, gather evidence | Understand WHAT and WHY |\n| **2. Pattern** | Find working examples, compare | Identify differences |\n| **3. Hypothesis** | Form theory, test minimally | Confirmed or new hypothesis |\n| **4. Implementation** | Create test, fix, verify | Bug resolved, tests pass |\n\n## When Process Reveals \"No Root Cause\"\n\nIf systematic investigation reveals issue is truly environmental, timing-dependent, or external:\n\n1. You've completed the process\n2. Document what you investigated\n3. Implement appropriate handling (retry, timeout, error message)\n4. Add monitoring/logging for future investigation\n\n**But:** 95% of \"no root cause\" cases are incomplete investigation.\n\n## Supporting Techniques\n\nThese techniques are part of systematic debugging and available in this directory:\n\n- **`root-cause-tracing.md`** - Trace bugs backward through call stack to find original trigger\n- **`defense-in-depth.md`** - Add validation at multiple layers after finding root cause\n- **`condition-based-waiting.md`** - Replace arbitrary timeouts with condition polling\n\n**Related skills:**\n- **test-driven-development** - For creating the failing test case (Phase 4, Step 1)\n- **verification-before-completion** - Verify the fix worked before claiming success\n\n## Real-World Impact\n\nFrom debugging sessions:\n- Systematic approach: 15-30 minutes to fix\n- Random fixes approach: 2-3 hours of thrashing\n- First-time fix rate: 95% vs 40%\n- New bugs introduced: Near zero vs common\n\nexec\n/bin/zsh -lc 'node --version' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 174ms:\nv26.3.0\n\nexec\n/bin/zsh -lc 'npx tsx --version' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n exited 1 in 732ms:\ntsx v4.22.4\nnode node:net:1986\n      const error = new UVExceptionWithHostPort(rval, 'listen', address, port);\n                    ^\n\nError: listen EPERM: operation not permitted /var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/tsx-501/1394.pipe\n    at Server.setupListenHandle [as _listen2] (node:net:1986:21)\n    at listenInCluster (node:net:2065:12)\n    at Server.listen (node:net:2187:5)\n    at file:///Users/bradleybond/Developer/crystalball/.worktrees/acc401/node_modules/tsx/dist/cli.mjs:53:31537\n    at new Promise (<anonymous>)\n    at createIpcServer (file:///Users/bradleybond/Developer/crystalball/.worktrees/acc401/node_modules/tsx/dist/cli.mjs:53:31515)\n    at async file:///Users/bradleybond/Developer/crystalball/.worktrees/acc401/node_modules/tsx/dist/cli.mjs:55:459 {\n  code: 'EPERM',\n  errno: -1,\n  syscall: 'listen',\n  address: '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/tsx-501/1394.pipe',\n  port: -1\n}\n\nNode.js v26.3.0\n\nexec\n/bin/zsh -lc 'node --import tsx --test src/services/correlation/__tests__/bench-correlation.test.mts' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n exited 1 in 17465ms:\n▶ golden-streams corpus integrity\n  ✔ is deterministic across runs (138.749042ms)\n  ✔ is deterministic across a fresh module registry, not just a warm one (330.567292ms)\n  ✔ keys event pairs injectively, so two pairs cannot share one key (0.209042ms)\n  ✔ emits observations in a stable total order with unique ids (0.381417ms)\n  ✔ digests the corpus at 128 bits, not at a brute-forceable width (79.633542ms)\n  ✔ frames each record by length, so a regrouping of the corpus is not free (0.117083ms)\n  ✔ never plants a true pair on a decoy event (0.120209ms)\n  ✔ uses a pair key that survives the correlation-outcomes id split (0.059833ms)\n✔ golden-streams corpus integrity (552.967625ms)\n▶ what the corpus is built to stress\n  ✔ recovers every planted causal coupling (0.149291ms)\n  ✔ resists the base-rate trap (chatty independent stream) (0.078ms)\n  ✔ still exposes the confounded burst pair (0.054584ms)\n  ✔ still exposes the transitive mediated edge (0.038458ms)\n  ✔ reports no positive edge on the inhibitory pair (0.031375ms)\n  ✔ reports explicit offline replay, correction-family, and inhibitory evidence (62.278875ms)\n  ✔ re-derives inhibitory truth and summaries from the row ledger (73.281125ms)\n  ✔ leaks zero near-miss decoy pairs (0.743125ms)\n  ✔ grades every significant edge against planted truth (0.600833ms)\n  ✔ separates real couplings from noise on evidence, not on strength (0.136791ms)\n  ✔ counts every non-causal significant edge as a false edge (0.054625ms)\n  ✔ measures pair precision on distinct pairs, not on raw emissions (0.088333ms)\n  ✔ does not double-count one event pair matched by two rules (0.757541ms)\n  ✔ computes pair precision through the production denominator selection (0.092042ms)\n  ✔ scores true pairs with a real confidence, not a placeholder (0.040166ms)\n✔ what the corpus is built to stress (139.654083ms)\n▶ planted-coupling bookkeeping\n  ✔ declares at least one coupling of every kind the grader can emit (0.080167ms)\n  ✔ gives every planted coupling a rationale and a unique direction (0.071416ms)\n  ✔ describes every golden stream (0.099667ms)\n✔ planted-coupling bookkeeping (0.308375ms)\n▶ the committed baseline\n  ✔ passes against a live run (144.631833ms)\n  ✔ carries its own tolerance block (0.298625ms)\n  ✔ records why the numbers look the way they do (0.098667ms)\n✔ the committed baseline (145.093875ms)\n▶ the gate\n  ✔ fails closed on corpus drift and reports nothing else (2.933042ms)\n  ✔ fails on a coupling-precision regression past tolerance (14.977416ms)\n  ✔ passes silently on improvement — tolerances are one-sided (14.906875ms)\n  ✔ treats decoy leakage as zero-tolerance (15.094958ms)\n  ✔ fails when learned rules spray more pairs than the tolerance allows (16.049583ms)\n  ✔ fails closed when a corpus edit leaves the three counts intact (2.809542ms)\n  ✔ fails closed on a NaN metric instead of passing every directional check (3.198917ms)\n  ✔ fails closed when the committed baseline is missing a gated field (2.992584ms)\n  ✔ fails when the confidence kernel collapses under passing membership gates (14.215875ms)\n  ✔ fails when the learned-rule pipeline disappears entirely (7.02175ms)\n  ✔ fails on discrete false-edge growth the precision ratio would absorb (14.738083ms)\n  ✔ scores a perfect miner as an improvement, not as the largest regression (8.249125ms)\n  ✔ retains a null survivor separation as witnessed evidence (6.943042ms)\n  ✔ fails closed when a gated metric is missing outright, not just null (3.123958ms)\n  ✔ rejects a perfect-miner claim its own breakdown contradicts (3.869667ms)\n  ✔ fails when learned rules are synthesized but never fire (7.572458ms)\n  ✔ fails closed on a non-numeric tolerance instead of disarming its gate (0.111542ms)\n  ✔ rejects an unknown tolerance key rather than ignoring it (0.057917ms)\n  ✔ rejects an inherited-property tolerance key, which `in` would have accepted (0.05725ms)\n  ✔ fails when the causal rules go nearly quiet, not just exactly silent (7.285083ms)\n  ✔ rejects a perfect-miner claim that zeroes the breakdown too (3.42175ms)\n  ✔ rejects a coupling precision its own edge counts contradict (4.067542ms)\n  ✔ rejects perfect pair rates emitted over zero pairs (3.017ms)\n  ✔ fails when built-in pair emissions shrink at all (21.2635ms)\n  ✔ rejects impossible metric values instead of scoring them as improvements (4.620167ms)\n  ✔ rejects a fractional count (5.523541ms)\n  ✔ rejects a baseline that seeds a gate at zero (2.872375ms)\n  ✔ rejects a tolerance block that is not an object (0.176292ms)\n  ✔ rejects a tolerance block missing a gate rather than defaulting it (0.065792ms)\n  ✔ rejects a tolerance wide enough to absorb the whole measurement (0.106458ms)\n  ✔ rejects positive miner rates carried by an empty edge ledger (3.655958ms)\n  ✔ reconciles the learned-rule summaries against the edge rows that claim them (4.699ms)\n  ✔ reconciles coupling recall against the couplings it names as missing (8.247958ms)\n  ✔ rejects pair rates whose two routes to the true-emission count disagree (6.593333ms)\n  ✔ bounds the evidence separation instead of scoring a fabricated one (5.021208ms)\n  ✔ range-checks each false-positive component, not just their sum (7.347458ms)\n  ✔ fails when one causal rule goes dark inside a healthy aggregate (28.882208ms)\n  ✔ rejects a baseline that re-seeds the separation gate at zero (6.875083ms)\n  ✔ rejects a per-rule tally that does not add up to its own summary (7.181458ms)\n  ✔ accumulates every independent regression rather than short-circuiting (30.545833ms)\n✔ the gate (301.693833ms)\n▶ the gate rejects a run or a baseline that could not have happened\n  ✔ rejects a baseline written against an older schema (0.285ms)\n  ✔ rejects a corpus digest missing from BOTH operands (0.124042ms)\n  ✔ rejects a malformed digest before comparing it (0.062459ms)\n  ✔ rejects a baseline metric sitting at or below its own tolerance (4.796ms)\n  ✔ rejects a baseline whose learned-rule split exceeds its own rule count (6.571625ms)\n  ✔ rejects a rate that no whole number of hits could produce (7.050792ms)\n  ✔ rejects a report claiming fewer mined edges than it called significant (5.813459ms)\n  ✔ rejects stub edge rows that only carry the two fields the gate used to read (5.9495ms)\n  ✔ rejects a separation the edge rows do not derive (6.446625ms)\n  ✔ rejects a pair ledger padded with a duplicate key (5.091333ms)\n  ✔ rejects pair summaries the ledger does not reproduce (6.029916ms)\n  ✔ rejects a pair ledger whose keys name pairs that do not exist (5.813625ms)\n  ✔ rejects a pair row whose endpoints do not build its key (5.198625ms)\n  ✔ rejects a pair row that relabels a true pair as a miss (5.282458ms)\n  ✔ rejects an edge row whose verdict contradicts the planted corpus (7.369625ms)\n  ✔ rejects an edge row pointing at events the corpus never coupled (6.807167ms)\n  ✔ rejects the same directed pair reported twice at two lag windows (6.260709ms)\n  ✔ rejects a false-positive breakdown that reassigns rows between categories (6.05525ms)\n  ✔ rejects a baseline that drops rules out of the pinned inventory (10.656583ms)\n  ✔ gates the mined-candidate count, so a collapsed miner cannot read as clean (29.217084ms)\n  ✔ rejects a baseline that seeds a decoy emission as acceptable (32.115417ms)\n  ✔ accepts the committed baseline against a live run (30.03875ms)\n✔ the gate rejects a run or a baseline that could not have happened (194.142375ms)\n▶ the gate rejects a conclusion the report authored about itself\n  ✔ rejects a dead pass B that kept its four counters (6.1445ms)\n  ✔ rejects a learned-pair row attributed to a rule pass B never installed (7.767792ms)\n  ✔ rejects a learned-pair row whose key does not match its own endpoints (8.266292ms)\n  ✔ rejects a causal-rule roster widened past the causal edge rows (6.964959ms)\n  ✔ rejects pair emissions attributed to a rule the graded pass never registered (7.787084ms)\n  ✔ rejects a run that ships a built-in rule with no coverage probe (6.690541ms)\n  ✔ rejects a probe whose positive fixture did not match (6.483ms)\n  ✔ rejects a probe whose near-miss was accepted (6.457375ms)\n  ✔ rejects a ledger whose confidences are all a saturated constant (7.687166ms)\n  ✔ rejects a baseline that moves false positives between categories (29.196292ms)\n  ✔ rejects an edge row at a window the miner is not configured for (8.230458ms)\n  ✔ rejects a null lift reported alongside a finite z-score (6.079125ms)\n  ✔ rejects a coverage claim that does not match the pair ledger (7.007ms)\n✔ the gate rejects a conclusion the report authored about itself (115.740542ms)\n▶ the gate re-derives the numbers it used to take on the report's word\n  ✔ rejects a mined-candidate count the corpus could not have produced (13.731042ms)\n  ✔ rejects a padded rule inventory (5.780833ms)\n  ✔ rejects a coverage probe for a rule the engine never registered (6.907042ms)\n  ✔ rejects fabricated edge evidence that clears every per-row floor (30.495208ms)\n  ✔ rejects a pair ledger that attributes every emission to the wrong rule (37.477833ms)\n  ✔ re-runs the rule probes instead of reading what they reported (6.355042ms)\n  ✔ rejects a report that drops an advertised measurement outright (29.324667ms)\n  ✔ rejects a baseline whose candidate population sits below its own edges (7.200459ms)\n  ✔ rejects a padded roster on the baseline side too (7.048709ms)\n  ✔ rejects an edge between domains the corpus never observed (6.952791ms)\n  ✔ still accepts the untouched live run (30.374666ms)\n✔ the gate re-derives the numbers it used to take on the report's word (182.548375ms)\n▶ the committed report digest anchors the producer itself\n  ✔ pins the live run, and re-pins it identically (129.475709ms)\n  ✔ moves when a producer collapses 101 learned-pair rows to four (4.2955ms)\n  ✔ moves when a producer deranges pair attribution (13.58125ms)\n  ✔ moves when a producer forges probe text or edge evidence (25.060083ms)\n  ✔ survives the last-bit noise a cross-platform re-run can produce (10.066583ms)\n  ✔ moves when a producer deletes or forges a SUMMARY field (35.737875ms)\n  ✔ moves on ANY single leaf of the report, exhaustively (13130.703959ms)\n  ✔ sees a change five orders of magnitude below the old rounding step (5.957584ms)\n  ✔ reports the mismatch as a re-seed, not as a quality regression (14.820167ms)\n  ✔ rejects a baseline that pins the ledger to nothing (8.85075ms)\n✔ the committed report digest anchors the producer itself (13379.402333ms)\n▶ the reproduction walk demands a plain, own-keyed report\n  ✔ rejects a report whose fields come from its prototype (8.463833ms)\n  ✔ rejects a field deleted and re-supplied by a prototype (14.361792ms)\n  ✔ rejects an extra own field, including one holding undefined (13.649834ms)\n  ✔ rejects a ledger array carrying extra own properties (15.158042ms)\n  ✔ rejects symbol-keyed properties the key walk never reports (13.747958ms)\n  ✔ rejects a field that is an accessor rather than a stored value (14.107916ms)\n  ✔ rejects a non-enumerable own field the key walk would skip (13.964917ms)\n  ✔ still accepts the array length every real ledger carries (67.951541ms)\n  ✔ distinguishes -0 from 0 (14.207666ms)\n✔ the reproduction walk demands a plain, own-keyed report (175.966875ms)\n▶ a perfect miner can become the next baseline\n  ✔ accepts a null separation on the committed side when no false edges exist (15.738ms)\n  ✔ still fails the live run on the false edges it grew (15.358625ms)\n  ✔ rejects a null separation the baseline has not earned (3.13225ms)\n  ✔ allows a perfect baseline exactly zero free false edges (15.069125ms)\n  ✔ leaves the ordinary tolerance alone for an imperfect baseline (14.421833ms)\n  ✔ rejects a separation the baseline could not have measured (3.3735ms)\n✔ a perfect miner can become the next baseline (67.316459ms)\n▶ the re-seed emitter produces a baseline that passes\n  ✔ round-trips the live run into a baseline the gate accepts (24.58825ms)\n  ✔ pins every v13 replay, family, candidate, inhibition, and stream identity field (10.511417ms)\n  ✔ reproduces the committed baseline field for field (10.756084ms)\n  ✔ copies the note and tolerances it is handed rather than generating them (10.503ms)\n  ✔ cannot launder a tolerance past its ceiling by re-seeding through it (10.488167ms)\n  ✔ emits JSON — no undefined, no non-finite, nothing that vanishes on write (24.634917ms)\n✔ the re-seed emitter produces a baseline that passes (91.67625ms)\n▶ the re-seed guard compares against the previous baseline\n  ✔ refuses a candidate that regresses beyond the previous one-sided tolerance (25.188709ms)\n  ✔ allows a same-corpus improvement while replacing only reviewed exact anchors (24.882ms)\n  ✔ fails closed when the previous baseline is malformed (0.197166ms)\n  ✔ refuses corpus drift instead of comparing incomparable measurements (13.166917ms)\n  ✔ keeps the built-in rule inventory pinned across a reseed (13.078459ms)\n  ✔ refuses to reseed when a previously covered built-in rule goes dark (0.164666ms)\n  ✖ makes --seed refuse a regression before emitting candidate JSON (2.131166ms)\n  ✔ directs every tracked-baseline replacement through a temporary candidate file (0.776125ms)\n  ✔ guards a changed PR baseline against the base branch before the normal CI gate (0.636625ms)\n✖ the re-seed guard compares against the previous baseline (80.4745ms)\n▶ the one-shot v11 to v12 migration gate\n  ✔ accepts only the pinned S9-only transition with all new gates armed (0.637625ms)\n  ✔ rejects a migrated report whose learned execution path collapses behind one survivor (0.257334ms)\n  ✔ rejects one dark causal learned rule even when aggregate learned volume stays healthy (0.300542ms)\n  ✔ rejects collapsed built-in engine volume even when rates remain unchanged (0.221042ms)\n  ✔ fails closed on altered previous anchors, tolerances, manifest, family, or inhibition (2.782541ms)\n✔ the one-shot v11 to v12 migration gate (4.295083ms)\n▶ rule probes pin the fixture, not only the verdict\n  ✔ gives every probe a well-formed fixture digest (0.147625ms)\n  ✔ gives every rule a DIFFERENT fixture digest (0.0545ms)\n  ✔ covers more than one clause on every rule (0.0665ms)\n  ✔ moves when a near-miss is re-aimed at a different clause (0.640125ms)\n  ✔ moves when the near-miss RATIONALE is rewritten (0.77775ms)\n  ✔ moves when a clause is dropped from the probe outright (0.671958ms)\n  ✔ moves on any single field of a positive fixture (1.939375ms)\n  ✔ records the edge type and direction the positive fixture actually emitted (0.167083ms)\n  ✔ holds still when only the KEY ORDER of a fixture event changes (0.627625ms)\n  ✔ refuses a probe carrying no fixture digest (2.683083ms)\n  ✔ names the probe ledger — not the whole report — when a fixture moves (2.891292ms)\n✔ rule probes pin the fixture, not only the verdict (10.83925ms)\n▶ the pair ledger keeps direction and edge type\n  ✔ records a known edge type and this pair\"s own endpoints per emission (0.166917ms)\n  ✔ at least one emission is directed against the sorted key order (0.051875ms)\n  ✔ moves the report digest when every emission is INVERTED (5.732292ms)\n  ✔ moves the report digest when an edge type is rewritten (5.930625ms)\n  ✔ refuses an emission pointing at events that are not this pair (3.184916ms)\n  ✔ refuses an edge type the engine cannot produce (3.5095ms)\n✔ the pair ledger keeps direction and edge type (18.738042ms)\n▶ the baseline itemises what the digest anchors\n  ✔ commits the advertised measurements by value (4.779625ms)\n  ✔ refuses a baseline with no witnessed block at all (10.238459ms)\n  ✔ NAMES the moved measurement instead of only reporting a digest (14.414417ms)\n  ✔ names the ledger that moved, not just that something did (14.563042ms)\n✔ the baseline itemises what the digest anchors (44.163166ms)\n▶ round 13 — the halves round 12 missed\n  ✔ moves the digest when LEARNED rules invert their assertion (6.125666ms)\n  ✔ moves the digest when a learned emission is REVERSED (5.706042ms)\n  ✔ rejects a learned emission whose endpoints are not this row\"s pair (3.529292ms)\n  ✔ moves the digest when a rule the CORPUS never exercises inverts (5.669167ms)\n  ✔ refuses a probe reporting an edge type the engine cannot emit (3.006917ms)\n  ✔ refuses a probe that reports no direction for its positive (2.609958ms)\n  ✔ gives every clause a fixture that differs from the positive (0.22275ms)\n  ✔ refuses a probe carrying fewer than two clauses (2.833166ms)\n  ✔ refuses a probe that pads its clause count by repeating one (2.790083ms)\n  ✔ rejects the comma-join forgery of a witnessed list (13.739958ms)\n✔ round 13 — the halves round 12 missed (46.476792ms)\n▶ round 14 — the gate stops taking the producer at its word\n  ✔ re-derives every probe verdict independently of the producer (3.012875ms)\n  ✔ fails when the report claims a near-miss the engine actually matched (2.911625ms)\n  ✔ the verdicts do not depend on the instant they are graded at (5.244125ms)\n  ✔ every rule matches its positive fed BACK TO FRONT (0.111792ms)\n  ✔ fails when a reversed positive stops matching (2.7385ms)\n  ✔ exercises every accepted branch of every disjunctive matcher (0.210875ms)\n  ✔ fails when one accepted branch of a disjunction stops matching (2.658292ms)\n  ✔ moves the digest when a pair is emitted at the epoch (9.2355ms)\n  ✔ fails when the confidence BREAKDOWN disappears behind the scalar (3.182875ms)\n  ✔ projects every pair into the evidence graph the store really builds (0.146958ms)\n  ✔ fails when the projection contradicts the engine it came from (3.045333ms)\n  ✔ proves a retired learned rule actually leaves the engine (0.067542ms)\n  ✔ fails when a retired rule stays registered but is reported removed (2.472417ms)\n  ✖ refuses to re-seed against a --previous-baseline whose tolerance the live run actually violates (0.671667ms)\n  ✖ seeds cleanly against a --previous-baseline the live run genuinely satisfies (0.495416ms)\n✖ round 14 — the gate stops taking the producer at its word (36.53425ms)\n▶ round 15 — the v12→v13 schema bump is a pinned migration, not a skipped check\n  ✔ reconstructs the reviewed v12 payload exactly — the pin says so (11.301958ms)\n  ✔ refuses a previous baseline whose payload is not the reviewed one (20.848ms)\n  ✔ sees a forbidden field that was DROPPED, not just moved (22.068208ms)\n  ✔ sees a field the reviewed baseline HAD that the live candidate no longer produces at all (21.184958ms)\n  ✔ accepts the real additive transition (21.490416ms)\n  ✔ refuses a manifest that is not the pinned one (18.513208ms)\n  ✔ refuses a source that is not the reviewed v12 baseline (22.300458ms)\n  ✔ refuses a source whose corpus digest is not the reviewed corpus (22.020041ms)\n  ✔ refuses a graded metric that moved under cover of the schema bump (22.128791ms)\n  ✔ refuses a report digest that did not move — nothing was added (21.533042ms)\n  ✔ pins the migration to exactly one hop (0.135792ms)\n  ✔ the runtime one-hop check is real code, not just a test-level assertion (21.976ms)\n  ✔ oneHopSchemaVersionCheckReason refuses a manifest that no longer targets the compiled schema (0.09925ms)\n  ✔ oneHopSchemaVersionCheckReason accepts a manifest that targets the compiled schema (0.042459ms)\n  ✔ refuses a caller-supplied manifest whose toSchemaVersion alone drifts from the compiled schema — validator wiring, not just the helper (21.973084ms)\n  ✔ refuses a previous baseline whose tolerances are not the reviewed ones — the shared-gate forgery Codex found (32.283167ms)\n  ✔ accepts the real additive transition with its real, unforged tolerances (10.37ms)\n✔ round 15 — the v12→v13 schema bump is a pinned migration, not a skipped check (290.7535ms)\nℹ tests 223\nℹ suites 20\nℹ pass 220\nℹ fail 3\nℹ cancelled 0\nℹ skipped 0\nℹ todo 0\nℹ duration_ms 17290.257584\n\n✖ failing tests:\n\ntest at src/services/correlation/__tests__/bench-correlation.test.mts:1:65937\n✖ makes --seed refuse a regression before emitting candidate JSON (2.131166ms)\n  Error: EPERM: operation not permitted, mkdtemp '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/crystalball-correlation-reseed-XXXXXX'\n      at mkdtempSync (node:fs:3034:18)\n      at TestContext.<anonymous> (/Users/bradleybond/Developer/crystalball/.worktrees/acc401/src/services/correlation/__tests__/bench-correlation.test.mts:2472:17)\n      at Test.runInAsyncScope (node:async_hooks:226:14)\n      at Test.run (node:internal/test_runner/test:1382:25)\n      at Suite.processPendingSubtests (node:internal/test_runner/test:960:18)\n      at Test.postRun (node:internal/test_runner/test:1522:19)\n      at Test.run (node:internal/test_runner/test:1447:12)\n      at async Suite.processPendingSubtests (node:internal/test_runner/test:960:7) {\n    errno: -1,\n    code: 'EPERM',\n    syscall: 'mkdtemp',\n    path: '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/crystalball-correlation-reseed-XXXXXX'\n  }\n\ntest at src/services/correlation/__tests__/bench-correlation.test.mts:1:88233\n✖ refuses to re-seed against a --previous-baseline whose tolerance the live run actually violates (0.671667ms)\n  Error: EPERM: operation not permitted, mkdtemp '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/acc501-broken-tolerance-XXXXXX'\n      at mkdtempSync (node:fs:3034:18)\n      at TestContext.<anonymous> (/Users/bradleybond/Developer/crystalball/.worktrees/acc401/src/services/correlation/__tests__/bench-correlation.test.mts:3317:17)\n      at Test.runInAsyncScope (node:async_hooks:226:14)\n      at Test.run (node:internal/test_runner/test:1382:25)\n      at Suite.processPendingSubtests (node:internal/test_runner/test:960:18)\n      at Test.postRun (node:internal/test_runner/test:1522:19)\n      at Test.run (node:internal/test_runner/test:1447:12)\n      at async Suite.processPendingSubtests (node:internal/test_runner/test:960:7) {\n    errno: -1,\n    code: 'EPERM',\n    syscall: 'mkdtemp',\n    path: '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/acc501-broken-tolerance-XXXXXX'\n  }\n\ntest at src/services/correlation/__tests__/bench-correlation.test.mts:1:89115\n✖ seeds cleanly against a --previous-baseline the live run genuinely satisfies (0.495416ms)\n  Error: EPERM: operation not permitted, mkdtemp '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/acc501-satisfied-tolerance-XXXXXX'\n      at mkdtempSync (node:fs:3034:18)\n      at TestContext.<anonymous> (/Users/bradleybond/Developer/crystalball/.worktrees/acc401/src/services/correlation/__tests__/bench-correlation.test.mts:3351:17)\n      at Test.runInAsyncScope (node:async_hooks:226:14)\n      at Test.run (node:internal/test_runner/test:1382:25)\n      at Suite.processPendingSubtests (node:internal/test_runner/test:960:18)\n      at Test.postRun (node:internal/test_runner/test:1522:19)\n      at Test.run (node:internal/test_runner/test:1447:12)\n      at async Suite.processPendingSubtests (node:internal/test_runner/test:960:7) {\n    errno: -1,\n    code: 'EPERM',\n    syscall: 'mkdtemp',\n    path: '/var/folders/qt/hq7cydbd06d6g0yksx3mxm3c0000gn/T/acc501-satisfied-tolerance-XXXXXX'\n  }\n\nexec\n/bin/zsh -lc 'npm run typecheck:all' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 15874ms:\n\n> crystal-ball@2.25.147 typecheck:all\n> tsc --noEmit && tsc --noEmit -p tsconfig.api.json\n\n\ncodex\nThe fallback runner completed all 223 tests: 220 assertions passed; the only three failures were filesystem setup calls rejected by the read-only sandbox (`mkdtemp ... EPERM`), not assertion failures. The entire round-15 migration suite—including the public-validator wiring test—passed. `npm run typecheck:all` completed with exit 0. I’m doing the final diff/arithmetic consistency pass now.\nexec\n/bin/zsh -lc 'git diff 235331b2 2c6e8834 --check' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 111ms:\nexec\n/bin/zsh -lc 'git diff --exit-code' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'git status --short --branch' in /Users/bradleybond/Developer/crystalball/.worktrees/acc401\n succeeded in 134ms:\n## claude/acc-501-round-14...origin/claude/acc-501-round-14\n\ncodex\nBoth round-19 fixes are accurate and complete.\n\n## QA/QC Report\n\nRisk tier: High\n\nChecks passed:\n\n- Docstring now correctly describes public-validator reachability.\n- Mutation result is arithmetically correct: `223 tests / 222 pass / 1 fail`.\n- All other documented pass/fail totals reconcile; historical reachability claims are scoped or explicitly superseded by round 18b.\n- `npm run typecheck:all` — exit 0.\n- Diff check and worktree status — clean.\n\nChecks limited by sandbox:\n\n- `npx tsx --test ...` could not start because `tsx` IPC received `EPERM`.\n- Equivalent `node --import tsx --test ...` discovered 223 tests: 220 passed; three filesystem-dependent tests were blocked by `mkdtemp EPERM`. All migration-guard tests passed.\n\nFindings: none.\n\nResidual risk: three temporary-file CLI tests could not execute behaviorally in this read-only environment.\n\nVerdict: APPROVE\ntokens used\n110,764\nBoth round-19 fixes are accurate and complete.\n\n## QA/QC Report\n\nRisk tier: High\n\nChecks passed:\n\n- Docstring now correctly describes public-validator reachability.\n- Mutation result is arithmetically correct: `223 tests / 222 pass / 1 fail`.\n- All other documented pass/fail totals reconcile; historical reachability claims are scoped or explicitly superseded by round 18b.\n- `npm run typecheck:all` — exit 0.\n- Diff check and worktree status — clean.\n\nChecks limited by sandbox:\n\n- `npx tsx --test ...` could not start because `tsx` IPC received `EPERM`.\n- Equivalent `node --import tsx --test ...` discovered 223 tests: 220 passed; three filesystem-dependent tests were blocked by `mkdtemp EPERM`. All migration-guard tests passed.\n\nFindings: none.\n\nResidual risk: three temporary-file CLI tests could not execute behaviorally in this read-only environment.\n\nVerdict: APPROVE"
}
