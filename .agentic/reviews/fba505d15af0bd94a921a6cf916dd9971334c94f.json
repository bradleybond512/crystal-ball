{
  "reviewedSha": "fba505d15af0bd94a921a6cf916dd9971334c94f",
  "reviewer": "claude",
  "verdict": "approve",
  "blockingFindings": 0,
  "reviewedAt": "2026-08-31T16:05:31.750Z",
  "evidence": "I have completed the review. Here are my findings.\n\n## Findings\n\n### Scope\nDiff `76605bd4..fba505d1` (branch `codex/ux017-main-sync-verification` @ `fba505d1`): 3 code files (`scripts/sync-main-to-mac.mjs`, `scripts/setup-main-sync-agent.mjs`, `tests/main-sync-agent.test.mjs`) + 4 docs. Core change is pinning the main-sync build to the Node 22 toolchain with a sibling `npm`.\n\n### Node 22 / sibling npm pinning — sound, fail-closed\n- `assertSupportedMainSyncNode` (`sync-main-to-mac.mjs:30`) rejects any major ≠ 22; `buildMainSyncToolchain:41` requires an absolute Node path and non-empty PATH; `validatePinnedNodeToolchain:59` additionally `access(npmPath, X_OK)`-checks the sibling npm and throws if absent (`:65`).\n- Two-layer enforcement: setup validates the major *before* writing the plist (`setup-main-sync-agent.mjs:147` ahead of `installLaunchAgent:148`), and the plist pins `nodePath: process.execPath` (`:124`) — the same validated runtime; the scheduled run re-validates via `validatePinnedNodeToolchain` (`sync…:445`).\n- `runVerificationAndBuild:410` now shells `toolchain.npmPath` with `toolchain.env` (nodeDir prepended). The new PATH is a strict superset of the old (`:54`), so no build regression — it only removes the mixed-runtime ambiguity that caused npm to resolve under Node 26. Launch-agent PATH stays Cargo-first (`:37`), Node-first applies only to npm children — correct, and asserted at `tests:277`.\n\n### Failure-status behavior — correct loud/soft split\n- Toolchain-invalid and build failures throw plain `Error` → catch writes `phase: 'failed'` and rethrows → CLI `process.exit(1)` (`:539-563`). Required-checks-not-green throws `SyncBlockedError` → `phase: 'blocked'`, returns exit 0 (`:546-549`). The distinction is right: real breakage is loud to launchd, not-yet-green main is a soft skip.\n\n### Mutation evidence — genuine, not stale/fabricated\n- `docs/validation/UX-017-MUTATION-PROOFS.md` records 9 mutants each flipping one guard and showing the specific TAP failure. The recorded final-green checksums are captured at `697ff1ef`, which is *not* in HEAD's ancestry (rebased history) — but I verified the three files are byte-identical between `697ff1ef` and `fba505d1` (empty `git diff`), so the checksums validly describe this SHA.\n- The test is wired into the required `targeted-tests` check via `test:data` (`targeted-tests.mjs:49,59` → `package.json:71`), so the guards are enforced, not just documented.\n\n### Exact-commit validation — intact, fail-closed\n- `findMergedPullRequestForCommit:317` requires `merge_commit_sha === sha` exactly; `readMergedPullRequestCheckStates:345` re-requires `mergeCommit.oid === sha`; `evaluateRequiredChecks:288` treats any missing/non-success required check as failure. Unchanged by this diff and still the real install gate.\n\n### Controller/build-clone separation & rollback — code-correct, operationally gated\n- The build always runs in the disposable `~/.crystalball-main-sync/repo` (fresh clone → `reset --hard` → `clean -fdx`, `:229-250`); the controller checkout is never reset/cleaned/updated. Evidence doc (`UX-017-MAIN-SYNC-EVIDENCE.md:299-313`) prescribes the durable controller at `…/controller`, distinct from `repo`, detached at the reviewed SHA.\n\n### Fail-closed log evidence — exemplary\n- The doc explicitly *rejects* its own `if … rg … else` EBADENGINE check as fail-open (a failed `tail`/rotated log takes the same `else`, `:276-280`) and mandates an inode+size+`awk` fail-closed replacement (`:284-295`). This is the correct posture.\n\n### Non-blocking observations (not defects)\n1. **Live launchd binding is still the disposable worktree** (`:181` working dir = `.worktrees/codex-ux017-main-sync-verification`). The branch itself flags this as temporary and gates relocation on PR #1693 (`:198-204`). Out of scope of this diff, but this is the one operational item not to lose track of post-merge.\n2. **Two tests are Node-major-dependent** (`tests:147` version test and the CLI-failure test at `tests:191`, which relies on the runner being Node 22 to reach the npm-sibling branch). CI is uniformly Node 22 (`.nvmrc`=22; every workflow `node-version: 22`), so this is safe in the gate; only a local run under a different major would show a confusing failure.\n3. **Setup validates the Node major but not the npm sibling** (`setup…:147`), so a Node-22-without-sibling-npm would write a plist that then fail-closes on every scheduled run. Fine (fail-closed), but could be caught earlier via `validatePinnedNodeToolchain` in setup. Enhancement only.\n4. **CLI-failure test copies `libnode*.dylib`** (`tests` fixture) — self-contained on official setup-node builds (CI), potentially fragile on split installs locally. Minor test-fragility, not a product hazard.\n\n## Conclusion\n\n**Zero blocking findings remain for `fba505d15af0bd94a921a6cf916dd9971334c94f`.** The toolchain pinning is fail-closed and two-layered, failure statuses are correct, exact-commit gating is intact, the durable controller / disposable build clone are properly separated in code, and the log-evidence procedure is explicitly fail-closed. The four items above are non-blocking (operational follow-up #1 tracked under PR #1693; the rest are enhancements/test-hygiene). I approve **only** this exact SHA; any later commit invalidates this verdict."
}
