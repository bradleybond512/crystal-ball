# PR1650 happy-dom dependency refresh

Objective: refresh the existing happy-dom dependency PR against current main without carrying its obsolete lockfile churn. Development DOM implementation changes must preserve application tests and real browser behavior.

Initial comparison base: canonical main `382fff7fe6657e8fc8beb8cb786a70c032385f77`. Final integration base: `97faf76e0cfe8cdc414c7d96cb0c4c606124ee27`, including the separately reviewed UX-060 Welcome focus fix. Scope: package.json, package-lock.json, this brief and evidence only. Owner: native_dependency_validation; independent review and publication owned by parent. Other agents share the repository; isolated worktree required.

Approved design: use Node22/current npm with committed .npmrc; target `happy-dom@20.14.0` via package-lock-only update, direct range ^20.14.0. Reject unrelated semantic lock changes. No production code, test assertions, panel/a11y baselines, permissions, provider, native installation, or release changes. Existing PR1650 remains the delivery vehicle.

Acceptance: npm ci and lock validation; both TypeScript configs; full renderer tests; per-panel smoke with retained old/new reports; panel fixtures, delegation, component-library and direct DOM suites; real full-variant accessibility, settings keyboard and Home shell browser tests; explicit full/tech/finance/happy identity/theme coverage and builds; unchanged full bundle budgets; agentic gate and independent review. The smoke harness forces full and does not prove all-variant runtime coverage. Existing theme-toggle.spec.ts explicitly skips non-happy variants; report skips honestly and use applicable system-theme coverage where present.

Failure handling: reproduce against the same main with old happy-dom before attributing any failure. Preserve all assertions and baselines. Behavior repair requires a separate scoped design. No mutation proof is required for a dependency-only change; any later behavioral repair requires one.

Risk: medium development dependency update may alter DOM scheduling, computed styles, selectors and event behavior. It does not enter the production bundle directly. Unknowns: existing DOM/browser baseline failures, exact transitive lock changes and environment resource limits. License/maintenance: MIT upstream Happy DOM; existing maintained dependency, no new package introduced. Rollback: restore the two manifest files and npm ci. No native app install.
