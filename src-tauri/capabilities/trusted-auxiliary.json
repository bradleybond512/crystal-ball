{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "trusted-auxiliary",
  "description": "Capabilities for Crystal Ball settings and live-channel windows",
  "windows": ["settings", "live-channels"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
