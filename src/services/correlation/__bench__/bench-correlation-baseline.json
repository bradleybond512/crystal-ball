{
  "schemaVersion": 9,
  "seededAt": "2026-08-03",
  "note": "ACC-501 seed run against the UNCORRECTED miner: significantEdges() gates on fixed thresholds (minLift 2, minZ 2, minSupport 3) with no multiple-comparisons correction and no de-clustering, so 22 of 256 mined edges clear and only 5 are real — coupling precision 22.7%. Recall is 1.0 and the engine's built-in rules are perfect on event-level truth (precision 1.0, recall 1.0, zero decoy leakage), so every unit of headroom here is in the miner, not the rules. The 14 unplanted edges split into two kinds: 6 are a systematic near-coincidence (the S2 grid-storm anchors sit a fixed ~0.5d before the S8 chain triplets, so space/infra -> chain-x/y/z score z=4.63 and will SURVIVE a Bonferroni correction), and 8 are weak incidental noise (z 2.5-4.8) that a correction should kill. Two real couplings (macro->maritime, space->infra) were evicted at MAX_LEARNED_RULES by burst artefacts, which is what edgeStrengthSeparation=0.05 measures: `strength` saturates at 1.0, so the ranking that decides the cap is nearly flat. ACC-502 owns the correction, ACC-503 the mediated edge, ACC-504 the burst artefacts. schemaVersion 2 added corpusDigest (content identity, not counts), falseEdgeCount + causalLearnedRuleCount + meanTruePairConfidence gates, and fail-closed numeric validation. schemaVersion 3 hardened the digest against array-boundary ambiguity (JSON-encoded, not delimiter-joined), added causalLearnedRulePairCount as a LIVENESS gate (synthesized rules that never fire), validates the tolerance block itself, and cross-checks the report against its own breakdown fields. schemaVersion 4 answers a third review round that demonstrated seven live PASS-on-nothing paths: the digest is 128-bit (a 32-bit preimage was found in seconds), the causal-pair liveness gate is proportional rather than exactly-zero, the edge and pair summaries reconcile against the row-level ledger and the derived precision ratio, rates and counts are range-checked (pairPrecision 2 used to read as an improvement), the baseline must ARM every gate it feeds (no zero seeds), and the tolerance block must be a complete object rather than a partial overlay on compiled defaults. schemaVersion 5 answers a fourth round that found eight more: the digest is LENGTH-PREFIXED (a separator byte is just another code unit, so two records collided with one concatenated record at any width — this is why the digest moved), every tolerance has a ceiling (a legal-looking block of wide tolerances disarmed every liveness gate at once), an empty edge ledger can no longer carry positive miner rates, coupling recall reconciles against the named missingCouplings and the learned-rule summaries against edges[].becameLearnedRule, pair precision and recall must agree on the true-emission count they both imply and cannot exceed the truePairUniverse, the causal-pair liveness floor now applies PER RULE (7/6/6 summing to 19 hid one dead matcher inside a 9.5 aggregate floor), separation is bounded to ±48 and each false-positive component is range-checked, and edgeEvidenceSeparation joined the must-arm set — see bench-correlation-baseline.ts. schemaVersion 6 answers a fifth and sixth round in which the LEDGERS self-attested: rows carried their own verdict, their own isTruePair flag and their own decoyEmissions, and the gate trusted them, so a ledger of fabricated endpoints reconciled with every summary it was generated alongside — the gate now imports planted truth directly from __bench__/golden-streams.ts and derives each verdict, each true-pair flag and each decoy tally itself. schemaVersion 7 answers a seventh round that found the conclusions still authored by the pass that summarized them: pass B reported four learned-rule pair counters with no rows at all (a dead synthesize->install->match path PASSED), pair ruleIds were only checked for non-emptiness (every emission relabelled not-a-registered-rule PASSED), five of nine built-in rules had no positive fixture (disabling all five matchers PASSED), confidence was consumed by the same ledger that produced it (constant 1.0 everywhere PASSED), the five false-positive categories were gated only in aggregate (2/1/0/0/14 rewritten to 0/0/0/0/17 PASSED), and edge rows never validated windowHours or the null-lift condition (-999 windows and all-null lifts PASSED). It adds learnedPairs rows, a re-derived causal-rule roster, per-rule probes with one-clause near-misses, ruleCoverage, a confidence saturation/degeneracy guard, per-category false-positive growth gates, and miner-window validation. schemaVersion 8 answers a tenth round that pointed at the last circular seam: every check ran inside the process that produced the report, against truth from the same commit, so a change INSIDE runCorrelationBenchmark() moved the report and the comparator's re-run identically — deranged pair attribution, collapsed learned-pair rows, forged probe text and a deleted metric all passed, because none of them move an aggregate the committed file pins. ledgerDigest is the anchor that lives outside the process: a 128-bit digest of the edge, pair, learned-pair and probe rows, written into this file by the human who re-seeded it. A real miner change now fails on identity and must be re-seeded in a reviewed diff, which is the intended cost. It also makes edgeEvidenceSeparation nullable on the committed side (a perfect miner reports no false edges, and requiring a number here made the goal state un-seedable) and hardens the reproduction walk to own, plain-object keys (Object.create(realReport) had no own keys, serialized as {}, and reproduced). schemaVersion 9 answers an eleventh round that showed schemaVersion 8 pinned the wrong half of the report: ledgerDigest covered the row-level ledgers, so a producer-side change could still DELETE meanCausalEdgeStrength or meanCausalEdgeZ, or forge causalCouplingsLostToCap, meanFalsePairConfidence or confidenceSeparation, and return a clean PASS — the summary layer where the advertised measurements live was pinned by nothing but re-derivation inside the process that produced it. reportDigest replaces it and covers EVERY field, containers included (an array emits its length and an object its sorted key list before their contents, so a deleted field is not merely fewer records). Number quantization moved from 4 decimals to 9: at 4 a uniform strength-0.00001 applied across the whole producer reproduced the digest exactly, and 9 still sits five orders of magnitude above the libm last-bit noise that made rounding necessary in the first place. The reproduction walk now reads each field ONCE from its own property descriptor (Reflect.ownKeys, data descriptors only, enumerable only): an enumerable getter had been returning the real value to the comparator and a different value to the next consumer, and an extra non-enumerable own property was invisible to Object.keys and to JSON.stringify but not to a plain read. A baseline seeded from a perfect run (null separation, zero false edges) now forces falseEdgeGrowth to 0 rather than inheriting the tolerance, which is what the separation-exemption comment had always claimed. The cost of pinning the whole report is that an IMPROVEMENT also fails, on identity — `npm run bench:correlation -- --seed` prints the block to commit, and that re-seed diff is where a human reads the new numbers.",
  "streamCount": 10,
  "observationCount": 378,
  "plantedCausalCount": 5,
  "truePairUniverse": 22,
  "corpusDigest": "8411c23a6f009f2245ec779a7593685e",
  "reportDigest": "769323700a8092d40932c863b30bdedb",
  "builtInRuleIds": [
    "airquality-wildfire",
    "biosurv-aviation",
    "conflict-displacement",
    "earthquake-infrastructure",
    "earthquake-tsunami",
    "sanctions-maritime",
    "space-weather-infrastructure",
    "weather-aviation",
    "weather-wildfire"
  ],
  "ruleCoverage": [
    "earthquake-tsunami",
    "sanctions-maritime",
    "space-weather-infrastructure",
    "weather-wildfire"
  ],
  "couplingPrecision": 0.2273,
  "couplingRecall": 1,
  "minedEdgeCount": 256,
  "significantEdgeCount": 22,
  "confoundedFalsePositives": 2,
  "mediatedFalsePositives": 1,
  "independentFalsePositives": 0,
  "inhibitoryEdgesReported": 0,
  "unplantedFalsePositives": 14,
  "falseEdgeCount": 17,
  "edgeEvidenceSeparation": 8.4898,
  "learnedRuleCount": 12,
  "learnedRuleFalsePositives": 9,
  "causalLearnedRuleCount": 3,
  "enginePairCount": 22,
  "distinctEnginePairCount": 22,
  "pairPrecision": 1,
  "pairRecall": 1,
  "decoyPairsEmitted": 0,
  "meanTruePairConfidence": 0.5401,
  "learnedRulePairCount": 101,
  "causalLearnedRulePairCount": 19,
  "minCausalLearnedRulePairCount": 6,
  "tolerances": {
    "couplingPrecisionDrop": 0.02,
    "couplingRecallDrop": 0,
    "pairPrecisionDrop": 0,
    "pairRecallDrop": 0,
    "edgeEvidenceSeparationDrop": 1,
    "meanTruePairConfidenceDrop": 0.05,
    "falseEdgeGrowth": 0,
    "causalLearnedRuleShrink": 0,
    "learnedRuleFalsePositiveGrowth": 0,
    "learnedRulePairGrowth": 5,
    "causalLearnedRulePairShrinkRatio": 0.5,
    "enginePairShrink": 0,
    "minedEdgeCountShrink": 10
  }
}
