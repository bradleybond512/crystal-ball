{
  "title": "Cross-Domain Correlation Examples",
  "examples": [
    {
      "description": "Find connections between active conflicts and cyber threats",
      "steps": [
        "correlate({ domains: [\"conflicts\", \"cyber\"] })",
        "// Review shared entities (countries, IPs) appearing in both domains",
        "// Follow up on a specific country:",
        "search_conflicts({ country: \"Ukraine\" })",
        "lookup_ip({ ip: \"185.220.101.1\" })"
      ]
    },
    {
      "description": "Investigate market-military connections in the Middle East",
      "steps": [
        "correlate({ domains: [\"markets\", \"military\"], region: \"Middle East\" })",
        "// Check if oil prices correlate with military activity:",
        "trend({ source: \"markets\", metric: \"oil_price\", window: \"30d\" })",
        "get_military_posture()"
      ]
    },
    {
      "description": "Three-way correlation: conflicts, weather, and health crises",
      "steps": [
        "correlate({ domains: [\"conflicts\", \"weather\", \"health\"] })",
        "// Regions with all three signals are under compound stress",
        "// Drill into a specific region:",
        "get_region_brief({ place_name: \"Horn of Africa\" })"
      ]
    },
    {
      "description": "Chain query to follow a correlation thread",
      "steps": [
        "correlate({ domains: [\"conflicts\", \"cyber\"], region: \"Eastern Europe\" })",
        "// Take a shared IP from the results and investigate:",
        "chain_query({ steps: [{ endpoint: \"/api/threatfox\", params: { query: \"185.220.101.1\" } }, { endpoint: \"/api/abuseipdb\", params: { ip: \"185.220.101.1\" } }] })"
      ]
    }
  ]
}
